import { MapMark } from "@bumpinto/web";

/** W2 · bekleme ekranının harita işareti — kesikli halka, iki gri nokta ve
    gradyan iğne. Salt dekoratif, props almaz. */
export function Mark() {
  return <MapMark />;
}
