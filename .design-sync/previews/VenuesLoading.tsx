import { VenuesLoading } from "@bumpinto/web";

/** W3e · host "Mekanları bul"a bastığı andan liste gelene kadar: dört iskelet satırı
    + el yazısı not. */
export function Searching() {
  return <VenuesLoading name="Cuma kahvesi" />;
}
