import { Wordmark } from "@bumpinto/web";

/** Her web ekranının ilk öğesi: gradyan iğne + "BumpInto" kilidi.
    Props almaz — metin i18n'den (`common.wordmark`) gelir, tek kanonik hücre. */
export function Lockup() {
  return <Wordmark />;
}
