# BumpInto — Plan Index

Spec'ler: `docs/superpowers/specs/2026-08-31-bumpinto-mvp-design.md` (MVP) ·
`2026-09-01-web-tailwind-i18n-design.md` (web stil/i18n) ·
**`2026-09-01-web-parity-design.md` (rev 2 — web = tam ürün, oturum tipi, Mekanlar, Google haritası)**.

UI kaynağı (bağlayıcı): Claude Design `719fcd5f-…` — **2026-09-06'dan itibaren `Web Ekranlar v3.dc.html`
(72 artboard, mobil v3 ile senkron; W13–W20 yasal/hesap sayfaları dahil), `Mobil Ekranlar v3.dc.html`, `Mobil Onboarding, İzinler ve Yasal.dc.html`**
(v2 dosyaları arşiv; kullanıcı mobil v3'ü beğendi, web senkronu istedi) ve `b536b3aa-…` (`Design System v2.dc.html` §06–§10).

**Tasarım denetimi (2026-09-02) — iz üstü bağlayıcı kaynak.**
`docs/superpowers/specs/2026-09-02-design-audit-findings.md`. **W-3/W-4/M-2 başlamadan okunur;
artboard'ın yanlış olduğu yerlerde o dosya bağlayıcıdır.** İzlere düşen bulguları: K-W1, K-W3, K-M1.
İz üstü kalan iki çelişki: DS chip 46px ↔ spec 44px, kesik pin iki anlam taşıyor. Tasarım dosyaları
değiştirilmedi (Claude Design'da yama arayüzü yok; 174KB'lık artboard dosyasını yeniden yazma riski
alınmadı).

**Haritasız uzlaşma karar dokümanı (2026-09-03) — iz üstü bağlayıcı kaynak.**
`docs/superpowers/specs/2026-09-03-map-free-group-decision-ux.md`: ürün tezi (adalet → ilgi uyumu → uzlaşma →
ortak an), §4 tasarım kararları (adalet kıstası, TravelChips, tek rozet, ulaşım türü, adalet öncelikli sıra,
harita politikası, dil sözlüğü), §5 iş paketleri, §6 YAPMA listesi. İzlere düşen planlar: **B-7, W-6, M-3**;
K-görevleri: K-W8, K-B11/13/15. Artboard'lar bu karara göre yeniden çiziliyor (kullanıcı onayı bekliyor).

**Mobil tasarım yönü + mağaza uyumluluğu (2026-09-06) — iz üstü kaynak, KULLANICI ONAYI BEKLİYOR.**
`docs/superpowers/specs/2026-09-06-mobile-design-direction.md` (rakip analizi, mevcut tasarım eleştirisi,
8 iyileştirme, kabuk kararları, 6 onay maddesi) ve `2026-09-06-mobile-store-compliance.md` (Apple/Play/KVKK
zorunlu 17 ekran + form işleri). Claude Design `719fcd5f-…`: **`Mobil Ekranlar v3.dc.html`** (P0–P26, 26 ekran)
ve **`Mobil Onboarding, İzinler ve Yasal.dc.html`** (O1–O19). Onaylanırsa M-1/M-2/M-3 UI kaynağı bu iki dosya
olur; `Mobil Ekranlar v2` eskir. İzlere düşen adaylar: K-B27–K-B29, K-W14, K-M3.
**Mobil yığın kararı (2026-09-07) — iz üstü BAĞLAYICI.** Mobil uygulama **Expo** ile yazılır:
CNG prebuild + **dev build** (Expo Go YOK) + EAS build/submit. Bare React Native **reddedildi** —
RN 0.82'den beri New Architecture her iki yolda da zorunlu ve kapatılamaz, ihtiyaç duyulan yerel işler
(WebRTC, Google Sign-In, Live Activity/widget) config plugin ve `expo-widgets` ile karşılanıyor, CNG tek
yön değil (`expo prebuild` çıktısı gerektiğinde commit edilip bare'e geçilebilir; tersi pahalı).

**Sürüm politikası (2026-09-07, kullanıcı talimatı — BAĞLAYICI):** *her paket latest.* Hedef
**Expo SDK 57** (`expo` 57.0.x → React Native **0.86.2**, React **19.2.3**, `targetSdk`/`compileSdk` **36**,
`minSdk` 24). Planlardaki eski **SDK 54** hedefi geçersizdir. "latest"in iki anlamı ayrılır:
Expo'nun yönettiği yerel modüller için latest = **`expo install`in SDK 57 için çözdüğü sürüm**
(npm `latest` DEĞİL — prebuild'i kırar); saf JS paketleri için latest = npm `latest`.
Tam kural: **plan38 (M-4) "Sürüm politikası" bloğu** — M-5/M-6/M-7/M-8/M-9/I-3 oraya atıf yapar.
Bumpun getirdikleri: New Arch zorunlu · reanimated **4** + zorunlu `react-native-worklets`
(babel eklentisi `react-native-worklets/plugin`) · gesture-handler **3** (Gesture API, eski
`PanGestureHandler` yok) · `expo-widgets` first-party (M-9 T7 revizyon notu) · `expo-maps` mevcut ama
~~`react-native-maps` 1.27'de kalındı~~ → 2026-09-08 kaldırıldı, harita MapLibre (K-M2) · plan44'ün target API 36 / 16 KB kapısı SDK 57'de zaten karşılanıyor
(kapı **regresyon bekçisi** olarak durur).

**React 19 — YAPILDI (2026-09-07).** İlk değerlendirmem yanlıştı: iki React majörü M-4'ü *blokladı*.
Mobil kurulunca hoisting root'u 18 → 19'a çevirdi, `@testing-library/react` ayrı bir React kopyası aldı
ve **332/520 web testi "Invalid hook call" ile düştü**. Çözüm: `pnpm-workspace.yaml`'a
`overrides: react/react-dom = 19.2.3` (Expo SDK 57 pini) + `frontend/web` aynı sürüme çekildi + tek satır
kaynak düzeltmesi (`ShareCard.tsx` `RefObject<HTMLDivElement | null>`). **Web 520/520 test + build yeşil.**
Override **silinmez**. Ayrıca `allowBuilds`'e `@parcel/watcher` ve `unrs-resolver` eklendi.

**Bağımlılık sürüm yönetimi (2026-09-07, BAĞLAYICI).** Üç mekanizma, üç ayrı iş:
`overrides` = `react`/`react-dom` 19.2.3 (transitive dahil zorlar; `@testing-library/react` gibi dış
paketleri de aynı kopyaya çeker — **silinmez**) · `catalog:` = web+mobil+shared'in paylaştığı saf-JS
paketleri (`zustand`, `i18next`, `react-i18next`, `axios`, `@stomp/stompjs`, `typescript`), sürüm
`pnpm-workspace.yaml`'da tek yerde, `package.json`'a `"catalog:"` yazılır · **hiçbiri** = Expo'nun
yönettiği yerel modüller, `expo install` sahibi. React'i `@bumpinto/shared`'dan re-export etmek
**çözüm değildir** (JSX dönüşümü `react/jsx-runtime`'ı sabit gömer, yönlendirilemez); shared ileride
React'e ihtiyaç duyarsa `peerDependency` olarak tanımlanır. TypeScript web/shared/mobil **6.0.3**'te
birleşti (npm latest 7.0.2 bilinçli alınmadı — Expo SDK 57 pini). Doğrulandı: web 520/520 + build,
mobil typecheck + 2/2, `expo install --check` temiz, `i18n:check` 0 fark.

**M-4 T1 saha düzeltmeleri (2026-09-07):** SDK 57 gesture-handler'ı **~2.32** (3.x DEĞİL) ·
`babel.config.js` yazılmaz (`babel-preset-expo` worklets eklentisini kendi ekler) · **RNTL 14'te `render`
Promise döndürür → tüm testlerde `await render(...)`**, `extend-expect` yok · jest `expo install` ile
pinlenir (~29.7), `@latest` ile değil · `tsconfig` `types: ["jest"]` · şablon ekranları `src/app`'ten kök
`app/`'a taşındı. Tam liste: plan38 "T1 saha notları".
**Açık risk:** M-6'nın `react-native-webrtc` 124.0.8 + `@config-plugins/react-native-webrtc` 15.0.2
(peer `expo >=56`, SDK 57 sürümü yok) + `react-native-incall-manager` 4.2.2 üçlüsü New Arch'ta
doğrulanmadı → plan40'ta **T2 risk kapısı**. M-4'ü bloklamaz.

**Gereksinim analizi (2026-09-06 gece) — bağlayıcı:** `docs/superpowers/specs/2026-09-06-v3-requirements.md`
(destek matrisi, sözleşme kararları, R-B/R-W/R-M gereksinimleri, plan paketleri **B-14, B-15, W-13, W-14, W-15,
M-4, M-5, M-6** ve sıra; Flyway V12–V17 rezervi). Planlar henüz yazılmadı; kullanıcı kararı §5.

## Kimlik şeması

Planlar bileşen **izlerine** ayrılır; her iz kendi harfiyle numaralanır ve kendi tablosunda yönetilir.

| Harf | İz | Kapsam |
|---|---|---|
| `B` | Backend | Spring Boot uygulaması — domain, application, adapter, DB şeması, zamanlanmış işler |
| `W` | Web | `frontend/web` + `frontend/shared` |
| `M` | Mobil | `frontend/mobile` (Expo) |
| `I` | Altyapı | CI, imaj, K8s, dağıtım — tek bir bileşene ait olmayan, hepsini besleyen işler |

Yeni plan, ait olduğu izin bir sonraki numarasını alır. Sıradaki NUMARALAR: **B-20, W-19, M-13, I-3** *(**B-18/W-18/M-12 = "Buradayım" izi, plan47/48/49, 2026-09-11 açıldı; B-19 = Arkadaşlar, aday**; B-16/W-16/M-10 = plan45 yenileme jetonu, 2026-09-09 **done**; **B-17/W-17/M-11 = Keşfet izi, 2026-09-09 açıldı** — plan gövdesi onları `B-17/W-16/M-10` diye anıyor, W/M numaraları bayat)*. *(Bu satır numaralandırma içindir — yürütme sırası DEĞİL; onun için kritik yola ve bağımlılık grafiğine bak. M-4 bitince M-5 ve M-7 birlikte açıldı; **M-5 kritik yolda**, M-7 değil. **2026-09-08: M-5, M-7 ve M-8 done — v3 mobil izi (M-4 → M-7 → M-8) kapandı;** sıradaki mobil işler M-6 (sesli sohbet, plan40) ve M-9 (v3 cilası, plan43), ikisinin de bağımlılıkları karşılandı.)* (M-4 done, 2026-09-08) (W-15 done, 2026-09-07) (B-13/W-12/I-2 = plan30/31/32, 2026-09-06) (B-8…B-11 ve W-7…W-10 plan17–27 ile kullanıldı; tablo satırları eksik, spec başlıklarında kayıtlı).

Her izin plan tablosunun altında ikinci bir tablo var: **spec dışı görevler** (`K-B1`, `K-W4`,
`K-M2` …) — planlama ve yürütme sırasında bulunmuş, spec'te yer almayan işler, sapmalar, kararlar ve
adaylar. Kalem değil **görev** olarak yazılır: paket bulgular atomik satırlara bölünür. Numaralar
sabittir; yeni görev tablonun **sonuna** eklenir, sıra plan sırasını izler.

**Dosya adları tarihsel şemada kalır** (`2026-09-01-plan3-web.md`). `Eski #` kolonu iki şema
arasındaki tek çeviri anahtarıdır: plan gövdelerindeki "Plan 2", "Plan 5 Task 3" gibi çapraz
referanslar hâlâ eski numarayı kullanır — hangi kimliğe karşılık geldiğini o kolondan oku.

## Ajanlar için bağlayıcı kurallar

1. Bir planı yürütmeye başlarken bu dosyada o planın **Durum** alanını `in-progress` yap.
2. Her görev bitişinde **Son adım** alanını güncelle (ör. `Task 3/8 bitti`).
3. Plan tamamlanınca **Durum** → `done`, **Not** alanına tek satır özet.
4. Engellenirsen **Durum** → `blocked`, **Not** alanına neden + ne gerektiği.
5. Bu dosyayı yalnızca düzenle — git commit'i kullanıcı yapar (AGENTS.md).
6. **Her iz kendi içinde sıralıdır; sırayı kimlik numarası değil aşağıdaki "Yürütme sırası" bölümü
   belirler** (ör. B-3 retention, B-5 ve B-6'dan SONRA koşar). **Farklı izler eşzamanlı koşabilir.**
   6a. Bir plana başlamadan önce **Bağımlılık** kolonundaki her kimliğin durumunu bu dosyadan
   doğrula. Görev-seviyeli bağımlılık (`I-1:T3`) yalnız işaretlendiği görev bloğunu kapatır —
   planın geri kalanı beklemez.
   6b. `deferred` planlar iz akışına GİRMEZ, atlanır.
7. **UI işlerinde tasarım kaynağı Claude Design'dır** — ilgili planın "UI Kaynağı" bölümüne uy;
   ajan kendi tasarımını yapmaz.
8. **Bir planın "Ek A" bölümü varsa gövdeden ÖNCE okunur ve çelişkide kazanır** (M-1, I-1, B-3).
9. **Flyway numara sicili (2026-09-06 düzeltildi, dosyalarla eşlendi):** V1–V2 mevcut · V3 = B-5 · V4 = B-6 ·
   V5 = B-7 · V6 = A-zinciri (participant token imzalı) · V7 = A4 (participants.user_id) · V8 = B-9 (çoklu
   aktivite) · V9 = B-10 (çapa) · **V10 = B-13** (`provider_usage` + venues alanları) · **V11 = B-13** (PostGIS +
   `venues_open`; I-2 ithal işleri bu tabloya yazar). **V12 = B-3** (plan 6; gövdesindeki "V5/V6" notları bayat) · **V13–V16 = B-14 ✓ UYGULANDI** (Apple, soft delete, rapor/engel, rıza) ·
   **V17 = B-15 ✓ UYGULANDI** (`venues.tagline`) · **V18 = B-15 ✓ UYGULANDI** (`participants.last_seen_at`/`link_opened_at`, `sessions.join_code`) · **V19 = B-16 ✓ UYGULANDI** (`refresh_tokens`: `user_id`, `token_hash` SHA-256 UNIQUE, `family_id`, `rotated_from`, `client`, `issued_at`/`expires_at`/`revoked_at`; `user_id` FK ON DELETE CASCADE) · **V20 = B-17** (açık plan: `sessions.meet_at/capacity/join_policy` + `seat_requests` + `meet_checkins`) · **V21 = B-17** (`users.interests`) · **V22 = K-B26 ✓ UYGULANDI** (`venues.maps_url` düşürüldü, 2026-09-11; yerel DB'ye indi) · **V23 = B-18 ✓ UYGULANDI** (`sessions.open_until/audience/locality` + backfill + Keşfet indeksi, plan47; 2026-09-11 yerel DB'ye indi — codegen için 8061'de kaldırılan backend migrate etti) · **V24 = B-19** (`friendships`, aday) · V25 = push/device_tokens (rezerv — **2026-09-09: V20 rezervasyonu Keşfet'e devredildi**; **2026-09-11: V22'yi K-B26 fiilen aldı, push V23'e kaydı; 2026-09-11 akşam: V23'ü B-18, V24'ü B-19 aldı, push V25'e kaydı** — push'un planı henüz yok, Flyway numarası fiilen inen migration'ındır). Foto karuseli açılırsa V21+. Yeni migration açan plan burada numara alır; `outOfOrder` hep kapalı.
10. **Spec dışı bulguları plan gövdesinde bırakma, bu dosyaya yaz.** Yürütme sırasında spec'te
    olmayan bir şey çıkarsa (yeni uç, bilinçli sapma, sonraki plan adayı, maliyet/araç kararı)
    ilgili **izin** "Spec dışı görevler" tablosuna, sıradaki `K-<iz><n>` kimliğiyle tablonun
    **sonuna** ekle. Bulgu birden çok işi paketliyorsa her işi ayrı satır yap.
    10a. Bir kalem **tek** ize düşer. Birden çok izi bağlayan kaynak veya karar dosyanın **üst
    bloğuna** (başlık altındaki spec/UI kaynağı bloğu) yazılır; izlere düşen sonuçları ilgili
    tablolarda K-görevi olur ve üst bloğa referans verir (ör. tasarım denetimi → K-W1, K-W3, K-M1).
    10b. **Yeni `## B` / `## W` / `## M` / `## I` bölümü açılmaz** — her iz bu dosyada tek yer
    tutar: plan tablosu + spec dışı görev tablosu.

**Plan durumları:** `ready` (yazıldı, yürütülmedi) · `in-progress` · `blocked` · `done` ·
`deferred` (yazıldı, bilinçli olarak yürütülmüyor) · `superseded` (yerini başka plan aldı; M-1/M-2/M-3 → M-4, 2026-09-06)

**Spec dışı görev durumları:** `done` (uygulandı ya da karar verilip yürürlüğe girdi) ·
`açık` (yapılacak iş, hedef planı belli) · `aday` (henüz plan açılmadı — hedef plana aday)

**Bağımlılık gösterimi:** `B-2` = o planın **tamamı** `done` olmalı ·
`I-1:T3` = yalnız o planın 3. görevi · `—` = bağımlılık yok · `✓` = koşul şu an sağlanıyor.

---

## Yürütme sırası (2026-09-02, rev 2 spec'inden türetildi)

Üç şerit paralel koşar; oklar zorunlu sırayı gösterir.

```
Backend   : B-5 ✓ ──> B-6 ✓ ──> B-7 (V5) ──> B-3 (V6, T1–T4) ──┐
Web       : W-3 ✓ ──> W-4 ✓ ──> W-5 ✓ ──> W-6a (hemen) ──> [B-7:T1–T4] W-6b ──┤
Mobil     : [B-6] M-1 (Ek A) ──> [B-5, W-4] M-2 ──> [B-7, W-6] M-3 ──────────┤
Altyapı   : I-1 T1–T3 (hemen) ──> [B-3 T5 ← I-1:T3] ──> I-1 T4 ◄──────────────┘  (yayın kontrol listesi)
```

**2026-09-03 güncellemesi:** B-7 açılış öncesi yürütülür (V5); B-3 V6'ya kaydı. W-6a B-7'yi beklemez.

Kritik yol (güncel): **B-7 → W-6b → M-1 → M-2 → M-3 → I-1:T4**. W-6a, B-7 ve I-1 T1–T3 aynı anda başlayabilir.

**2026-09-06 v3 güncellemesi (gereksinim dok. §4):**

```
Backend : B-3 (V12) ──> B-14 (V13–V16) ──> B-15 (V17–V18) ──> B-16 ✓ (V19 yenileme jetonu) ──> [push, sonraki iz]
Web     : W-12 ✓ ──> W-13 ──┐        [B-14] W-14 ──> [B-15, W-13] W-15
Mobil   : [mevcut API] M-4 ──┬─> M-7 ──> M-8 ──> [B-15] M-9 ✓ (v3 cilası)
                             ├─> [B-14, W-14] M-5              ├─> [M-7, B-14] M-6
Altyapı : [M-4] I-3 T1–T5 (CI/build/e2e) ──> [M-5] I-3 T6–T9 (submit, görseller, runbook)
```

Kritik yol (v3, mağaza yayını): **B-3 → B-14 → W-14 → M-5 → I-3:T6**. Hemen başlayabilir: B-3, M-4, W-13 (W-12 bittiyse).
M-7 → M-8 → M-9 ürün akışı M-5 ile paralel; M-6 M-7'yi bekler. (2026-09-06 gece: plan38 üçe bölündü — M-4 temel, M-7 kurma/katılım, M-8 karar akışı; M-9 cila ve I-3 yayın hattı eklendi.)
M-1/M-2/M-3 `superseded`; I-1:T4 yayın kontrol listesi artık M-5:T11 (`RELEASE-CHECKLIST.md`) ile birlikte okunur.

**2026-09-11 "Buradayım" güncellemesi (spec `docs/superpowers/specs/2026-09-11-instant-plan-badges-design.md`):**

```
Backend : B-17 ✓ ──> B-18 (V23, plan47) ──> [aday] B-19 (V24 arkadaşlar)
Web     : [B-17] W-17 W1 (hemen) ──> [B-18] W-18 T1 (shared) ──> W-17 W2–W7 + W-18 T2–T6 (W2 birleşik, kilit 22)
Mobil   : [M-4 ✓, M-7 ✓, B-17] M-11 M1 ──> [W-18 T1] M-11 M2 + M-12 T1–T2 (birleşik, kilit 23) ──> M-11 M3–M5 + M-12 T3–T5
```

Kritik yol: **B-18 → W-18 T1 → W-17/W-18 → M-11/M-12**. Shuffle eşleşmesi (K-B39) ölçülebilir kapı arkasında; B-19 plan dosyası B-18 bitince yazılır.
**2026-09-11 gece: B-18 ✓ ve W-17 + W-18 ✓ (birleşik, inline). M-11 + M-12 ✓ (birleşik, inline — sapmalar ve cihaz borcu satırlarında; manuel test ve commit kullanıcıda).** Keşfet + "Buradayım" izi üç platformda kapandı; açık karar K-B40; sıradaki aday B-19 (arkadaşlar, plan dosyası yazılmadı).

---

## B — Backend

| Kimlik | Plan | Dosya | Eski # | Durum | Bağımlılık | Son adım | Not |
|---|---|---|---|---|---|---|---|
| B-1 | Backend iskelet + alan çekirdeği + karar motoru | `2026-09-01-plan1-backend-core.md` | Plan 1 | done | — | Task 8/8 + final review | 23/23 test yeşil (BUILD SUCCESS); domain saf, sıfır TODO; commit'ler kullanıcıda |
| B-2 | Application + adapter katmanları (API, Security, Unirest, STOMP) | `2026-09-01-plan2-backend-api.md` | Plan 2 | done | B-1 | Task 10/10 + 2 temizlik turu + kapanış denetimi | 119/119 test yeşil (temiz build); sıfır TODO/ölü kod; ArchUnit 3 kural; subagent-driven (impl Opus / review Fable); commit'ler kullanıcıda |
| B-5 | **Oturum modeli rev 2** — `SessionType`, `BROWSING`, `shuffle`, elle konum (`points`), yuvarlanmış konum + şehir etiketi, orta nokta/yarıçap, BROWSING'de "Bunu seç", runoff kilitleyenler | `2026-09-02-plan9-backend-session-model-v2.md` | Plan 9 | done | B-2 ✓ | Task 6/6 + orkestratör borç kapatma turu | 140/140 test yeşil (temiz build), web tsc + 5/5 web testi yeşil, sıfır TODO. Migration **V3**. Subagent-driven (impl Sonnet / review Opus, 19 ajan). Spec §8 kalem 4–9 kapandı. **Plan düzeltmesi:** planın `midpoint`/`radiusKm`'i ham döndüren kod bloğu, kendi "tam koordinat API'den asla çıkmaz" değişmezini çiğniyordu — 2 kişilik oturumda orta nokta diğerinin tam konumunu veriyordu; ikisi de yuvarlandı. Katılımcı sırası `joinedAt`'e göre deterministik yapıldı; `participant_left` olayı eklendi (removePoint sessizdi) |
| B-6 | **Hesap ve liste API'leri** — `GET /api/sessions`, `GET/PUT /api/me` (tercihler + dil), `POST /api/auth/logout`, `GET /sessions/{slug}/preview` (kamu), `SessionView.viewer` | `2026-09-02-plan10-backend-account-api.md` | Plan 10 | done | B-5 ✓ | Task 4/4 + kapanış incelemesi | 149/149 test yeşil (temiz build), web build + 5/5 yeşil, sıfır TODO. Migration **V4**. Subagent-driven (orkestrasyon Fable / impl Sonnet / review Opus; Task 3 → 3a+3b). Spec §8 kalem 1–3 + `preview` (host adı, kişi sayısı, `participants[{ad, host, hasLocation}]`) + `SessionView.viewer`. Tasarım denetimi (§9) gereği `readyCount`/`doneCount`/`decidedVenuePhotoUrl` eklendi; artboard'lar düzeltildi. **Bayat çerez hatası kapatıldı** (`PUBLIC_ENDPOINTS`). M-1'in cihaz-yerel liste tavizi kalktı. Sapmalar aşağıda; commit'ler kullanıcıda |
| B-3 | Veri saklama — süresi dolan oturumların kalıcı silinmesi | `2026-09-01-plan6-data-retention.md` | Plan 6 | done | B-6 ✓ | Task 5/5 + tam regresyon | Spec §6 GDPR. **Migration V12** (gövdedeki V5/V6 bayat). **399 test yeşil (6 env-gated skip).** Plana 2026-09-07 Ek A yazıldı — gövdeden önce okunur. **Kapsam kararı 3 kullanıcı talimatıyla ezildi: K8s CronJob değil, uygulama içi `@Scheduled`** (K-B30) → Task 5'in manifest'i yazılmadı, I-1:T3 bağımlılığı düştü. Çok-pod tekilleştirmesi `for update skip locked` ile (leader election/ShedLock yok). `PurgeRunner` → `adapter.in.job.SessionPurgeJob` (cron 03:30 UTC, `SESSION_PURGE_CRON`, `RETENTION_ENABLED` arkasında). Yeni testler: cascade kanıtı (V1'den beri testsizdi), sınır katılığı, ikinci-pod atlaması, zamanlayıcı kaydı. Yan bulgu K-B32: `retentionScheduler` kapanışta 5 sn timeout'a düşüyordu (cron görevi gecikmeli kuyrukta bekliyor) — düzeltildi, suite 78s→50s. I-1 yayın kapısı artık işaretlenebilir |
| B-4 | Dinamik aktivite keşfi — self-host Overpass (OSM) | `2026-09-01-plan7-activity-discovery.md` | Plan 7 | superseded | iz akışı dışı | — | **YÜRÜTÜLMÜYOR — 2026-09-06 spec `2026-09-06-open-hybrid-venue-stack-design.md` ile geçersiz: açık taban Overpass yerine Overture + OSM ithali (I-2/plan32), OPEN türleri B-13 `OpenVenueSource`.** Yerine ucuz yol seçildi: `ActivityType` 5→15 genişletildi (B-2 kodu üzerinde, 123/123 test). Açılırsa API sözleşmesi değişir → W-1/W-2/W-3/W-4/M-1/M-2 geriye dönük düzeltme; Task 7 (K8s) `I-1:T3`'e bağımlı |
| B-7 | **Adalet çekirdeği, ulaşım türü, karar şeffaflığı, sağlayıcı alanları, bütçe tavanları** — `TravelMode` + hıza ters ağırlıklı orta nokta, yuvarlanmış konumdan dakika (gizlilik), mekan başına `{maxMinutes, spreadMinutes}`, adalet öncelikli `deckOrder`, `decisionKind`/`decidedAt`/`runoffReason`/`likeCounts`, RUNOFF oy gizliliği, `midpointLabel` (Nominatim backend), Google aynı-katman alanları + FSQ Pro alanları, Nearby/Photo aylık tavanları | `2026-09-03-plan15-backend-fairness-travelmode.md` | Plan 15 | done | B-6 ✓ | Task 1a ✓ (V5, TravelMode, ağırlıklı centroid; 186 test yeşil) · Task 1b ✓ (travelMode API + /api/me defaultTravelMode + Bruno) · Task 1c ✓ (TravelMinutes/Fairness/DeckOrdering, ağırlıklı orta nokta, `VenueDto.fairness`, `ParticipantDto.midpointMinutes`, idempotent shuffle) · Task 2a+2b ✓ (DecisionKind/RunoffReason, decidedAt, likeCounts yalnız DECIDED, voteTally gizlilik kapısı, runoffVotes sızmıyor) · Task 3 ✓ (ReverseGeocodePort + Nominatim adapteri, UA/1 istek-sn/Caffeine, `midpointLabel` find-venues'ta bir kez) · Task 4a ✓ (Venue/VenueCandidate +6 alan, Google maskesi aynı katman: primaryTypeDisplayName/businessStatus/shortFormattedAddress/userRatingCount/regularOpeningHours/addressComponents, OPERATIONAL filtresi foto çözümünden önce) · Task 4b ✓ (FSQ Pro-only FIELDS `fsq_place_id,name,latitude,longitude,categories,location,website`, `VenueDto` +provider/category/address/locality/ratingCount/hoursToday/placeLink düz, mapsUrl yol tarifi fallback) · Task 5 ✓ (Nearby 1000/ay sert tavan — QuotaExceededException → FSQ'ya düşer; Place Photo 1000 görsel/ay, tükenince photoUrl null; `Quota` 3 arg) · Task 6 ✓ (openapi.json + api-types.ts yeniden üretildi, Bruno find-venues/shuffle docs, ARCHITECTURE alan modeli özeti; 230 test) · Kapanış incelemesi ✓ (2026-09-03): `PointRequest.travelMode` eklendi, `GeoMath.centroid(List)` silindi, V5 not-null testi, ARCHITECTURE §7 düzeltmeleri; **231 test yeşil**. Subagent-driven (impl Sonnet / review Opus, ~30 ajan). Kullanıcıda: commit, Bruno e2e (gerçek Google idToken), `:8060`'ta bırakılan `spring-boot:run` (PID 37675) — ajanlar IDE-debug JVM'ini iki kez öldürdü. Bilinçli sapmalar: shuffle kanonik taban (rating→externalId), TravelEstimate mod-suz overload silindi, Nominatim bloklayan throttle (K-B22), PARTIAL→Runoff'ta kind kaybı (karar dok. §7.7) | 10 görev bloğu (1a–1c, 2a–2b, 3, 4a–4b, 5, 6), TDD + Bruno. Karar dokümanı §4 + §5.A. Migration **V5**. Beraberlik puanla kırılır (§4.5); `ParticipantDto.midpointMinutes`, `VenueDto.locality` eklendi; `shuffle` adalet sırasını idempotent uygular (UI fiili "Karıştır ve kaydır" değişmez). Açılış maliyet modeli: Google bütçeli (1k Nearby + 1k foto/ay), FSQ Premium alanları çıkar, harita yok. K-B11/K-B13/K-B15 buraya düşer; K-B12 (paylaşımlı kota deposu) kapsam DIŞI kalır (Redis işi) |
| B-12 | **Sesli sohbet backend** — ses odası (süreç içi, 2h sert sınır), 3 REST ucu (`/voice`, `/voice/credentials`), STOMP sinyal relay (`/app/.../voice/signal` → özel konu), abonelikle üyelik, boş oturumda kapanış, Cloudflare TURN kısa ömürlü kimlik (STUN'a düşer), `SessionView.voice` + `inVoice` | `2026-09-06-plan28-voice-backend.md` | Plan 28 | done | B-8 ✓ | Task 8/8 + iki aşamalı inceleme | Spec `2026-09-06-voice-chat-design.md` K1–K12. `AppProps` +2 kayıt (11 test kurucusu), `requireHost` paylaşıldı. Cloudflare TURN anahtarı env'de yoksa `relay=false` (yan özellik, fail-open bilinçli). W-11 bu planın `openapi.json`'ını bekler. 365 test yeşil; inceleme ekleri: SessionGates, VoiceInboundGuard bütçeleri (20/240 dk), endsAt oturumu aşmaz, Tomcat 32 KB tamponu |
| B-13 | **Açık hibrit mekan yığını — backend** — `VenueSource` SPI (descriptor, YAML kategori eşlemesi, tek biçim config, 5 kurallı açılış doğrulaması, ArchUnit), tür başına sabit sıralı yönlendirme + `BudgetGate` (`provider_usage` V10), Foursquare **Premium** tek çağrı (foto/puan/popülerlik/saat), Google SPI'ye uyarlanmış ve inaktif, `OpenVenueSource` (V11 PostGIS `venues_open`), `MapLinks` + `VenueDto.ratingScale/popularity/travel[]`, saklama kuralı (saatlik), `GET /api/config`, `POST /api/geocode{,/reverse}`, `RoutingPort` + OSRM matrisi | `2026-09-06-plan30-open-hybrid-backend.md` | Plan 30 | done | B-10 ✓ · B-12 ✓ · *T6 için* `postgis/postgis` imajı (plan32 T1 ile aynı) | Task 11/11 + görev başına Opus incelemesi | **Backend 384 test yeşil (6 env-gated skip).** Spec `2026-09-06-open-hybrid-venue-stack-design.md` §3–§6, §8–§14. 11 görev. **Ön ölçümler (spec §16):** FSQ Premium curl (foto kapsaması), kümede PostGIS yetkisi. `PostgresContainer.shared()` → `postgis/postgis:16-3.4` (tüm entegrasyon testleri). `foursquare.yml`'de 10 kimlik doğrulanmamış → `FoursquarePremiumContractTest` ilk gerçek anahtarda kırmızı, düzeltmeyi o sürer. TripAdvisor (§5.3) bu planda DEĞİL: Terra API sözleşmesi ölçülünce ayrı küçük plan. `AppProps.Providers/Quota` silinir (12 test kurucusu `TestProps`'a). Yeni K-B26: `VenueDto.travelMinutes` W-12 sonrası silinir İnceleme ekleri: `ProviderUsageAdapter.increment` REQUIRES_NEW; degraded (kısmi hata) sonuç önbelleklenmez, kaynağa kova yarıçapı gider; OSRM 60 sn matris önbelleği + 60 sn geri çekilme; `venues.name` nullable (V10, saklama indirgemesi); FSQ/Google atıf yalnız seçilen türlere; geocode throttle → 429 `geocode_busy`, geocode 30/dk; V11 indeks `geography`. Bilinçli sıra sapması: T1a/T1b, T3→T4→T6 eklemeli, T5 geçiş (eski sağlayıcılar orada silindi). `openapi.json`/`api-types.ts` yenilendi (throwaway PostGIS :5435, backend :8061), web `tsc -b` temiz. K-B26 açıldı |
| B-14 | **Mağaza uyumluluk çekirdeği — backend** — V13–V16 (Apple, soft delete, rapor/engel, rıza), `AppleIdVerifier` (JWKS, çoklu audience, nonce) + `AppleTokenClient` (ES256 secret, refresh, revoke fail-open), `POST /api/auth/apple` + hesap birleştirme (`apple_sub` → e-posta), `MeResponse.consents/authProviders[]` + `PUT /api/me/consents`, `AccountDeletion` (host oturumları silinir, katılımlar anonimleşir, `purge_after=+30g`) + `DELETE /api/me` + `POST /api/me/delete-token`, `POST /api/reports` + `/api/me/blocks`, `ParticipantDto.blocked` + `VoiceAdmission` ses odası filtresi, OpenAPI/`api-types.ts` | `2026-09-06-plan33-backend-store-compliance.md` | Plan 33 | done | **B-3 ✓ (V12)** · B-12 ✓ | Task 13/13 + tam regresyon | Gereksinim dok. §2, R-B1–R-B5. **13 görev** (T12 hesap süpürmesi 2026-09-07'de eklendi — K-B33; belge görevi T13 oldu). **434 test yeşil (6 env-gated skip).** Migration **V13–V16**. `auth_providers` CSV (V8 deseni; API yine dizi). Apple ayarsızsa uç 503, uygulama açılır (Turn deseni) — `APPLE_*` anahtarları kullanıcıda, testler mock. `SecurityConfig` artık `typ` claim'i taşıyan HİÇBİR jetonu hesap jetonu saymaz (kara liste → beyaz liste). **V15 FK kararı (K-B33):** rapor `on delete set null` (moderasyon izi kalır, bildiren susturularak kayıt temizlenemez), engel `on delete cascade` — hesabın 30 gün sonra fiziksel silinebilmesi buna bağlı. Bilinçli sapmalar: `AppleTokenClient` statik `Unirest` yerine enjekte `UnirestInstance` (ev konvansiyonu); silme e2e testi `GET /api/sessions/{slug}` yerine public `/preview` ile doğrular (plan o ucu public sanmış); ses odası testi anonim koltuk engeliyle kurulur (fixture'da misafir hesapsız katılır). `openapi.json`/`api-types.ts` yenilendi (throwaway :8061), web `tsc -b` temiz. W-14 ve M-5 bu sözleşmeyi bekliyordu |
| B-15 | **v3 ürün cilası — backend** — V17 `venues.tagline` (FSQ `tips`/OSM türevi ≤80, ek çağrı yok), V18 `last_seen_at`/`link_opened_at`/`join_code`, `GET /api/me/export` (1/saat), `ParticipantDto.lastSeenAt/linkOpenedAt`, `POST /api/sessions/{slug}/nudge/{participantId}` (60 sn, `nudged` olayı), `SessionView.joinCode` + `GET /api/sessions/by-code/{code}`, `GET /og/{slug}.png` (java.awt, Caffeine, 24 s) + `GET /api/sessions/{slug}/og` | `2026-09-06-plan34-backend-v3-polish.md` | Plan 34 | done | **B-14 ✓** | Task 10/10 + tam regresyon | R-B6–R-B10. 10 görev. **490 test yeşil (6 env-gated skip, baz 434).** Migration **V17–V18** (bkz. Flyway sicili yukarıda). Subagent-driven (impl+review Sonnet/Opus çiftleri, görev başına tek inceleme turu + gerçek bulguda düzelt-doğrula döngüsü). Bu iz, planın öngördüğünden fazla gerçek hata inceleme sürecinde bulundu ve kapatıldı: **T1** `Venue.withDeckOrder` gerçek tagline/taglineSource'u değil null'ı ilettiği için deste yeniden sıralamasında tagline sessizce düşüyordu — düzeltildi. **T2** `OpenVenueSource` planın kendi taslağındaki ham `row.getCategory()` (makine etiketi, ör. `leisure=park`) yerine mevcut `categoryLabel(...)` insanileştirilmiş değerini kullanacak şekilde bağlandı — plan aynen uygulansa önceki bir düzeltme geri alınırdı. **T3** paylaşımlı rate-limit kova önbelleğinin 10 dk tahliye TTL'i yeni 1 saatlik export penceresinden kısaydı → uç fiilen ~6× daha gevşekti; TTL artık en uzun politika penceresinden türetiliyor. **K-B34 açıldı** (kapsam dışı bırakıldı): export kovası istemci IP'siyle ve kimlik doğrulamadan ÖNCE anahtarlanıyor; `TRUST_FORWARDED_FOR=false` (bu depoda her yerde varsayılan) ile kova dağıtım genelinde paylaşılabilir ve anonim bir prob 401'den önce onu tüketebilir — gerçek çözüm kontrolü auth'un arkasına taşımak, bu planın kapsamı dışında. **T4** join-code backfill migrasyonunda off-by-one (`random() * 31` yerine `* 32` olmalıydı — 32 karakterlik alfabede bir karakter istatistiksel olarak hiç üretilemiyordu) düzeltildi; `Session`'ın tüm kopyalama metotlarına join-code round-trip testi eklendi (önceden testsizdi); `PresenceStampsAdapter.stampsOf`'ta Hibernate Open-In-View kimlik haritasının aynı istek içinde `REQUIRES_NEW` damga yazımından ÖNCEki değeri döndürebildiği bayat-okuma tehlikesi bulundu, entity sorgusu yerine skaler/tuple projeksiyonuna geçilerek kapatıldı. **T5** korumasız bir presence-damga DB yazımı hem WebSocket yayınından hem ses odası grace-zili zamanlamasından ÖNCE durduğu için geçici bir DB hatası ses odasını sonsuza dek açık bırakabiliyordu; aynı sınıftaki mevcut desene uyan try/catch ile kapatıldı. **T6** planın kendi `InMemoryNudgeCooldown` taslağında gerçek eşzamanlı yükte olası bir yarış vardı: aynı saat anına denk gelen iki dürt belirsiz aynı-an karşılaştırması yüzünden ikisi de kabul edilebiliyordu; karar artık cache'in `compute` çağrısı içinde atomik veriliyor. **K-B35 açıldı** (kapsam dışı bırakıldı): dürt, ses odası kabulünün aksine `Blocks`'a bakmıyor — engelli bir çift birbirini dürtebiliyor; düzeltme belli (`compute`'tan önce `blockedWith(...)` kontrolü) ama uygulanmadı. **T7** Spring test-context sayısı × Hikari varsayılan havuzu paylaşımlı Testcontainers Postgres'inin `max_connections=100` varsayılanını aşmaya başladı (üretim hatası değil, test altyapısı) → test container'ında `max_connections=300`'e çıkarıldı. **T8** uzun kesiksiz bir kelime (kullanıcı oturum adında gerçekçi) satırın ilk kelimesi olmadığında sarma taşma korumasını atlayıp kart kenarlarının dışına taşabiliyordu → her satır tek bir sığdırma/kısaltma çıkışından geçirildi; ayrıca varsayılan `ImageIO.write` PNG yolu `java.io.tmpdir`'de geçici dosyaya dayandığından salt-okunur konteyner dosya sisteminde patlardı → doğrudan bellek-içi görüntü çıkış akışına geçildi. **T9** OG meta ucu, doğrulanmamış ham `slug` yol değişkenini hiçbir temizlik yapmadan mutlak URL'lere yansıtıyordu — HTML özel karakter taşıyan bir slug değişmeden geçip, downstream bir HTML şablonlama tüketicisi (web) bu URL'leri güvenli bir DOM API'si yerine string birleştirmeyle `<meta>` etiketine yazarsa depolanmış/yansıtılmış XSS riski oluşturuyordu; `UriUtils.encodePathSegment` ile iki URL de kodlanarak kapatıldı. **T10 (bu görev):** `backend/ARCHITECTURE.md` §3/§7/§11/§12/§14 ve `docs/CONFIGURATION.md` §1 güncellendi (`nudged` olay satırı + kural 7 — bölümdeki "Beş kural" başlığı da 7'ye düzeltildi, `domain/og`/`adapter/out/image` paket satırları, `Session.joinCode`/`Venue.tagline`/presence damgaları alan notları, `AppProps.og`, iki yeni borç satırı, `OG_CACHE`/`APP_BASE_URL`/`PUBLIC_API_BASE_URL` env satırları + fontconfig notu); `openapi.json`/`api-types.ts` throwaway `:8061`'de yeniden üretildi (grep doğrulaması ≥6: 9 eşleşme), web `tsc -b` temiz. Bilinçli sapmalar: FSQ `tips` alanı gerçek anahtarla ölçülmedi (null-tolere sözleşme, satır çizilmez); OG render konteynerde `fontconfig` ister, yoksa 500. **Açık kalan iki takip:** K-B34 (export rate-limit auth öncesi/IP-anahtarlı), K-B35 (dürt `Blocks`'u atlıyor) — ikisi de gerçek tasarım değişikliği gerektirdiği için bu planın kapsamı dışında bırakıldı, sonraki bir plana aday. Plan34 = plan iz'inin son görevi, **iz burada tamamlandı**. |
| B-16 | **Yenileme jetonu — backend** — `refresh_tokens` tablosu (V19: `user_id`, `token_hash` SHA-256, `issued_at`/`expires_at`/`revoked_at`, `rotated_from`, cihaz ipucu), `TokenService.issueRefreshToken` **opak rastgele** (JWT DEĞİL — iptal edilebilir olmalı), `POST /api/auth/refresh` (PUBLIC uç), **tek kullanımlık ROTASYON + yeniden kullanım tespiti** (kullanılmış jeton tekrar gelirse tüm aile iptal — hırsızlık sinyali), erişim jetonu TTL'i **12h → 15dk**, `logout` ve hesap silme (V14 soft delete) ailenin tamamını iptal eder | `2026-09-09-plan45-refresh-token.md` | Plan 45 | **done** (2026-09-09) | B-14 ✓ | — | **Neden:** bugün TEK erişim jetonu var (`token-ttl: 12h`), yenileme YOK; süre dolunca kullanıcı sessizce düşüyor. K-M38 bunun semptomunu kesti (ölü jeton artık gönderilmiyor) ama KÖK ÇÖZÜM bu. **Kapsama alınacak ikinci karar:** geçersiz `Authorization` bearer'ı bugün 401 üretiyor ve `X-Participant-Token`a BAKILMIYOR — misafir akışını da kırıyordu (K-M38 kanıtı: yalnız katılımcı jetonu 200, + geçersiz bearer 401). Filtre zincirinde bearer başarısızsa anonim sayılıp katılımcı jetonuna düşülmesi burada değerlendirilir. **Geçiş:** mevcut 12 saatlik jetonlar süresi dolana dek geçerli kalmalı (kullanıcı çıkışa atılmaz). Web'de yenileme jetonu AYRI HttpOnly çerez, `path=/api/auth/refresh`; mobilde SecureStore.  **UYGULANDI (Plan 45).** Kapılar: backend **523 test** · web **676** · mobil **222** (49 süit) + tsc + lint · i18n tr 642/en 653/nl 653 (DEĞİŞMEDİ — yeni anahtar yok). **Bearer kararı VERİLDİ:** bayat bearer, istek KENDİ BAŞINA kimlik taşıyorsa düşürülür — public uç ya da o slug'a ait GEÇERLİ katılımcı jetonu. Koşulsuz düşürülmedi, çünkü hesap uçlarının 401 dönmeye devam etmesi 401 kesicisinin TEK tetikleyicisi. **Bilinçli sapmalar:** (1) yenileme çerezinin yolu `/api/auth/refresh` DEĞİL **`/api/auth`** — RFC 6265 gereği daha dar yol `/api/auth/logout`a gönderilmez, çıkışta jeton ne okunur ne silinirdi (bu dosya aynı hatayı katılımcı çerezinde yaşamıştı); (2) şemaya **`family_id`** eklendi — yalnız `rotated_from` ile aile iptali özyinelemeli CTE isterdi, `rotated_from` denetim izi olarak kaldı; (3) `client` sütunu cihaz ipucu. **Geçiş bedeli (açık):** dolaşımdaki 12 saatlik jetonlar iptal EDİLMEDİ ama yenileme jetonları yok — doğal süreleri dolunca o kullanıcılar BİR KEZ yeniden giriş yapar. "Geçerli erişim jetonu → yenileme jetonu" önyüklemesi bilinçli olarak yazılmadı: çalınan her erişim jetonunu kalıcıya çevirirdi. **Cihaz kanıtı** M-10 satırında. |
| B-17 | **Keşfet — açık plan backend** — V20 `sessions.meet_at/capacity/join_policy` + `seat_requests` + `meet_checkins`, V21 `users.interests`; `OpenPlan`/`JoinPolicy`, `SeatRequest`, `SeatRequests` servisi, `GET /api/discover` (`DiscoverQueries`), `seat_requests_changed` gövdesiz zili, check-in | `.github/modernize/java-upgrade/20260902170714/plan.md` §B | plan.md §B | **done** (2026-09-10) | B-15 ✓ | Task 9/9 (B0–B8) + tam regresyon | Tasarım: Claude Design `Web Ekranlar v3 - Keşfet POC`. **9 görev (B0–B8).** **2026-09-09 iz AÇILDI** (planın Task B0'ı bugüne dek yapılmamıştı) ve iki düzeltmeyle: (1) **plan gövdesindeki kimlikler bayat** — plan `B-17 · W-16 · M-10` diyor ama W-16/M-10'u plan45 (yenileme jetonu, 2026-09-09 done) aldı; iz **B-17 · W-17 · M-11**. (2) **V20 çakışması**: kural 9'da V20 `push/device_tokens` için REZERVdi; Keşfet fiilen iniyor, push'un planı bile yok → V20/V21 Keşfet'in, push V22'ye kaydı. **Kapsam eklendi (K-B15 kullanıcı kararı 2026-09-09):** Keşfet kartı semt adı (`midpointLabel`) basıyor ama orta-nokta modunda o alan `find-venues`a kadar null (`DeckFlow:136` ters geocode'u orada tetikliyor) — Keşfet ise tam o aşamadaki (henüz mekân seçilmemiş) oturumları listeliyor. Karar: **açık plan KURULURKEN bir kez ters geocode** (plan başına tek Nominatim çağrısı, mevcut 30/dk throttle). Çapalı oturumda değişiklik yok, etiket zaten oluşturmada yazılıyor. Etiket katılımcı eklendikçe bir miktar bayatlar; `find-venues` anında zaten tazeleniyor **UYGULANDI (inline, 2026-09-10).** Kapilar: backend **583 test** (baz 528, 6 env-gated skip) · web **692**/107 + `tsc -b` temiz · mobil **269**/61 + tsc + lint temiz · i18n tr 663/en 674/nl 674 (DEGISMEDI — §B'de yeni anahtar yok). Migration **V20–V21** (kural 9'a islendi). `openapi.json`/`api-types.ts` throwaway :8061'de yeniden uretildi (kullanicinin :8060 IDE JVM'ine dokunulmadi): `/api/discover` 1, `seat-requests` 4 yol, `PlanCardDto`/`OpenPlanDto`/`SeatRequestDto`/`MySeatResponse` sozlesmede; web ve mobil tsc temiz. Bruno: `discover/` + `seat-requests/` (6 istek) + `sessions/checkin.yml`. ARCHITECTURE §3 (quota paketi + "B-17 yeni paket ACMADI" notu), §8 (hesap-JWT istisnasi), §11 (`seat_requests_changed`), §12 (`discover` 60/dk, `seat` 10/dk) guncellendi. **Bilincli sapmalar:** (1) `SessionCommands`, `SessionViewAssembler` ve `SessionController` yeni bagimlilik aldi (`ReverseGeocodePort`, `Clock`, `MeetCheckins`) → 5 test cagri yeri guncellendi; eski imzalar overload olarak KORUNDU, uretim kodu kirilmadi. (2) `Texts.note(raw)` 140 sinirla eklendi (plan `Texts.note(raw,140)` diyordu; sinir tek yerde sabit). (3) `TravelEstimate.rounded5` YAZILMADI — `TravelMinutes.between` zaten approx+5dk yuvarlama yapiyor, ikinci uygulama acilmadi. (4) Plandaki "DiscoverController'dan Jwt'yi kaldir" mutasyonu ANLAMSIZ: 401 `SecurityConfig`'in `anyRequest().authenticated()`inden geliyor, kontrolorden degil — yerine `DiscoverApiTest` anonim 401'i dogruluyor. Mutasyonla dogrulanan iki kapi: `SeatRequests.blockedEitherWay` ve `DiscoverQueries`'in ters-yon engel satiri (ikisi de kaldirilinca KIRMIZI). **Test kurgusundan ogrenilenler (K-B36'ya yazildi):** `@JdbcTest` tek transaction'da kosar (iki kisit ihlali ayni testte olmaz), `sessions.decided_venue_id` venues'a FK, ve `target/test-classes` bu oturumda IKI KEZ bayat kaldi (`rm -rf` sart oldu). **2026-09-11 güvenlik incelemesi → K-B37:** davet-linki ucu açık planı tanımıyordu, kapatıldı (plan46). |
| B-18 | **"Buradayım" anlık plan + kitle + rozet sayaçları — backend** — V23 `sessions.open_until/audience/locality` (+backfill, Keşfet indeksi PUBLIC'e daralır), `OpenPlan.openUntil/audience` (`end/inProgress/listedInDiscover`), `Session.locality`, Keşfet `coalesce(open_until, meet_at) > now ∧ audience = PUBLIC`, pencereli plana çapa zorunlu (400 `open_plan_anchor_required`), `FRIENDS` 400 `audience_not_available`, `StatsDto.plansMet/metStreakWeeks` (`MetStreak`), K-B38 kapanışı | `2026-09-11-plan47-instant-plan-backend.md` | plan47 | **done** (2026-09-11, inline; 613 test yeşil, +26) | B-17 ✓ | T6 codegen ✓ (`api-types.ts` +15 satır, yalnız ekleme) | Spec `docs/superpowers/specs/2026-09-11-instant-plan-badges-design.md`; tasarım Keşfet POC P1/P1c/P3a/P2a/P5b/P6. 6 görev |
| B-19 | **Arkadaşlar — backend** — `friendships` (V24), aday = birlikte plana katılmış hesap, karşılıklı onay, engel siler; `FRIENDS` kitlesi açılır; `/api/friends*` + `/api/friends/plans`; gizlilik metni (hukukçu kapısı) | — (plan B-18 bitince yazılır) | — | aday | B-18 | — | Spec §8 modeli bağlayıcı; gizlilik-önce: canlı konum YOK, rehber/arama/link YOK |

**Spec dışı görevler** — planlama/yürütme sırasında bulundu, spec'te yok. `Plan` kolonu:
`done` için kalemin çıktığı plan, `açık`/`aday` için hedef plan.

| Kimlik | Görev | Durum | Plan | Not |
|---|---|---|---|---|
| K-B1 | `runoffVotedParticipantIds` — runoff'ta kimin kilitlediğini döndür | done | B-5 | Runoff sağ bölgesi "kim kilitledi" ister (neyi seçtiği değil) |
| K-B2 | `/shuffle` + `/points` için adlandırılmış rate-limit kovası | done | B-5 | **Açılmadı** (bilinçli sapma): genel `/api/*` 120/dk kovasına düşüyor. Gerekçe: ikisi de sağlayıcı çağrısı yapmıyor ve `shuffle` durum korumalı (ilk çağrıdan sonra 409). Bruno dokümanı koda göre yazıldı, plana göre değil |
| K-B3 | `GET /api/sessions/{slug}/preview` — kamu önizlemesi | done | B-6 | Katıl ekranı katılmadan önce host adını, oturum adını ve kişi sayısını gösteriyor; mevcut `GET /sessions/{slug}` 401 döndüğünden kamu ucu şart. Tasarım denetimi §9 gereği `participants[{displayName, host, hasLocation}]` da eklendi (koordinat/id yok) |
| K-B4 | `SessionView.viewer` — "host muyum / hangi katılımcıyım" | done | B-6 | Web sayfa yenilenince bellekten silinir; sunucu söyler |
| K-B5 | `SessionSummaryDto`'ya `readyCount`, `doneCount`, `decidedVenuePhotoUrl` | done | B-6 | Tasarım denetimi §9 gereği eklendi; artboard'lar düzeltildi |
| K-B6 | `POST /api/auth/logout` ve okuma uçları için adlandırılmış kova | done | B-6 | **Alınmadı** (bilinçli sapma): genel `/api` 120/dk. Auth kovası Google doğrulaması içindir |
| K-B7 | Dil doğrulaması tek kaynaktan | done | B-6 | `@Pattern` yerine `UserPreferences.LANGUAGES` (400 IllegalArgumentException ile) |
| K-B8 | Bayat çerez hatası — kamu uçlarında cookie okunmasın | done | B-6 | Bearer resolver `SecurityConfig.PUBLIC_ENDPOINTS` listesindeki uçlarda cookie okumaz; önceden bayat `bumpinto_at` ile logout/login 401 alıyordu |
| K-B9 | `summariesOfHost` satır başına sorguyu kaldır | done | B-6 | Satır başına sorgu yerine 3 sorgu (sayfa + katılımcılar `in` + mekanlar) |
| K-B10 | `PUT /api/me` tam değiştirme sözleşmesi | done | B-6 | Gönderilmeyen tercih temizlenir (displayName hariç) — W-3 Profil formu tam durumu yollar |
| K-B11 | `SessionView.likeCounts` — DECIDED'da mekan→beğeni sayısı | **done** (satır bayattı, 2026-09-11) | B-7 | W-3'te Karar ekranının "hepiniz aynı yeri beğendi / 3/3 beğendi!" kesişim-1 kutlaması KAPALI bırakıldı: boş `voteTally` seyrek fallback'te ve force-decision'da da boş olduğundan oybirliği kanıtlanamıyor. Deste notundaki "diğerlerinin beğenileri sonuçta belli olur" vaadini de karşılar **2026-09-11:** B-7 ile gelmiş — `SessionQueries` yalnız DECIDED'da dolduruyor; web `ResultScreen`/`BackupPlan` tüketiyor |
| K-B12 | Sağlayıcı kota durumunu paylaşımlı depoya taşı | aday | B-7 | `ProviderQuotaCache` ve `GooglePlacesVenueProvider`'ın aylık `searchNearby` sayacı **süreç içi**. Bedeli: pod yeniden başlayınca cache boş (ilk scheduler turuna dek `@Order` sırası — kota bilgisiz seçim), Google sayacı sıfırlanır → ay içinde **eksik sayar, bütçe aşılabilir** (para); çok pod'da hiçbiri paylaşılmaz (her pod kendi bütçesini ayrı yer). İş: ikisini Redis'e ya da tek tabloya (`provider_quota(provider, period, calls, remaining, reset_at, measured_at, source)`) taşımak — **Plan 5'teki rate-limit kovasının Bucket4j-Redis'e taşınmasıyla aynı iş, aynı Redis**. Kabul: restart sonrası kota satırı `unknown` değil son bilinen değer; iki pod aynı ayda tek sayaç. Bu arada gözlem loglardan: scheduler her turda `quota <provider>: kalan/limit (%) resets <an> [kaynak]`, orkestratör `venues from <provider>` (INFO) **2026-09-11:** satır kısmen bayat — aylık bütçe sayacı artık KALICI ve pod'lar arası ortak (`BudgetGate` → `ProviderUsagePort`, V10 `provider_usage`, B-13); restart'ta sıfırlanıp bütçe taşırma riski kapandı, scheduler da kaldırıldı. Süreç içi kalan YALNIZ `ProviderQuotaCache` (yanıt başlıklarından son bilinen kota, ilk gerçek aramada dolar) |
| K-B13 | Mekan açık/kapalı saati | **done** (satır bayattı, 2026-09-11) | B-7 | API'de yok → W-4 Mekanlar'da rozet çizilemedi, artboard da düzeltildi (audit §11) **2026-09-11:** `VenueDto.hoursToday` var; web `VenueCard`/`VenuePopCard`/`WhyHere` basıyor (W-13) |
| K-B14 | BROWSING'de SOLO konum düzenleme | aday | B-7 | Bugün backend 409 döndürüyor → W-4 ekranı düzenlemeyi göstermiyor |
| K-B15 | Orta nokta için şehir adı (ters geocode) | **done** (B-17, 2026-09-10) | B-7 | W-4 kapsülü "Orta nokta · ≤ N km" (`radiusKm`) ile yetiniyor **2026-09-09:** satır kısmen bayattı — B-13'ten beri etiket `find-venues` anında ters geocode ile YAZILIYOR (`DeckFlow:136`), yani BROWSING'den sonra ad var. Eksik olan, oturum daha COLLECTING'ken adın olmaması. Bu Keşfet'te ürün hatasına dönüşüyor: kart tam o aşamadaki planları listeliyor. Kullanıcı kararı: **açık plan kurulurken bir kez ters geocode** → B-17 Task B4'ün kapsamı. Gizli (Keşfet'te olmayan) oturumlarda davranış DEĞİŞMEZ — orada hâlâ `find-venues` anında çözülür, boşuna ağ çağrısı yok **2026-09-11:** B-17 ile kapandı — `SessionCommands` açık plan kurulurken `ReverseGeocodePort` ile semt adını yazıyor |
| K-B16 | Host'un kaydırmama seçeneği ("Ben de kaydıracağım") | aday | B-7 | W-4'te düştü — hiçbir artboard'da ve backend'de yok |
| K-B17 | Apple girişi | **done** (satır bayattı, 2026-09-11) | B-7 | M-1 Ek A'da kapsam dışı bırakıldı **2026-09-11:** B-14 ile geldi — `POST /api/auth/apple`; istemciler W-14 ve M-5 |
| K-B18 | `GET /api/sessions` sayfalama | aday | B-7 | Bugün son 20 kayıt |
| K-B19 | Google dışı mekan verisi — Foursquare OS Places (Apache 2.0) + OSM/Overpass | done (B-13) | B-4 | Ücretsiz haritanın ön koşulu: Places ToS "No Use With Non-Google Maps". Bedeli: mekan fotoğrafı yok, 15 tür için kategori eşlemesi, PostGIS. Maliyet gerekçesi ve karar: K-W4 |
| K-B20 | Katılımcı `locationLabel`'ını sunucuda çöz (Nominatim backend'de) | done (B-13) | B-8 | B-7 yalnız orta nokta etiketini sunucuya taşıdı; katılımcı etiketi hâlâ istemciden (`lib/geocode.ts`) geliyor — politika (UA, oran, cache) tek yerde olmalı |
| K-B21 | "Belli bir nokta" çapa modu (`anchorMode`/`anchorLat/Lng/Label`) | **done** (satır bayattı, 2026-09-11) | B-8 | Karar dokümanı §7.2 açık; orta nokta yerine katılımcı yanı ya da adres; "adil" rozetleri bu modda kapanır **2026-09-11:** `anchor` adıyla 2026-09-05'te geldi (`449d644`, V9 `sessions.anchor_lat/lng`); web `newSessionStore` + `MidpointCard` `anchored`, mobil `app/sessions/new.tsx` |
| K-B22 | Nominatim ters geocode `find-venues` içinde senkron/bloklayan throttle | done (B-13) | B-8 | B-7:T3 plan gereği `Thread.sleep` ile 1 istek/sn kapısı `@Transactional` içinde: en kötü 1 sn + 5 sn HTTP zaman aşımı kadar isteği ve DB işlemini tutar, eşzamanlı find-venues çağrıları 1/sn serileşir. Kozmetik etiket için pahalı. İş: `tryAcquire` ile kapı doluysa etiketi atla ya da asenkron çöz + sonraki poll'da yaz |
| K-B23 | Anonim alınan koltuğu hesaba bağla (`participants.user_id`) | done | A-zinciri | A4/A5 sonrası kalan boşluk: `Caller.ANONYMOUS` ile açılan koltuğun sahibi yoktur, kişi sonradan giriş yapsa bile o koltuk hesabına bağlanmaz — ikinci cihazda kimliğini kurtaramaz ve yeniden katılıp **mükerrer satır** açar (orta noktayı çeker). Kural: istek geçerli bir katılımcı token'ı + hesap taşıyorsa ve token'ın koltuğu sahipsizse **ve** hesabın o oturumda koltuğu yoksa → `user_id` yazılır. Yıkıcı değil, idempotent, `(session_id,user_id)` unique index koruyor. **done 2026-09-04** (`SessionCommands.claimSeat`, tek onarım kapısı `GET /api/sessions/{slug}`, 253 test, mutasyonla doğrulandı). Kapsam DIŞI (kullanıcı kararı 2026-09-04): host kendi linkine anonim katılmışsa iki koltuk kalır — temizliği "host katılımcı çıkarabilsin" ürün kalemine bağlı |
| K-B24 | SOLO + mobil: katılımcı token'ı kaybolunca `/points`'e dönüş yolu yok | **done** (2026-09-09) | M-1 | A5 sözleşme boşluğu: oda içi tek kimlik katılımcı token'ı, `ParticipantTokenDelivery.refresh` yalnız web (çerez), `join` SOLO'da 409 → mobil host token'ını kaybederse elle nokta ekleyemez. **Şimdi kod yazılmadı (bilinçli)**: mobil istemci yok, M-1 başlamadı; varsayım üstüne uç eklemek gereksiz kod. M-1/M-3'te ya mobil için gövde/başlık onarım kanalı ya da SOLO'ya kimlikli `join` açılmalı **2026-09-09 KAPANDI (K-M39 ile birlikte):** `SessionCommands.join`daki SOLO kapisi koltuk kurtarmadan SONRAYA alindi — koltugu OLAN cagiran (yani host) kendi koltugunu ve TAZE jetonu geri alir, koltugu OLMAYAN yine 409 gorur (davet linki SOLO'da hala kapali). Regresyon: `SessionCommandsTest.soloHostRecoversItsSeatWhileTheInviteLinkStaysClosed` (once KIRMIZI: `ConflictException: solo session has no invite link`). Backend 528 test yesil |
| K-B25 | Rate-limit anahtarlaması ingress arkasında | aday | I-1 | `RateLimitFilter` bugün `getRemoteAddr()`'a bakıyor; ingress/LB arkasında tüm trafik tek IP'den gelir → tek kova, herkes birbirini 429'lar. Çözüm `X-Forwarded-For` + güvenilen proxy sayısı, ama **ingress henüz yok** (I-1:T1–T3 yapılmadı): topoloji bilinmeden anahtar seçilemez. I-1 ile birlikte açılır **2026-09-09:** K-B34 kapandi ve `export` politikasi filtreden CIKTI; bu satirin kapsami artik yalniz ANONIM uclar (auth, join, find, ws, geocode, og, api). Hesap kimligi gerektiren uclar icin `AccountQuotaPort` yolu acildi — ayni sinifta yeni bir kural gerekirse ingress beklemeden oraya yazilir **2026-09-11 düzeltme:** "bugün `getRemoteAddr()`'a bakıyor" eksik — filtrede Plan 2'den beri `bumpinto.rate-limit.trust-forwarded-for` bayrağı var (varsayılan kapalı; açıkken XFF'nin EN SOLDAKİ değeri, yalnız başlığı ezen ingress arkasında güvenli). Güvenilen proxy sayısı tasarımı hâlâ ingress topolojisini bekliyor |
| K-B26 | `VenueDto.travelMinutes` W-12 sonrası silinir (`travel[]` tek kaynak); `venues.maps_url` sütunu bir sonraki temizlikte düşer | **done** (2026-09-11) | B-16 | B-13 T7 bilinçli olarak korudu: web/mobil bugün `travelMinutes`'ı okuyor. **W-13 (plan35) web tarafını `travel[]`'a taşıdı — `shared/fairness.ts` artık `travel[]` okuyor, `travelMinutes`'ı okuyan istemci kalmadı.** **2026-09-07 düzeltme:** bu satır "alanın DTO'dan düşmesi B-15'te" diyordu ama B-15 (plan34, v3 cilası) kapsamına hiç girmedi — `ApiDtos.VenueDto.travelMinutes` ve `venues.maps_url` hâlâ duruyor; hedef **B-16**'ya taşındı (mobil M-4+ hâlâ aynı `shared/fairness.ts`'i tüketiyor, silmeden önce M tarafının da geçmiş olması gerekir) **2026-09-11 KAPANDI:** `ApiDtos.VenueDto.travelMinutes` ve `VenueEntity.mapsUrl` silindi, `backend/src/main/resources/db/migration/V22__drop_venues_maps_url.sql` eklendi (DTO'daki `mapsUrl` KALDI — `MapLinks`'ten türetiliyor); codegen `api-types.ts`'ten `travelMinutes`'ı düşürdü. Manuel test kullanıcıda geçti (2026-09-11). |
| K-B27 | Sign in with Apple (Apple id doğrulama + aynı e-postayla hesap birleştirme + hesap silmede token iptali) | done (B-14) | B-13 | App Store 4.8: Google girişi olan uygulama eşdeğer alternatif sunmalı. Kaynak `2026-09-06-mobile-store-compliance.md` §1 L1, §4.1. |
| K-B28 | Hesap silme ucu (`DELETE /api/me`: hesap + host oturumları silinir, katılımlar anonimleşir; 30 gün yedek temizliği) + web sayfası `/account/delete` (W izine K-W14) | done (B-14) | B-13 | Apple 5.1.1(v) + Play hesap silme politikası; Play Data safety formu web linki ister. Kaynak uyumluluk dok. §1 L13–L14, §4.2. |
| K-B29 | Bildir / engelle (rapor kaydı, engel listesi; engellenen kişi sesli sohbette eşleşmez, roster'da engelleyene görünmez) | done (B-14) | B-13 | Apple 1.2 UGC: canlı sesli sohbet + görünen ad. Tasarım O18–O19. Kaynak uyumluluk dok. §1 L15, §4.3. |
| K-B30 | **Oturum purge tetikleyicisi K8s CronJob DEĞİL, uygulama içi `@Scheduled`** — B-3 kapsam kararı 3 kullanıcı talimatıyla ezildi (2026-09-07: "K8s kullanacağımızın garantisi yok"). Purge B-13'ün `retentionScheduler` havuzunda günlük koşar; `adapter.in.job`/`PurgeRunner` ve `deploy/k8s/retention-cronjob.yaml` yazılmadı | done (B-3) | B-3 | Çok-pod tekilleştirmesi leader election/ShedLock yerine `select … for update skip locked` ile çözüldü: replikalar ayrık parti alır, kilit beklemez, yeni bağımlılık yok. K8s'e geçilirse `bumpinto.retention.session-purge-cron` boşaltılıp CronJob'a taşınabilir |
| K-B31 | I-1 yayın kontrol listesindeki "retention **CronJob** uygulandı" kapı satırı bayat | done (B-3) | I-1 | K-B30 kararının sonucu; plan5 Task 4 Step 1 satırı uygulama içi zamanlayıcıya göre düzeltildi |
| K-B32 | `retentionScheduler` kapanışta terminate olmuyordu (`Timed out while waiting for executor`), pod sonlandırması her seferinde 5 sn+ gecikiyordu | done (B-3) | B-13 | Sebep: cron görevi `ScheduledThreadPoolExecutor`'a tek atımlık **gecikmeli** iş olarak girer ve varsayılan politika onu kapanışta BEKLER. `setExecuteExistingDelayedTasksAfterShutdownPolicy(false)` + `setRemoveOnCancelPolicy(true)`; `waitForTasksToCompleteOnShutdown` korundu (koşan purge partisi yarıda kalmasın). B-13'ten beri gizliydi: suite'teki her `@SpringBootTest` `retention.enabled=false` ile koştuğu için zamanlayıcıya hiç iş girmemişti |
| K-B33 | **`users.purge_after` süpürmesi sahipsiz idi** — B-14 damgayı yazıyor (V14 `deleted_at`/`purge_after`) ve fiziksel temizliği "B-3'ün CronJob'ı"na havale ediyor, ama B-3 kapsam kararı 2 gereği `users`'a DOKUNMUYOR (yalnız `sessions` siler) ve CronJob da yok (K-B30) | done (B-14:T12) | B-14 | Damga atılıp süpürülmezse silinen hesap satırı sonsuza kadar kalır — Apple 5.1.1(v)/Play hesap silme vaadi yerine gelmez. **İkinci bulgu: şema süpürülemez durumdaydı** — `reports.reporter_user_id` ve `blocks.blocker_user_id` `users`'a not-null cascade'siz bakıyordu, fiziksel silme FK ihlaliyle patlardı. V15 kararı: rapor `on delete set null` (moderasyon izi kalır, kişisel bağ kopar — cascade olsaydı bildiren susturularak kayıt temizlenebilirdi), engel `on delete cascade`. Plan 33'e 2026-09-07'de yazıldı: ön koşul bloğu, V14/V15, `SchemaMigrationTest` FK kanıtı, yeni T12 (`RetentionPort` + `AccountRetention` + `RetentionJob`); altyapı B-3'ten gelir |
| K-B34 | **`export` hız-limiti kovası hesaba değil IP'ye anahtarlı ve kimlik doğrulamadan ÖNCE tüketiliyor** — gerçek limit kullanıcı başına değil kurulum başına 1/saat | **done** (2026-09-09) | B-15 | B-15:T3'te açıldı. `RateLimitFilter` `@Order(HIGHEST_PRECEDENCE)`, yani güvenlik zincirinden önce koşar: **kimliksiz** bir `GET /api/me/export` 401'i almadan önce o saatin tek jetonunu yakabilir. Anahtar `clientIp()` → `getRemoteAddr()`; `TRUST_FORWARDED_FOR` varsayılanı `false` ve depoda hiçbir yerde açılmıyor, dolayısıyla ingress arkasında TÜM kurulum tek kovayı paylaşır. Aynı tuzak `ws` (240) ve `geocode` (30) politikalarının notlarında zaten yazılı; export 1/saat ile listenin en darı olduğu için etkisi orada kozmetik değil, **özelliği fiilen kullanılamaz** kılıyor. Çözüm kovayı hesap kimliğine anahtarlamak, bu da kontrolü kimlik doğrulamanın ARKASINA taşımayı gerektirir (filtre yerine interceptor ya da uçta ikinci kova) — T3 kapsamında değil, bilinçli ertelendi, T3'ü bloklamaz. **K-B25 ile aynı kökten** (ingress arkasında anahtarlama); ikisi birlikte çözülmeli **2026-09-09 KAPANDI:** kural KATMAN degistirdi. `RateLimitFilter`in `export` politikasi SILINDI (uc artik `api` kovasinda, 120/dk IP — kimliksiz seli ucuz reddeder, gercek limiti kurmaz); "hesap basina 1/saat" yeni `AccountQuotaPort` + `adapter.out.quota.InMemoryAccountQuota` ile `UserDataExport.of`un ILK satirinda, yani kimligin ARKASINDA ve okumadan ONCE tuketiliyor. `TooManyRequestsException` artik `retryAfter` tasiyor ve `ApiExceptionHandler` onu `Retry-After` basligina yaziyor (saatlik kotaya "60 sn sonra dene" demek yeniden deneme firtinasiydi; entegrasyon testi 3600 bekliyordu). Regresyon: `UserDataExportTest.theHourlyQuotaIsPerAccountNotPerInstallation` (Ayse'nin dosyasi Kerem'in saatini YAKMAZ) + `RateLimitFilterTest.shippedPoliciesDoNotKeepAnHourlyBucketOnIp`. **K-B25 ACIK KALIR:** anonim uclarin IP anahtarlamasi hala ingress topolojisini bekliyor — bu duzeltme yalnizca hesapli ucu o bagimliliktan KURTARDI |
| K-B35 | **Dürt engeli TANIMIYOR** — A, B'yi engellemiş olsa bile B `POST /api/sessions/{slug}/nudge/{A}` çağırabilir ve A'nın istemcisi "B seni dürttü" basar | **done** (2026-09-09) | B-15 | B-15:T6'da açıldı. `NudgeCommands.nudge` yalnız üç kapı kurar (koltuk üyeliği, kendini dürtme, elle eklenen nokta) ve `Blocks`'a HİÇ bakmaz; oysa ses odası aynı sorunu zaten çözmüş durumda: `VoiceAdmission.blockedWith(sessionId, participantId)` çift yönlü engelli çiftleri aynı odadan dışlar (`VoiceRoomListener`, `VoiceSignalController` kullanıyor). Dürt bu yerleşik deseni izlemiyor, üstelik engellenen kişinin kurbanına **kimlikli** (`nudged{fromParticipantId, toParticipantId}`) ve **tekrarlanabilir** (60 sn'de bir) bir bildirim gönderebildiği için kötüye kullanıma en açık kanal burası — K-B29'un (Apple 1.2 UGC) kapatmak için var olduğu boşluğun aynısı. Bu görevin kapsamı DIŞI: plan 34 T6 metni engelden hiç söz etmiyor, bilinçli ertelendi, T6'yı bloklamaz. Çözüm: `NudgeCommands.nudge` içinde elle-nokta kapısından SONRA, kota kapısından ÖNCE bir `blockedWith(...)` kontrolü (403) — sıra önemli: reddedilen dürt kotayı tüketmemeli (T6'nın `aRejectedNudgeDoesNotSpendTheQuota` testiyle aynı ilke). `NudgeCommands` böylece `VoiceAdmission` benzeri bir bağımlılık kazanır **2026-09-09 KAPANDI:** `NudgeCommands` artik `Blocks`i dogrudan enjekte ediyor (VoiceAdmission uzerinden DEGIL — koltuk listesi zaten elde, admission onu bir kez daha okurdu) ve elle-nokta kapisindan SONRA, kota kapisindan ONCE `blockedPairIds(...)` kontrolu var: engelli cift HICBIR yonde birbirini durtemez (ses odasiyla ayni simetri) ve reddedilen durt kotayi TUKETMEZ. Regresyon: `NudgeCommandsTest.aBlockedPairCannotNudgeInEitherDirection` (kapi `false &&` ile devre disi birakilarak KIRMIZI dogrulandi) + `aBlockOnlyBindsThePairItNames` (ucuncu kisiye giden zil calmaya devam eder) |
| K-B36 | **Backend test kurgusu: uc tuzak** — (a) `@JdbcTest` TEK transaction'da kosar, ilk kisit ihlali transaction'i iptal eder → ayni testte IKINCI bir ihlal sema hatasi degil "transaction aborted" verir ve kisiti hic sinamaz; (b) `sessions.decided_venue_id` **venues'a FK** (V2) → `decided(randomUUID(), ...)` ile kurulan fixture FK ihlali dogurur, durum sinamak icin `withStatus(DECIDED)` kullan; (c) `target/test-classes` bayat kalabiliyor — `NoClassDefFoundError` ya da "Field 'postgres' must be a Container" gibi anlamsiz hatalar veriyor, `rm -rf target/test-classes` sart | **done** (2026-09-10) | B-17 | Ucu de B-17 yurutulurken YASANDI (ilki iki kez). Ucu de kod hatasi DEGIL test kurgusu hatasidir ve belirtileri kok sebebi gizliyor: (c) ozellikle yaniltici, cunku hata mesaji Spring/Testcontainers yapilandirmasini isaret ediyor. `backend/ARCHITECTURE.md` §15 zaten "stale target/classes" gotcha'sini aniyor; bu satir test tarafi icin ayni uyariyi tasiyor |
| K-B37 | **Keşfet slug'ı + PUBLIC davet-linki ucu = onaysız koltuk** — `PlanCardDto.slug` herkese basılıyor, `SessionCommands.join` açık planı tanımıyordu → oturum açmış (hatta anonim) herkes `POST /participants` ile host onayı, engel ve kapasiteyi atlayıp 201 + jeton alıyor, `SessionView`'ı (adlar, ~1,1 km konumlar, mekânlar, `joinCode`), konum yazma, oy, dürtme ve sesi açıyordu; host'un atma yolu yok | **done** (2026-09-11) | B-17 | 2026-09-11 güvenlik incelemesi (High, 9/10). Düzeltme `2026-09-11-plan46-open-plan-join-gate.md`: kapı `join`de koltuk kurtarmadan SONRA, 409 `open_plan_seat_request_required`; açık planda koltuk yalnız `SeatRequests`'ten. Opak kart kimliği AÇILMADI (slug artık yetenek değil, YAGNI). **Yan bulgu:** seat-request uçları `/api/sessions/{slug}/` altında → kendi katılımcı çerezini taşıyan host `@AuthenticationPrincipal Jwt` ile 403 alıyordu (MockMvc çerez taşımadığı için görünmedi); hesap `details`'ten okunur, fail-closed korunur. **Devir W-17/M-11:** katılım sayfası bu 409'u görünce plan detayına yönlendirir. +4 test |
| K-B38 | **Çapalı açık planda Keşfet kartı kesin noktayı sızdırıyor** — `midpointLabelAt` çapa ETİKETİNİ `midpointLabel`e yazıyor, `toDiscover` onu `PlanCardDto.locality` olarak herkese basıyor; host "Café X, Kleine Berg 12" yazarsa onaysız herkes görür ("kesin nokta yalnız onaylılara" kuralı delik). Medium | **done** (B-18 T3/T4, 2026-09-11) | B-18 | 2026-09-11 spec yazımında bulundu. Düzeltme: yeni `Session.locality` = çapa noktasının ters geocode'lu semti; `midpointLabel` üyelere özel kalır |
| K-B39 | **Shuffle eşleşmesi (Swarm ruhu) — kapı arkasında** — mekânda son 5 saatte bulunanlar, simetrik opt-in, sağ/sol kaydırma. **Tetik:** 4 ardışık hafta boyunca haftada ≥ 100 anlık plan VE anlık planların ≥ %50'si host dışı ≥ 1 koltuk alıyor (ölçüm SQL'i spec §7). Ön kararlar: eşleşme = "birlikte plan aç" daveti, sohbet yok; profil fotoğrafı + moderasyon pipeline'ı ayrı iz; flört dili yasak | aday (kapı) | — | 2026-09-11 ürün kararı; gerekçe spec §0 (yoğunluk matematiği, kategori, güven) |
| K-B40 | **`streak_3` rozeti kazanıldıktan sonra kaybolabiliyor** — rozet `stats.metStreakWeeks` (MEVCUT seri) ≥ 3'e bağlı; seri koparsa (bu ve geçen ISO haftasında buluşma yok) profilde soluklaşır ve seri yeniden kurulunca "yeni rozet" tekrar kutlanır — P5b "profilinde durur", P6 "rozetler sende" kopyasıyla çelişir. Important | aday | B-18 (+W-18/M-12 `badges.ts`) | 2026-09-11 W-18 T5 incelemesinde bulundu; web ve mobil aynı shared kuralı okur. Adaylar: (a) sunucu `StatsDto.bestMetStreakWeeks` (`MetStreak` aynı zaman damgalarından en uzun seriyi hesaplar), `badgesFor` onu okur, profil sayacı mevcut seri kalır; (b) `streak_3`'ü kaldır ya da kopyayı değiştir. Sunucu değişikliği W-17/W-18 kapsamı dışıydı ("sunucu tarafı değişmez") — **kullanıcı kararı bekliyor** |

## W — Web

| Kimlik | Plan | Dosya | Eski # | Durum | Bağımlılık | Son adım | Not |
|---|---|---|---|---|---|---|---|
| W-1 | pnpm workspace + web katılım uygulaması | `2026-09-01-plan3-web.md` | Plan 3 | done | B-2 | Task 7/7 + kapanış denetimi | 5/5 test + tsc + prod/preprod build yeşil; subagent-driven; tüm ekranlar artboard'lardan birebir; pnpm 11 uyarlamaları; elle uçtan uca kullanıcıda; commit'ler kullanıcıda |
| W-2 | Web UI — Tailwind v4 + i18n (tr/en/nl) + rem token migrasyonu | `2026-09-01-plan8-web-tailwind-i18n.md` | Plan 8 | done | W-1 | Task 7/7 + final review | Tailwind v4 utility-first (utility yalnız components/), @theme rem token'ları, react-i18next tr/en/nl; 5/5 test + build yeşil. **en/nl çevirileri `_status` işaretiyle tasarım onayı bekliyor** (onay artefaktı: W-3 Task 2 + `Katıl EN/NL 1280` artboard'ları) |
| W-3 | **Kabuk, kimlik, hesap ekranları, dil menüsü, iki bölgeli yerleşim** — TopBar/LangMenu/AvatarMenu, Google web girişi, Landing, Oturumlar, Profil, hata sayfaları, mevcut 5 oturum ekranının ≥1024 iki bölgeye taşınması (harita hariç) | `2026-09-02-plan11-web-shell-account.md` | Plan 11 | done | W-2 ✓ · B-6 ✓ | Task 7/7 + kapanış incelemesi | 23/23 test + tsc + prod/preprod build yeşil; sıfır TODO; tr/en/nl 183/183/183 anahtar. Subagent-driven (orkestrasyon Fable / impl Sonnet / review Opus; Task 3 → 3a+3b, ~17 ajan). Varsayılan dil **en**; `?lng=` > sunucu tercihi > tarayıcı; `<html lang>`+başlık canlı. Kimlik sunucudan (`SessionView.viewer`; bellek-içi `self` kalktı), `locationLabel` join/konum değişikliğinde gönderiliyor (Nominatim ters geocode), `preview` 401'de bir kez. **Bilinçli sapmalar:** Google butonu GIS render'ı; `/sessions/new` W-4'e dek 404; Karar'da adres/açık-kapalı/km yok (API'de yok); Oturumlar kartında avatar satırı yok (liste API'sinde ad yok); **kesişim-1 kutlaması KAPALI** (B-7 `likeCounts`); Bekle kopyası rev-1 (onay bekliyor); Profil ad düzenleme satır-içi input (artboard'da düzenleme hâli çizilmedi). Tasarım düzeltmeleri Claude Design'a yazıldı (audit §10). Elle uçtan uca (gerçek Google client id) kullanıcıda; commit'ler kullanıcıda |
| W-4 | **Oturum tipi, Yeni buluşma, Lobi, Mekanlar, Google haritası** — `MapView` (Maps JS + AdvancedMarker, Map ID stili), tip seçimi, gruplu etkinlik seçici, Bireysel elle konumlar, rol/durum yönlendirmesi, Mekanlar (liste ↔ harita, Karıştır, Bunu seç), Katıl/Bekle/Karar haritaları, Profil tercih düzenleme | `2026-09-02-plan12-web-session-type-map.md` | Plan 12 | done | W-3 ✓, B-5 ✓, B-6 ✓ | Task 5/5 + kapanış incelemesi | 45/45 test + tsc + prod/preprod build yeşil; sıfır TODO; tr/en/nl 241/241/241 anahtar. Subagent-driven (orkestrasyon Fable / impl Sonnet / review Opus; Task 2→2a+2b, Task 4→4a+4b, ~22 ajan). Harita yalnız Google: `@googlemaps/js-api-loader` **v2** (`setOptions`+`importLibrary`; plandaki `Loader` sınıfı artık yok), tek `MapView` organizması (`mapPins.ts` = DS §10, içerik imzasıyla yeniden çizim, `lgOnly`), anahtar yokken yer tutucu notu. Anahtarlar `VITE_GOOGLE_MAPS_KEY` + `VITE_GOOGLE_MAPS_MAP_ID` (kullanıcı; I-1 Ek A). **Plan düzeltmeleri / bilinçli sapmalar:** (1) "Ben de kaydıracağım" anahtarı ve "Link hemen oluşur" ipucu düştü — hiçbir artboard'da yok, backend'de yok; (2) `shuffle`/`pick`/`findVenues`/`addPoint`/`removePoint` deckStore'da değil `sessionStore`'da (slug orada bağlı; `mutate()` sayacı bayat poll yanıtını düşürür); (3) Katıl'da kendi pini çizilmez — artboard (audit §9) pinsiz + "Katılınca konumlar haritada görünür"; (4) orta nokta kapsülü "Orta nokta · ≤ N km" (`radiusKm`); şehir adı B-7; (5) SOLO kurulumda nokta/find-venues hatasında da `/j/slug`'a gidilir, Bireysel kurulum ekranı sunucu durumunu gösterir; (6) 390'da harita yalnız Lobi ve Mekanlar (`lgOnly`: Maps JS yüklenmez); (7) Mekanlar'da açık/kapalı rozeti ve şehir yok (API'de yok), SOLO BROWSING'de konum düzenleme yok (backend 409) — artboard'lar da düzeltildi (audit §11); (8) tek `useOwnLocation` kancası (Katıl/Yeni buluşma/Profil), `useSessionAction` (Lobi/Bireysel/Mekanlar), `ActivityBadge`, `VenueMeta`/`VenueThumb`; (9) **Yeni oturum 1280 · Grup** sağ bölgesi (`InvitePreview`) artboard'da çizilmedi, spec §5'e göre yazıldı. **B-7 adayları:** host kaydırmama seçeneği, mekan açık/kapalı, SOLO BROWSING'de konum düzenleme, orta nokta şehir adı. Gerçek anahtarla göz kontrolü + uçtan uca (Grup/Bireysel) kullanıcıda; commit'ler kullanıcıda |
| W-5 | **Deste kaydırma jesti + karar animasyonları** — Tinder benzeri sürükle-bırak (sağ = beğen, sol = geç), eşik/fırlatma, dönüş + damga, arka kartın öne gelmesi, buton/klavye kararlarında aynı uçuş, geri al'da geri süzülme; kütüphanesiz (Pointer Events + CSS) | `2026-09-02-plan14-web-deck-swipe.md` | Plan 14 | done | W-4 ✓ | Task 4/4 + Opus inceleme | 60/60 test + tsc + prod/preprod build yeşil; sıfır TODO; yeni i18n anahtarı yok. Fable inline yazdı, Opus inceledi (APPROVE WITH MINORS; S2×3 + S3×4 kapatıldı: giriş animasyonu sürüklemeyi ezmesin, bayat hız fırlatma sayılmasın, ikinci parmak yoksayılır, klavye repeat/modifier/input koruması, d2 `key`, uçan kart `aria-hidden`, testte sabit saat). Yeni: `lib/swipeMath.ts` (saf geometri + test), `molecules/SwipeCard.tsx` (Pointer Events, DOM'a doğrudan yazan sıcak yol, damgalar), `VenueDeck` uçan katman + tek `commit` yolu, `app.css` `--animate-fly-out/fly-in-*/rise`, `test-setup.ts` PointerEvent çokdolgusu (jsdom 25'te yok). **Bilinçli sapma:** damga ve jest artboard'da çizilmedi — DS token'larından türetildi, Claude Design geri yazımı kullanıcıda. Karar iyimser (`decide()` anında, çıkan kart ayrı katmanda); reduced-motion'da uçuş atlanır. **Ek (kullanıcı isteği):** uçuş 0.7s + akıcılık (yığın terfisi `promote`/`appear`/`rise` 0.32s, üç eğri: swipe ease-out / stack in-out / snap esneme; buton kararı satır-içi timing); `molecules/DecisionBurst` — beğenide kalp + konfeti, geçte × + toz (Task 5). Elle kontrol (fare 1280 / dokunma 390 / dikey scroll / ← → ⌫ / efektler) kullanıcıda; commit kullanıcıda |
| W-6 | **Haritasız değerlendirme + grup uzlaşması** — TravelChips + FairnessBadge, Mekanlar liste-önce (MapView tembel, davetli liste, sıralama), Lobi/Bekle orta nokta kartı + ulaşım türü seçici + stepper, Deste kart anatomisi (uyum satırı, semt, atıf), Deste bitti bekleme lobisi (gönderildi hâli, dürt, olmadan devam et), Runoff v2 (trailer, gizli sayım, adil olana bırak), Karar v2 (haritasız, Neden burası?, yedek plan, açılış), analitik olayları | `2026-09-03-plan16-web-fairness-mapfree.md` | Plan 16 | done | W-5 ✓ · B-7 ✓ | Task 0–1 ✓ (serverFields köprüsü, shared fairness.ts, TravelChips/FairnessBadge, tek `travel` prop; 89 test) · Task 2 ✓ (liste-önce, MapView lazy ayrı chunk + LazyBoundary + useMediaQuery, VenueSort shared byFairness/byRating, SOLO SelectionCard, Attribution) · Task 3 ✓ (Deste bitti = bekleme lobisi, sunucu `deckDone` ile gönderildi hâli, PersonRow, useSessionAction hata yolu) · Task 4 ✓ (Runoff v2: RunoffTrailer karar veren hücre, INTERSECTION/FALLBACK kopyası, VoteTally RunoffTie içinde, Adil olana bırak, adlı kilit notu, lib/voters.ts) · Task 5 ✓ (Karar v2: harita yok, WhyHere ADALET/UYUM/YER, BackupPlan, tek seferlik açılış, decisionKind eyebrow, Karar verildi · HH:mm, ghost Maps bağlantısı) · Task 6a ✓ (MidpointCard/ActivityStrip/SessionSteps, Bekle'de harita yok, TwoZone mobileFirst, lib/travelMode.ts) · Task 7 ✓ (FitLine tek uygulama, formatRating Intl, kart anatomisi §4.9, semt bastırma, unionProvider atfı, DeckScreen bağlandı) · Task 6b ✓ (TravelModeField Segmented üstünde, 390 ikon-only, 5 yüzey; authStore.updatePrefs defaultTravelMode koruma düzeltmesi) · Task 8 ✓ (lib/analytics.ts no-op sarmalayıcı + 3 olay, serverFields.ts silindi → @bumpinto/shared, lib/serverEnums.ts, scripts/i18n-parity.mjs `pnpm i18n:check`; 212 test) · Kapanış incelemesi ✓ (2026-09-03): WinnerCard çift atıf, SoloSetup/NewSession 390 harita (§4.7), konumsuz katılımcı notu (§5.C), tr-casing düzeltildi; `fairnessOf` sunucu-yalnız yolu shared'a taşındı. **45 dosya / 225 test, tsc, build + preprod, i18n:check yeşil; MapView ayrı chunk.** Subagent-driven (impl Sonnet / review Opus, ~35 ajan). Tasarım kaynağı Claude Design değil, yerel `web.out.html` (limit doldu). **Bilinçli sapmalar:** tek `travel` prop (labels+selfId), TravelModeField Segmented üstünde, SoloSetup'ta mevcut manuel noktanın modu düzenlenemez (backend ucu yok, K-W10), PointsEditor tam alan (artboard 22px `.f-mp` değil), ActivityStrip/SessionSteps plan-kod görünümü (artboard ile ufak fark, K-W11). Commit, gerçek anahtarla göz kontrolü ve uçtan uca kullanıcıda | 10 görev bloğu (0–8, 6a/6b); adalet mantığı `frontend/shared/src/fairness.ts` (M-3 tüketir); SOLO onay kartı; TravelRange ve `(N)` yok. Karar dokümanı §4 + §5.B/§5.C; §6 YAPMA listesi bağlayıcı. W-6a (B-7'den bağımsız hızlı kazanımlar) hemen başlayabilir. Artboard'lar: `Web Ekranlar v2` aynı etiketler + yeni `Mekanlar grup 390 host`, `Deste bitti 390`, `Gönderildi 1280/390`, `Runoff 1280 kilitli`, `Karar 1280 oylama` (kullanıcı onayı bekliyor) |
| W-11 | **Sesli sohbet web** — `liveChannel` (STOMP tek yerde, yeniden abonelik), `voiceMesh` (full-mesh, küçük-id teklif, yeniden kurma + 15 sn bekçi), `audioLevels` (K12), `voiceStore` (mikrofon → abonelik → kimlik → mesh), `VoiceDock` (başlat/katıl/sustur/ayrıl/bitir, geri sayım, sebep), `ParticipantRow` mikrofon + halka | `2026-09-06-plan29-voice-web.md` | Plan 29 | done | B-12 (openapi) | Task 7/7 + iki aşamalı inceleme | Spec `2026-09-06-voice-chat-design.md` §7. Dock `sticky bottom-0` + `order-last` (spacer yok, AppShell değişmez). Katman 3 (sessizlik tespiti) v2. 402 web testi yeşil; inceleme ekleri: liveChannel istemci kimlik kapıları, mesh yeniden kurma + bekçi (15 sn), join jetonu, sticky dock, Button forwardRef, connectFailed durumu |
| W-12 | **Açık hibrit mekan yığını — web** — `configStore` (`GET /api/config`, yedekli), `MapView`/`MapPicker` motor anahtarı (`.google` + `.maplibre` dosyaları, `MapFrame`, `lib/maplibre.ts`, lazy chunk), veri-güdümlü `Attribution` (`sources[]`), `formatRating(rating, scale)` + sağlayıcı işareti, `lib/geocode.ts` → backend uçları, link sözleşmesi | `2026-09-06-plan31-open-hybrid-web.md` | Plan 31 | done | B-13 (`openapi.json`: `/api/config`, `/api/geocode`, `ratingScale`, `travel[]`) | Task 7/7 + görev başına Opus incelemesi | Spec §7, §8, §10–§12, §14. 7 görev. `maplibre-gl@^5.6.0` tek yeni bağımlılık. 390 davranışı değişmez (`lgOnly`, ghost). Popülerlik UI'ı, Static Maps, tile self-host (plan32) kapsam dışı. Aşama 2: bu plan bitince Google Maps anahtarları kapatılır (kullanıcı). **Web 68 dosya / 432 test yeşil** (tsc temiz, i18n:check 0, prod+preprod build); `maplibre-gl@5.6.0` kendi ~940 kB parçasında (Google motoru yüklerken hiç inmez). MapLibre `error` yalnız yüklenmeden önce başarısız sayılır (inceleme düzeltmesi). Config yüklenemezse yedek kalıcı değil, `failed` + yeniden deneme. Attribution'da sabit OpenFreeMap satırı yok (harita kontrolü basar) |
| W-13 | **v3 kabuk senkronu — web** — erişilebilirlik token'ları (`Overline` ink2, rozet ≥0.75rem, amber/pembe metin), `shared/fairness.ts` → `travel[]` (`travelMinutes` silinir, K-B26 kapanır), `RangeBar` + `TravelBars` (`TravelChips`/`FairnessBadge`/`TravelList` silinir), `VenueMeta` saat + `tagline`, VoiceDock `lg:` sağ alt + `voice.endedTimeLimitHint`, `useOnline` + `OfflineBanner`, `VenueRowSkeleton`, DS önizlemeleri | `2026-09-06-plan35-web-v3-shell-sync.md` | Plan 35 | done | **W-12 ✓** (`travel[]`, `Attribution sources[]`) | Task 12/12 + görev başına Opus incelemesi | **81 dosya (20 yeni, 6 silindi) / 74 dosya · 458 test yeşil** (baz 68/430); tsc temiz, `i18n:check` 0 fark, prod+preprod build ve `.design-sync` preview kapısı yeşil. Subagent-driven (impl Sonnet / review Opus; T6 → 6a+6b; 6 düzeltme turu). R-W16 token'ları (`--color-amber-ink`/`--color-flame-ink`, `Overline` ink2, rozet tek punto 0.75rem), R-W1/R-W2 `RangeBar` + `TravelBars` (`TravelChips`/`FairnessBadge`/`TravelList` silindi, `lib/travelText.ts` tek adalet cümlesi, `shared/fairness.ts` artık `travel[]` okuyor → **K-B26 web tarafı kapandı**), R-W7 dock lg+ sağ altta yüzer + görünmez yer tutucu + `voice.endedTimeLimitHint`, R-W8 satırda `hoursToday` + `tagline` (alan yoksa satır yok, `lib/venueText.ts` cast'i B-15'te silinir), R-W9 `useOnline` + `OfflineBanner` + `VenuesLoading` iskeleti. **İnceleme düzeltmeleri (planın kendi hataları):** `border-amber` Tailwind kaynak sırasında hiç kazanmıyordu → tek dışlayıcı zincir; `pos()` %10–90 → artboard'ın %15–85'i; bant rengi `f.outlierId` yerine `line.leadTone`'dan sürülüyor; karar ekranı `TravelBars`'ı iki kez basıyordu → `VenueCard travelBars={false}` kapısı (W8: sol kart `.rc-ppl`, sağ kart tek `.tb`); `voiceStore` `bind()`'ın `view: null` adımı yüzünden odaya AÇIKKEN girende kalan süreyi "oda uzunluğu" diye uyduruyordu → `if (!state.view) return` + slug izleme + 5 dk yuvarlama; çevrimdışı `location.reload()` kullanıcıyı tarayıcı hata sayfasına düşürüyordu → yoklama sonrası reload; canlı bölge içeriğiyle mount olduğu için duyurulmuyordu → `AppShell`'de kalıcı `role=status` kabı; `VenuePopCard` saati iki kez basıyordu. **Plan hataları:** `pnpm --filter @bumpinto/shared test --run` no-op (`test` betiği yok — `fairness.test.ts` web vitest'inde koşuyor); `travelText.test.ts` `TravelInfo` anotasyonsuz tsc'yi kırıyordu; `VenueCheckRow`/`WinnerCard`/`LikedList` önizlemeleri `_fixtures`'tan beslenmiyor, kendi yerel verileri var; `VenueDeck`/`Page`/`Confetti` önizlemeleri plan listesinde yoktu ve `travelMinutes` taşıdıkları için yol göstergesini sessizce kaybediyorlardı. **Bilinçli sapmalar:** `providerMark` liste satırından düştü (artboard W3b/W6 basmıyor), kart dalında kaldı → K-W17; `SoloSetupPage` iskeleti atlandı (tek `busy` üç eylemi paylaşıyor) → K-W23. **Kullanıcıda:** commit, Claude Design yeniden senkronu (yeni: `RangeBar`, `TravelBars`, `OfflineBanner`, `VenuesLoading`, `VenueRowSkeleton`; değişen: `VenueCard`, `VenueCheckRow`, `WinnerCard`, `LikedList`, `RunoffList`, `Badge`, `Overline`, `SwipeCard`, `TwoZone`, `DecisionBurst`, `VenueDeck`, `Page`, `Confetti`; silinen: `TravelList`), plan T12 Step 4'ün 7 maddelik elle görsel kontrol listesi. |
| W-14 | **Mağaza uyumluluğu — web** — analitik rıza kapısı (`lib/analytics.ts`: rıza yoksa betik yüklenmez), `/privacy /terms /kvkk` (`content/legal/*.tsx`, TR taban), `/attributions` (veri-güdümlü), `/account` (Yasal/Veri/Hakkında/Tehlikeli bölge, `GET /api/me/export`), `/account/consent` (`PUT /api/me/consents`), `AppleSignIn`, `/account/delete` kurulumsuz akış (`DELETE /api/me`) + `/account/deleted`, `/support` | `2026-09-06-plan36-web-store-compliance.md` | Plan 36 | done | **B-14 ✓** (`/api/auth/apple`, `DELETE /api/me`, `/api/me/consents`) · *`/api/me/export` B-15'te, bkz. K-W27* | Task 11/11 + tam doğrulama | R-W10–R-W14, R-W17. 11 görev. **495 test yeşil** (baz 458), tsc temiz, i18n paritesi 0, prod build yeşil. **Kullanıcı düzeltmesi (K-W28):** yasal gövde üç dilde tam yazılır (TR taban + çeviri şeridi DEĞİL); `/kvkk` → **`/data-rights`**, rejim dile bağlı (TR = KVKK, EN/NL = GDPR), `/privacy` GDPR Art. 13/14 için hukuki dayanak + yurt dışına aktarım kazandı. Rıza kapısı: rıza yokken Clarity/GA4 betiği sayfaya HİÇ eklenmez. **2026-09-07 sonrası kapsam düşümü: `/attributions` yok (K-W29, ODbL riski kabul edildi), tacir bloğu ad+e-posta (K-W30).** Yeni DS: `Toggle`, `SettingsCard`, `SettingRow`, `SourceRow`, `FaqItem`, `ReaderZone`, `PlainShell`. Bilinçli sapmalar: `OneZone` yerine sola hizalı `ReaderZone` (uzun düzyazı ortalanmaz), `SourceRow`'da sahte `aria-label` yerine `SettingsCard label`, `LegalPage` testinde `cleanup()` (i18next ayakta kalan render'ları da yeniden basıyordu). **Metinler hukukçu onayı bekliyor** — yayın kapısı. Bildir/engelle (W20) W-15'te. UI: Web Ekranlar v3 W13–W19. |
| W-15 | **Sosyal güvenlik + paylaşım — web** — `toastStore`/`ToastHost` bildirim kuyruğu, presence 2.0 (`ParticipantRow`: çevrimiçi noktası, bekleyen nabız, "Linki açtı", "Son görülen"), `socialStore` + host dürtme (60 sn soğuma, WS `nudged` toast), `PersonSheet` (Bildir · Engelle · yerel Sustur) + `voiceMesh.setMutedPeers`/`voiceStore.togglePeerMute`, sonuç kartı görseli (`lib/shareCard.ts` + `html-to-image` 1080×1920, CORS ön deneme → gradyan+monogram, `ShareButton` iki modlu), `lib/ics.ts` + `MeetTimeDialog` (yerel saat) + Google Calendar, davet linki OG/Twitter meta (`index.html`) | `2026-09-06-plan37-web-social-share.md` | Plan 37 | done | **B-14 ✓ · B-15 ✓** · W-13 ✓ | Task 9/9 + tam doğrulama | Gereksinim dok. §3/§4 — **R-W15** (bildir/engelle/yerel sustur), **R-W5** (presence 2.0), **R-W6** (host dürtme), **R-W3** (sonuç kartı PNG), **R-W4** (ICS/Google Calendar) kapandı. 9 görev, subagent-driven (impl Sonnet / review Opus, görev başına tek inceleme turu + gerçek bulguda düzelt-doğrula döngüsü). **520 test yeşil** (baz 495 + toastStore 2, ToastHost 1, ParticipantRow +3, socialStore 3, PersonSheet 3, voiceMesh +1, voiceStore +2, shareCard 3, ShareButton 1, ics 2, MeetTimeDialog 1, meta 2 = +25), tsc temiz, `i18n:check` 0 fark (CLDR çoğul eki normalize edilince tr 524/en 533/nl 533 sayıları eşleşir), prod build yeşil (`html-to-image` `index` chunk'ına girdi; `maplibre` chunk'ının 500 kB uyarısı W-12'den beri zaten vardı, yeni sorun değil). Bu iz de B-15'in hemen öncesindeki gibi öngörülenden fazla gerçek hata inceleme sürecinde buldu — HEPSİ kapsam içinde kapatıldı (B-15'in K-B34/K-B35'i gibi açık bırakılan yok): **T1** backend'in gerçek `ReportRequest.reason` birliği planın varsaydığı `OFFENSIVE_NAME`,`VOICE_HARASSMENT`,`SPAM`,`OTHER` DEĞİL, `HARASSMENT`,`SPAM`,`IMPERSONATION`,`OTHER` çıktı — düzeltme T2 (i18n `social.reason*` anahtarları) ve T6 (`PersonSheet` `REASONS` `satisfies` kilidi) içine işlendi. **T3/T4** temiz (T4'te yalnız `manual:true` satırı testsiz kaldı, mantık izle doğrulandı). **T5** temiz; plan-seviyeli iki boşluk not edildi: `nudged` WS dinleyicisi ve host'un "bekleyen" dürtme filtresi testsiz, `report` başarılı + `blockParticipant` başarısız kısmi hatasında jenerik hata mesajı kullanıcıyı yeniden bildirmeye yöneltebilir (bkz. K-W33). **T6** temiz (REASONS düzeltmesi yukarıda); plan-seviyeli iki boşluk: diyalogda odak tuzağı yok (bu kod tabanının ilk modalı), "ses odasına yeniden katılınca sustur hayatta kalır" testsiz (mantık izle doğrulandı). **T7** planın kendi `ShareCard.tsx` kodu BAYATTI — `venue.travelMinutes` (W-13'ün kaldırdığı, `travel[]` lehine silinen alan) okuyordu; `TravelBars`'ın zaten kullandığı paylaşılan `fairnessOf()` yardımcısıyla yeniden yazıldı. İki gerçek hata: `ShareButton` dosya-paylaşım modu `getFile` geri çağrısının senkron fırlatmasını/asenkron reddini yakalamıyordu (düğme kalıcı devre dışı kalabilir ya da sessizce hiçbir şey olmayabilir); `ShareCard` altbilgisi yol verisi hiç yokken yanıltıcı "herkes ~0–0 dk" özeti basıyordu (gerçek sıfır fark durumundan ayırt edilemez) — ikisi de düzeltildi. **T8** CİDDİ kullanıcıya dönük hata: buluşma saati seçici UTC okuyup yazıyordu — UTC+0 dışındaki HER kullanıcı takvim davetini yanlış duvar saatinde alırdı (New York UTC-4 izlemesiyle 4 saatlik sapma doğrulandı); gerçek yerel saat semantiğine geri alındı, yalnız TEST ortamının belirleyiciliği için `vite.config.ts` `test.env.TZ=UTC` sabitlendi (üretim kodu saat dilimi körü bırakılmadı). **T9 (bu görev):** OG/Twitter meta etiketleri `index.html`'e eklendi (`<title>` sonrası; `og:type`/`site_name`/`url`/`title`/`description`/`image{,:width,:height}` + `twitter:card`), `og:image` mutlak URL testi. **Plan sapması:** planın kendi `meta.test.ts` taslağı `node:fs`/`node:path`/`__dirname` kullanıyordu — proje `@types/node` içermiyor (kod tabanı emsali `lib/fastRefresh.test.ts`, aynı gerekçeyle `node:fs`'den kaçınıyor) ve bu `tsc -b`'yi kırardı; test `import.meta.glob(...,{query:"?raw",eager:true})` ile eşdeğer davranışa yeniden yazıldı, aynı iki iddiayı doğruluyor. **Bilinçli sapmalar (plan metninde gerekçeli):** ICS `TZID` yerine `X-WR-TIMEZONE`, rapor sonrası otomatik engel, dürtme düğmeleri satır içinde değil kart altında. UI kaynağı: Web Ekranlar v3 W3/W5/W6d/W8/W20. **Elle uçtan uca kontrol listesi** (plan T9 Step 5, 9 madde — iki tarayıcı/gerçek foto CDN/sistem paylaşım sayfası gerektirdiğinden ajan koşamaz) kullanıcıda; plan dosyasında yazılı durur. K-W15/K-W16/K-W33 açıldı. |
| W-16 | **Sessiz yenileme — web** — `@bumpinto/shared` `createHttp`e **tek-uçuşlu** (single-flight) 401 kesicisi: 401 gelince bir kez `POST /api/auth/refresh`, ardından ÖZGÜN isteği tekrar oynat; eşzamanlı 401'ler TEK yenileme sözüne kuyruklanır; yenileme başarısızsa oturum kapatılır ve köke dönülür | `2026-09-09-plan45-refresh-token.md` | Plan 45 | **done** (2026-09-09) | **B-16** | — | Kesici **shared**'a yazılır (web ve mobil aynı gövdeyi kullanır — depo deseni: saf mantık shared, platform adaptörde); web yalnız çerez tabanlı `refresh()` ve `onSignedOut()` sağlar. **Tuzaklar:** yenileme ucunun KENDİSİ 401 verirse tekrar denenmez (sonsuz döngü), aynı anda 5 istek 401 alırsa 5 yenileme atılmaz, tekrar oynatmada gövde/başlık bozulmaz. Testler: N paralel 401 → 1 yenileme · yenileme hatası → çıkış · yenileme ucunda tekrar YOK.  **UYGULANDI (Plan 45).** Kesici `frontend/shared/src/http.ts` içinde; `createRefreshGate` dışa açıldı (mobil önden yenilemesi AYNI uçuşa katılmak zorunda). 8 birim testi: N paralel 401 → 1 yenileme · yeni jetonla tekrar oynama · kimlik uçlarında tekrar YOK · yenileme hatası → `onSignedOut` · ikinci yenileme yok · uçuş bitince kapı açılır · 401 olmayan hata dokunulmaz · **jeton kaybolduysa `Authorization` başlığı DÜŞER** (tekrar oynamada `config` eski başlığı taşır, `delete` olmasa ölü bearer sonsuza dek geri gönderilirdi). Web `refresh()` KESİCİSİZ ham axios ile atılır (döngü yapısal olarak imkânsız); `onSignedOut` kancası `main.tsx`te kurulur — `authStore` `api`yi ithal ettiği için ters yön döngü olurdu ve testlerin `vi.mock` ikizlerini bozardı. |
| W-17 | **Keşfet — web** — `/kesfet` sayfası + `discoverStore` + TopBar girişi (POC P1/P1m/P1b), `NewSessionPage` açık plan alanları (P3), plan detayı + katılım isteği (P2), host istek paneli (P4), "Buluştunuz mu?" alt sayfası (P5), profil ilgi alanları | `.github/modernize/java-upgrade/20260902170714/plan.md` §W | plan.md §W | **done** (2026-09-11 gece, inline, W-18 ile birleşik — kilit 22) | **B-17** ✓ | W1–W7 + kapılar | 7 görev (W1–W7). Plan gövdesi bu izi `W-16` diye anıyor — o numara plan45'te kullanıldı, doğrusu **W-17**. **Kapılar (W-18 ile ortak):** web+shared **797 test / 117 dosya** (taban 692), `tsc -b` temiz, `pnpm build:web` temiz, i18n **tr 811 · en 829 · nl 829**, mobil `typecheck` temiz (shared değişti). Görev başına tek inceleme turu (3 tur; 2 Critical + 17 Important bulundu, hepsi aynı turda kapatıldı, biri kullanıcı kararına → K-B40); düzeltme testleri mutasyonla doğrulandı (jsdom'da yeniden üretilemeyen tek odak kaybı testte "pozitif kontrol" olarak işaretli). **Plandan sapmalar:** (1) **W1** i18n yolu `frontend/shared/src/i18n/locales` (plan web yolunu diyordu); shared `mySeat` jeton SAKLAMAZ — `rememberParticipantToken` yalnız mobilde, `join` kalıbındaki gibi çağıran saklar; `discover` süzgeci virgüllü tek parametre, boşsa hiç gönderilmez (axios dizi biçimini Spring bağlamaz); `seatsFull` (dolu plan Keşfet'e gelmez), şehirli `overline` (API'de şehir yok) ve `showInDiscover*` (kilit 22) YAZILMADI. (2) **W2** plan bölümü SOL bölgede ve yalnız GRUP'ta (sağ bölge 390'da gizli; SOLO↔plan değişmezi store setter'larında). (3) **W3** çipler 15 türün düz şeridi (sıra ilk yüklemede donar), son seçili tür kaldırılamaz (sunucu boş süzgeci ilgi alanlarına çevirip çipleri geri zıplatırdı), "Diğer türlere bak" tüm türleri seçer; dakika konumu PROFİL varsayılanından 2 ondalık yuvarlanır (Keşfet açılışında konum izni istenmez); 390 aralık rayı 3'lü (artboard); `discover.trust`'tan "host onaylıyor" düştü (OPEN planlar var); tarih 6 günü aşınca gün+ay (`meetAtOptions` — 14 günlük pencerede "Cmt" iki hafta sonunu aynı gösteriyordu); `Sticker amber`; Keşfet TopBar'da 390'da da görünür + AvatarMenu satırı; her açılışta "Bu hafta" (§11.2). (4) **W4** `SessionPreview`'da semt/dakika YOK → plan detayında o satırlar çizilmez; OPEN koltuk isteği anında `APPROVED` döner ve çerez YAZMAZ → `refresh()`; istek profil konumunun koordinatını gönderir, ETİKETİ göndermez (host onaydan önce görür, ev adresi olabilir); `sessionStore.previewSettled`: önizleme denemesi bitmeden `JoinForm` açılmaz (mount'ta konum izni ister, açık planda yanlış ekran) — kilitli "önizleme gelmediyse katılım formu" testi "önizleme alınamadıysa" olarak güncellendi; K-B37 devri: `JoinForm` 409 `open_plan_seat_request_required` → önizlemeyi tazeler; `SignInBlock onDone`; `GoogleSignIn` `onDone`'u ref'ten okur (satır içi geri çağrı GIS düğmesini her render'da yeniden kuruyordu); bekleme yoklamasında ağ hatası durumu değiştirmez. (5) **W5** panel yalnız `LobbyPage`'de (WaitingRoom host'a hiç çizilmez — plan ikisini diyordu); depo plana (`slug`) bağlı ve çıkışta sıfırlanır (Keşfet deposu da); hesap oturumu olmayan host uca gitmez (401 → çıkış kesicisi), giriş bloğu görür; 409 gövdeleri düz metin (`plan full` → errFull, `already decided` → sessiz tazeleme); `SeatRequestDto.minutes` hiç gelmez, çizilmez; bekleyen avatar nötr, onaylının rengi roster'daki kanonik dizinden; Onayla/Geç `aria-describedby` ile kişiye bağlı. (6) **W6** `CheckinPrompt` SessionPage'de HER aşamanın üstünde (buluşma lobi/mekan/deste/karar aşamalarının herhangi birinde geçebilir); yalnız cevap işaretler, kapatmak o açılışlık; 409 tekrar denenebilir, 403/404 kalıcı; diyalog açıkken deste kısayolları susar; "Lobby/Result'ta `openPlan` damgası" alt maddesi hiçbir artboard'da yok → YAPILMADI. (7) **W7** ilgi alanları `ProfilePrefs` satırı (en çok 5, iyimser; hata → sunucunun onayladığı listeye döner); `updatePrefs` `interests`'i TAŞIMAZ (sunucuda null = değiştirme; bayat kopya uçuştaki ilgi alanı kaydını ezerdi). **Manuel test kullanıcıda.** |
| W-18 | **"Buradayım" + kitle + rozetler — web** — shared `openPlan.ts`/`planRange.ts`/`badges.ts` + `newSession.ts` `plan` alanı; form "Ne zaman: Belirsiz \| Şimdi \| Tarih seç" + "Kim görsün? Herkes \| Kimse" ("Keşfet'te göster" anahtarı kalkar); Keşfet "Buradayım" + "Şimdi" aralığı + süren kart; P2a süren detay + OPEN kopyası; `BadgeRow` + check-in rozet anı; i18n tr/en/nl | `2026-09-11-plan48-instant-plan-web.md` | plan48 | **done** (2026-09-11 gece, W-17 ile birleşik) | **B-18** ✓, W-17 ✓ | T1–T6 + kapılar (W-17 satırında) | 6 görev. W-17 W2 başlamadıysa W-18 T2 şekliyle yapılır (kilit 22). **Sapmalar:** (1) kapasite artboard P3 **stepper**'ı (`Stepper` molekülü, 3–8, sınırda `aria-disabled` — odak kaybolmaz); shared `CAPACITIES`/`Capacity` KALDIRILDI → `MIN_CAPACITY/MAX_CAPACITY`, `openPlanInputOf` sıkıştırır. (2) **Şimdi çapası "o anki konum" (§1.3):** `useOwnLocation` konum KAYNAĞI taşır (`initial/detected/picked/typed`), Şimdi'ye girince `redetect()` — profil varsayılanı çapa olmaz (inceleme Critical'ı: OPEN planda koltuk alan herkese kayıtlı ev noktası gidiyordu); ANCHOR modunda kalmış eski çapa Şimdi'ye sızmaz (planın `??` yedeği kaldırıldı). (3) shared `toCreateRequest` geçersiz planda FIRLATIR (bayat tarih sessizce gizli oturum kuruyordu) ve açık planı GROUP'a zorlar; web store gönderimde yeniden doğrular. (4) Sayfa başlığı moda göre ("Plan aç" / "Buradayım"), CTA "Planı aç" / "Buradayım de"; plan hataları ilk gönderim/blur sonrası görünür (Şimdi seçer seçmez kırmızı alan yok). (5) shared `formatDuration(min, t)` (`duration.hm/h/m`) ve `meetAtOptions`. (6) Sayaç anahtarları **`profile.statHosted/statMet/statStreak`** (plan `profile.hosted/met/streak` diyordu; `profile.hosted` mobilde "buluşma kuruldu" anlamıyla kullanılıyor). `ProfileStats`/`StatCard` silindi (`BadgeRow` yerini aldı; `friendsMet` artık çizilmez — §11.6). (7) Rozet farkı `api.me()` + `setMe` ile (plan `authStore.load()` diyordu — o ağ hatasında kullanıcıyı çıkışa düşürür). (8) Kopya: `checkin.lead` rozetleri de anar ("yalnız sayıma gider" B-18'den beri yanlış); NL "afgesproken" → "ontmoet", "zeker" → "bevestigd"; kart ulaşım eki için `travelMode.*.by` ("bisikletle" / "by bike" / "met de fiets" — `.name` en/nl'de isimdi); artboard `.pho-tag` kontrastı WCAG için koyulaştırıldı. (9) `discover.thisWeek/weekend/all` hiç yazılmadı, doğrudan `discover.range.*` (kilit 22). **M-12 devri:** `CAPACITIES` yok → mobil `new.tsx` stepper + `MIN_CAPACITY..MAX_CAPACITY`; sayaçlar `profile.stat*`; `formatDuration`/`meetAtOptions`/`remainingMinutes` shared'da; `toCreateRequest` fırlatır → `toRequest` çağrısı try/catch; Şimdi çapası TAZE konum (kayıtlı varsayılan değil); seat-request 409 gövdeleri düz metin; `seat_requests_changed` tüm koltuklulara gider (host + hesap oturumu kapısı). **Açık karar: K-B40.** |

**Spec dışı görevler** — planlama/yürütme sırasında bulundu, spec'te yok. `Plan` kolonu:
`done` için kalemin çıktığı plan, `açık`/`aday` için hedef plan.

| Kimlik | Görev | Durum | Plan | Not |
|---|---|---|---|---|
| K-W1 | EN/NL artboard'larını rev 2'ye taşı | açık | W-2 | W-2'nin `_status` işaretli en/nl çevirileri onaylanamıyor — onay artefaktı `Katıl EN/NL 1280` artboard'ları (tasarım denetimi) **2026-09-11:** öncül bayat — `_status` işaretleri 2026-09-03'te (`938e12b`) locale dosyalarından kalktı; EN/NL için ayrı artboard onayının hâlâ istenip istenmediği kullanıcı kararı |
| K-W2 | Google giriş butonu Google'ın kendi pill'i olsun | done | W-3 | GIS politikası gereği; artboard'daki beyaz pill kullanılamıyor |
| K-W3 | "Bekle" ekranı kopyasını rev 2 akışına güncelle | açık | W-6 | Bugünkü metin rev-1 akışını anlatıyor (tasarım denetimi); yeni metin kullanıcı onayı bekliyor |
| K-W4 | Harita maliyeti — bugünkü kurulumda kal | done | W-4 | Karar: Dynamic Maps 10.000 yükleme/ay ücretsiz, 390'da Maps JS yüklenmiyor → ~2.000 oturum/ay'a kadar $0. Apple MapKit JS ve Mapbox elendi (Android'de Apple Maps yok; ToS engeli aynı). Ücretsiz harita (MapLibre + OpenFreeMap/Protomaps) yalnız veri Google'dan gelmezse mümkün → K-B19 (backend), K-M2 (mobil) |
| K-W5 | Tek `google.maps.Map` örneğini oturum boyunca ekranlar arası taşı | done | W-6 | **Alınmadı** (2026-09-03 kararı): harita 390'da hiç yüklenmiyor, 1280'de yalnız tıklayınca; tek örnek taşıma gereksizleşti |
| K-W6 | Karar/Bekle haritalarını Static Maps'e çevir | done | W-6 | **Alınmadı** (karar dokümanı §6): Karar ve Bekle'de harita yok — adres + "Google Maps'te aç" bağlantısı; statik görsel hâlâ Google haritası ve 110px'te okunmaz |
| K-W7 | `/terms` sayfası ve rotası | **done** (satır bayattı) | W-6 | Landing'deki "Koşulları" bağlantısı için sayfa/rota yok — bugün 404'e düşer (W-3 Task 4 notu) **2026-09-09 DOGRULAMA:** sayfa ve rota ZATEN var — `App.tsx:29` `<Route path="/terms" element={<LegalPage slug="terms" />} />`, metin `frontend/shared/src/content/legal/terms.ts`te uc dilde (W-14 ile geldi, K-W28 rejim kararina uygun). Satir W-14 kapanisinda guncellenmemis |
| K-W8 | **Haritasız değerlendirme + grup uzlaşması UX paketi** | **done** (B-7 + W-6, satır bayattı) | B-7 + W-6 | Karar dokümanı `docs/superpowers/specs/2026-09-03-map-free-group-decision-ux.md` (§5 A backend ön koşul / B hızlı kazanımlar / C ekran paketi; §7 kullanıcı kararları). Bulgular: 390 Mekanlar'da Maps JS liste sekmesinde de yükleniyor; `SessionViewAssembler` viewer dışı dakikayı tam koordinattan hesaplıyor (gizlilik); RUNOFF'ta `runoffVotes` herkese gidiyor; Google maskesi zaten Enterprise katmanında **2026-09-11:** çatı satırı; paket B-7 ve W-6 ile teslim edildi. Bulgular kapandı: `VenueBrowser` `MapView`'ı `lazy` yüklüyor, `SessionViewAssembler` konumu `approx` (~1,1 km) basıyor, RUNOFF'ta yalnız kimin kilitlediği (`runoffVotes().keySet()`) gidiyor |
| K-W9 | TravelRange (≥ 4 katılımcıda min–max çubuk) | done | W-6 | **Alınmadı** (2026-09-03): artboard yok, mock'lar 3 kişi; TravelChips satır sarar. ≥ 4 kişilik gruplar ölçülünce yeniden değerlendirilir |
| K-W10 | Bireysel akışta mevcut manuel noktanın ulaşım türünü düzenleme | aday | B-8/W-7 | W-6:T6b — `PointsEditor` yeni noktada modu gönderir; var olan katılımcının modunu değiştiren backend ucu yok (`LocationRequest` lat/lng zorunlu, manuel nokta için ayrı uç yok) |
| K-W11 | Artboard–kod görünüm uzlaşması (W-6 kalıntıları) | aday | W-7 | TravelModeField `.f-seg` ✓ ama `.f-mp` 22px kompakt seçici yok; ActivityStrip kart görünümü vs artboard düz satır; SessionSteps numaralı daire vs kalın metin; EBIKE ikonu [Lightning, Bicycle] vs bisiklet+rozet; Segmented radiogroup'ta roving tabindex yok (ActivityPicker'da da) |
| K-W13 | "Bu buluşma kapandı" ekranı (DECIDED / EXPIRED davet linki) | done | W-7 | Kapanmış ya da süresi dolmuş bir linke giren kişi bugün katılım formunu görüyor ve gönderince 409 alıyor — çıkmaz sokak. Gereken: `preview` zaten `status` taşıyor → `SessionPage` DECIDED/EXPIRED'de form yerine kapanış ekranı bassın (karar verildiyse seçilen mekan + "Yol tarifi", süresi dolduysa yalnız bilgi). Tasarım kaynağı yerel `web.out.html` W10 Hata kalıbı; 3 dilde kopya |
| K-W12 | Masaüstü genişlik: artboard 1280 çerçevesinden bilinçli sapma (kullanıcı incelemesi 2026-09-03) | done | W-6 | `Page` tavanı 70rem → 80rem (lg) / 96rem (xl), `wide` prop ile tavansız; Mekanlar: liste `minmax(26rem,32rem)` + harita kalan genişlik, `sticky`, `calc(100dvh-7rem)`; 390'da açılan harita 60dvh; Lobi haritası viewport yüksekliği. Global `cursor: pointer` kuralı (`app.css` @layer base) + harita pini. ▲ fark 0'da gizli. Artboard'lar/DS §07 (58/42 grid) bu kararla güncellenmeli (Claude Design limiti dolunca ertelendi) |
| K-W14 | Web v3 kod senkronu (W-12 adayı): DS token/renk düzeltmeleri (üstlük ink2, rozet ≥12px), `TravelChips` → yol çubuğu (`RangeBar`/`TravelBars`), `WinnerCard` → sonuç kartı + görsel paylaşım + takvim, presence/dock UI (Lobi/Bekle/Gönderildi), mekan satırında saat + "neyle bilinir", 3 etkinlik sınırı ve çapalı oturum artboard'ları, yasal sayfalar (`/privacy /terms /kvkk /account`, `/account/delete` K-B28) | **done** (satır bayattı, 2026-09-11) | W-13/W-14/W-15 | Kaynak `2026-09-06-mobile-design-direction.md` §2–§4; gereksinim dok. `2026-09-06-v3-requirements.md` §4. **W-13'te yapıldı:** DS token/renk düzeltmeleri (üstlük ink2, rozet tek punto 0.75rem, amber/pembe metin), `TravelChips`/`FairnessBadge`/`TravelList` → `RangeBar`/`TravelBars`, mekan satırında saat + "neyle bilinir", dock lg+ sağ altta yüzer, çevrimdışı şeridi + mekan arama iskeleti. **W-14'te yapıldı:** yasal sayfalar (`/privacy /terms /data-rights` — K-W28 ile `/kvkk` yerine), `/account` (Yasal/Veri/Hakkında/Tehlikeli bölge), `/account/delete`, Apple girişi. **W-15'te yapıldı (2026-09-07):** sonuç kartı görseli + takvim (`ShareCard`, `lib/ics.ts`, `MeetTimeDialog`) ve presence/dürt UI (`ParticipantRow` 2.0, `socialStore`, `PersonSheet`). **Hâlâ kalan (hiçbir plana düşmedi):** 3 etkinlik sınırı ve çapalı oturum artboard'ları koda hiç işlenmedi — sonraki bir plana aday **2026-09-11:** kalan iki kalem de kodda — 3 etkinlik sınırı (`MAX_ACTIVITIES`, `ActivityPicker`/`NewSessionPage`) ve çapalı oturum (`newSessionStore` + `MidpointCard` `anchored`) |
| K-W17 | Sağlayıcı işareti (`providerMark`) iki yüzey arasında tutarsız | **done** (satır bayattı, 2026-09-11) | W-15 | W-13/T6: artboard W3b **ve** W6 meta satırında sağlayıcı işareti çizmiyor (köken liste/kart dibindeki `Attribution`'da: "Powered by Foursquare", "Google Maps", "© OpenStreetMap contributors"). Plan `VenueMeta`'dan düşürdü, `VenueCard` polaroid dalında BIRAKTI → satır "★ 4,6", kart "★ 4,6 · Fsq". Yasal atıf zaten `Attribution`'da (K-W: yükümlülük yok, şeffaflık kaybı). Karar: kart dalından da düşürülürse `lib/format.ts#providerMark` + `format.test.ts` 3 testi ölür **2026-09-11:** tutarsızlık kapandı — `VenueCard` artık `providerMark` basmıyor (satır ve kart aynı); işaret yalnız `WinnerCard` meta satırında, artboard 2544 `.mi` gereği |
| K-W18 | `RangeBar`: herkes tam eşit dakikadaysa noktalar üst üste biniyor | **done** (satır bayattı, 2026-09-11) | W-15 | W-13/T4: `f.min === f.max` çok kişili durumda tüm baş harf noktaları %50'ye iniyor (bant hiç çizilmiyor — bu doğru). 5 dk'ya yuvarlama yüzünden gerçekçi bir durum. Artboard bu hâli çizmemiş; ofset uydurulmadı **2026-09-11:** `RangeBar.fanned()` eşit dakikadaki noktaları nokta genişliği kadar (`TIE_GAP` %5,5) simetrik yelpazeliyor |
| K-W20 | Masaüstü ses dock'u ölçüleri artboard'da yok | **kapandı** (karar değişti, 2026-09-11) | W-15 | W-13/T8: `Web Ekranlar v3` **W12 yalnız 390 · 7 durum bileşen sayfası**; 1280 varyantı çizilmemiş. Yön "v3 notları"nda tek cümle ("dock 1280'de sağ altta yüzer"). `lg:right-6`/`lg:bottom-6`/`lg:max-w-[32rem]`/`lg:rounded-card`/`lg:shadow-sh2` sayıları plandan geldi, artboard'dan doğrulanamıyor. 7. durum metni ve 390 şeridi artboard'la birebir tutuyor **2026-09-11:** yüzen masaüstü dock'u yok — kullanıcı kararıyla (2026-09-08) ses denetimi başlık satırında (`VoiceDock placement="header"`/`"header-lg"`); plan kaynaklı `lg:right-6`/`lg:bottom-6` sayıları koddan çıktı |
| K-W21 | 390'da ses dock'u CTA'nın ÜSTÜNDE olmalı | **done** (satır bayattı, 2026-09-11) | W-15 | W-13/T8 incelemesi: tasarım dock'u CTA'nın üstünde istiyor, uygulama `order-last` ile atıf altbilgisinin ALTINA koyuyor. W-11'den geliyor, W-13 kapsamı dışı **2026-09-11:** web v3 parite turunda kapandı — `MobileCta` `voice` slotu şeridin EN ÜSTÜNDE, `order-last` kalktı |
| K-W22 | Oda uzunluğu istemcide türetiliyor (`VoiceDto`'da alan yok) | aday | B-15/W-15 | W-13/T8: `limitMinutes` odanın AÇILDIĞI an gözlenerek türetiliyor; istemci saati kayması 30→29/31 yapabildiği için 5 dk'ya yuvarlanıyor. Kalıcı çözüm: `VoiceDto`'ya oda uzunluğu alanı |
| K-W23 | Bireysel kurulumda mekan arama iskeleti yok | **done** (2026-09-11) | W-15 | W-13/T10 (planın koşullu dalı): `SoloSetupPage` tek `useSessionAction()` örneğini `findVenues` + `addPoint` + `removePoint` için paylaşıyor; `if (busy) return <VenuesLoading/>` eklenirse konum eklerken/silerken de tam ekran iskelet çıkardı. Gereken: `findVenues`'e ayrı `useSessionAction()` ya da eylem-kimlikli `busy` **2026-09-11 KAPANDI:** `frontend/web/src/pages/SoloSetupPage.tsx` iki ayrı `useSessionAction()` açıyor — yalnız `find.busy` `<VenuesLoading/>` döner, nokta ekle/sil yerinde kalır, iki hata da basılır. Manuel test kullanıcıda geçti. |
| K-W24 | `VenuesLoading` artboard'ın iki öğesini basmıyor | **done** (satır bayattı, 2026-09-11) | W-15 | W-13/T10: W3e'de orta nokta kartı (`.card.f-mid` — "Orta nokta / … / ≤ 9 km · herkes ~25–35 dk") ve altta devre dışı "Karıştır ve kaydır" CTA'sı var; bileşen yalnız `name` alıyor. Basmak için `view` geçmek gerek — bilinçli kısaltma **2026-09-11:** `VenuesLoading` artık `view` alıyor — `MidpointCard` + devre dışı `MobileCta` Karıştır düğmesi basılıyor |
| K-W25 | İskelet ekranı kopyası artboard'dan daha genel | **done** (satır bayattı, 2026-09-11) | W-15 | W-13/T10: artboard "Çevredeki **kahve** mekanları aranıyor" + "**Google ve Foursquare'den 3 kişinin** yoluna göre sıralanıyor"; kod tür/sağlayıcı/kişi sayısı olmadan genel metin basıyor (T1'in i18n anahtarları böyle). Sağlayıcı adlarının düşmesi K-W17 kararıyla tutarlı; aktivite türü `view`'da var **2026-09-11:** tek ilgi alanında `venues.searchingTitleFor`; sağlayıcı adları `config.sources` + i18n'den, kişi sayısı `view`'dan (`venues.searchingCopyFrom`); ad yoksa genel kopya |
| K-W26 | Çevrimdışı şeridinin 1280 hâli artboard'da yok | aday | W-15 | W-13/T9: `Web Ekranlar v3`'te yalnız **W10b · Çevrimdışı — 390** var; plan "390/1280" diyor. `lg:mx-12` artboard'dan doğrulanamıyor (K-W20 ile aynı şekil). 390 ölçülerinde de küçük sapmalar: kenar 16 vs 18px, iç boşluk 16 vs 14px, kenarlık `line2` vs amber tonlu `#F3DDB0`, ikon 21 vs 19px |
| K-W27 | **`GET /api/me/export` W-14'ün bağımlılık satırında B-14'e yazılmıştı, aslında B-15'te** — `/account` sayfasındaki "Verilerimi indir" düğmesi B-15 inene kadar çalışma anında 404 döner (testler mock'ladığı için suite yeşil kalır) | **done** (2026-09-09) | B-15 | Kod W-14'te planlandığı gibi yazıldı (`exportMyData` + düğme + 429 hata metni); eksik olan yalnız sunucu ucu. **Yayın kapısı: `/account` B-15'siz prod'a çıkmamalı** — Play Data safety "veri indirme" vaadi kırık görünür. B-15 inince ek iş yok, uç kendiliğinden çalışır **2026-09-09 DOGRULAMA:** B-15 indi, uc gercekten var — `MeController.export` (`@GetMapping("/export")`), `openapi.json`te kayitli, `shared/src/api.ts` `exportMyData` onu cagiriyor, `AccountPage.tsx:54` blob'u indiriyor. Calisma anindaki 404 riski kalktı; yayin kapisi olarak isaretlenmesine gerek yok. (Hız siniri bu arada K-B34 ile hesaba anahtarlandi) |
| K-W28 | **Yasal metin yapısı kullanıcı talimatıyla değişti (2026-09-07)**: gövde TR taban + çeviri şeridi DEĞİL, üç dilde tam metin; `/kvkk` → **`/data-rights`**, rejim dile bağlı (TR = KVKK m.10/11, EN/NL = GDPR Art. 13/14 + 15–22) | done (W-14:T3–T4) | W-14 | Plan 36 başına Ek A yazıldı. `/privacy` GDPR Art. 13/14 için **hukuki dayanak** + **yurt dışına aktarım** başlıkları kazandı. Kabul edilen risk: dil ≠ yargı yetkisi (Hollanda'daki Türkçe konuşan GDPR'a tabi) — azaltma olarak her sürüm diğer rejime köprü kurar, testle sabit. **Metinler hukukçu onayı bekliyor** (planın yayın kapısı). M-5'in mobil yasal ekranları bu yapıyı izlemeli |
| K-W29 | **`/attributions` sayfası tamamen kaldırıldı (kullanıcı kararı, 2026-09-07)** — rota, sayfa, `content/legal/sources.ts` (`DATA_SOURCES` + `OSS_LICENSES`), altbilgi bağı ve altbilgideki `attribution.osm` satırı; `attrib.*` ve `legal.attributions` i18n anahtarları silindi. Gerekçe: teknik yığını rakiplere göstermemek | done (W-14) | W-14 | **KABUL EDİLEN RİSK — yazılı uyarıldı:** OSM/Nominatim verisi **ODbL 1.0** altında ve atıf bir *lisans koşulu*; Google Maps Platform ve Foursquare şartları da atıf zorunlu kılıyor. Sayfa rakipten bir şey saklamıyordu (ağ sekmesi sağlayıcıları zaten gösteriyor) ve gizlilik metni GDPR Art. 13 gereği alıcıları (Google, Foursquare, OSM) **adıyla saymak zorunda** — bilgi orada duruyor. **Hâlâ duruyor:** `molecules/Attribution.tsx` mekan ekranlarında (W-12); kaldırılması istenirse ayrı iş. Geri almak isteyene: bu satır + git geçmişi |
| K-W31 | **Mekan fotoğrafları tarayıcıdan sağlayıcı CDN'ine gidiyor** (`VenueThumb` `<img src={v.photoUrl}>` → Google Places Photo / Wikimedia / FSQ) | done | B-16? + W | Kullanıcı 2026-09-07 (ilk): "sistem her zaman backend üzerinden consume etmeli". Places/Foursquare/Nominatim **API çağrıları zaten backend'de** (W-12), geriye kalan tek gerçek sızıntı fotoğraf host'u. Çözüm önerisi: `GET /api/venues/{id}/photo` proxy + `VenueDto.photoUrl` göreli yol. Yan kazanç önerisi: W-15'in sonuç kartı görselindeki CORS sorunu da kapanır (html-to-image şu an dış CDN'den boyayamıyor). Maliyet: bant genişliği + önbellek; Google Places Photo ToS'u önbelleklemeyi sınırlar (photo_reference saklanabilir, görsel değil). **2026-09-07 KAPANDI — kullanıcı ilkeyi tersine çevirdi:** "asset'ler mümkün olduğu her yerde CDN'den gelmeli", backend'den değil; foto proxy'si YAPILMAYACAK, mevcut doğrudan-CDN davranışı kalıcı. CORS yan kazancı gereksiz — `ShareCard` zaten CORS hatasında gradyan+monogram'a düşüyor (W-15/T7), proxy'siz de bozulmuyor. |
| K-W32 | Harita karoları tarayıcıdan `tiles.openfreemap.org`'a gidiyor (`configStore` varsayılanı + `/api/config` `styleUrl`) | done | I | Proxy'lenebilir ama pahalı (her karo bir istek) ve openfreemap gizli bir bilgi değil. **Google Maps JS ve Google/Apple giriş SDK'ları taşınamaz:** Maps JS ToS proxy'lemeyi yasaklar, OAuth SDK'ları tanım gereği tarayıcıda koşar. `MAP_ENGINE` varsayılanı zaten `maplibre`. **2026-09-07 KAPANDI:** K-W31 ile aynı ilke — "asset'ler mümkün olduğu her yerde CDN'den gelmeli" — karo proxy'si de YAPILMAYACAK, mevcut doğrudan-CDN davranışı kalıcı. |
| K-W30 | `/support` iletişim bloğu **YALNIZ e-posta**; ad, telefon, adres ve **DSA m.30 atıfı** kaldırıldı (2026-09-07 ikinci düzeltme) | done (W-14) | W-14 | DSA m.30 pazaryerleri içindir (tüketicinin tacirle sözleşme kurduğu platformlar) — BumpInto'ya uygulanmıyordu, atıf en baştan yanlıştı. Not: AB'de dağıtımda tacir adı/adresi/telefonu **App Store ve Play listeleme sayfasında zaten görünür**; AB e-ticaret direktifi ad + coğrafi adres + e-postanın erişilebilir olmasını ister — adres artık yalnız mağaza listesinde |
| K-W15 | **`/j/:slug` OG kartı kenar yönlendirmesi** — crawler `User-Agent`'ını backend'in OG HTML'ine yönlendiren kural; SPA statik meta (W-15) yalnız varsayılan kartı verir | açık | I izi | Plan 37 T9 bulgusu; B-15 `GET /og/{slug}.png` hazır (done), yönlendirme kuralı yazılmadı — kenar katmanı (Cloudflare/Nginx) I izinin işi |
| K-W16 | **Engel kaldırma yüzeyi** — `GET /api/me/blocks` + `DELETE /api/me/blocks/{id}` UI'ı | **done** (2026-09-11) | W izi, sonraki plan | Plan 37 kapsamı yalnız engel EKLEME (artboard W20, W-15'te tamamlandı); liste/kaldırma hesap ekranının işi, W-14 (`/account`) da bunu kapsamadı **2026-09-11 KAPANDI:** backend `BlockDto.displayName` (okuma anında, `BlockRepository.listingRowsOf`) + `frontend/web/src/pages/BlocksPage.tsx` (`/account/blocks`, "Veri" kartından); W13–W20'de artboard yok → mevcut kabuk, onay diyaloğu yok. Mobil karşılığı K-M47. Manuel test (listele/kaldır) kullanıcıda geçti. |
| K-W33 | `socialStore.report`'ta kısmi hata: `api.report` başarılı olup ardından `api.blockParticipant` başarısız olursa kullanıcı yalnız jenerik "Gönderilemedi — tekrar dene." görür; rapor ASLINDA gitmiştir, yeniden denerse mükerrer rapor açılır | **done** (2026-09-09) | W izi, sonraki plan | Plan 37 T5 incelemesinde not edildi, düzeltilmedi (plan-seviyeli boşluk, implementer hatası değil). Düzeltme adayı: hata mesajını adıma göre ayır (rapor gitti ama engel gitmedi ⇒ farklı metin) ya da `report` başarılıysa `blockParticipant` hatasını sessiz yeniden dene **2026-09-09 KAPANDI, hem web hem MOBIL:** siralama kurali paylasilana tasindi — `frontend/shared/src/report.ts` `reportThenBlock(report, block)` uc sonuc doner (`done`/`reportFailed`/`blockFailed`); rapor duserse engel HIC denenmez (ucuncu bir yari-durum cikardi). Metni her istemci kendi yuzeyine gore secer: web `social.reportedNotBlocked` toast'i, mobil ayni anahtari `error` satirinda. **Plandan bir adim ileri:** engel dustugunde satir artik iyimser "engellendi" SAYILMIYOR — sunucu o kisiyi hala iceri alirken yerel gizleme sahte guvenlik olurdu. Yeni i18n anahtari uc dilde (tr 663 / en 674 / nl 674). Regresyon: `shared/src/report.test.ts` (3), `web/src/store/socialStore.test.ts` (2), `mobile/src/store/socialStore.test.ts` (2) |
| K-W34 | **Oturumlar ekranı: boş açık bölge + uzayan geçmiş listesi** — açık buluşma yokken "Açık buluşmalar" başlığının altı bomboştu; geçmiş kartı 20 satıra dek uzayıp iki sütunu dengesizleştiriyordu | **done** (2026-09-11) | kullanıcı isteği (ekran görüntüsü) | Web `SessionsPage` + mobil `app/sessions/index.tsx`. Açık bölge boşken tek satırlık not kartı (`sessions.openEmpty`; CTA başlıkta/alt çubukta TEK kalır); web'de boş geçmiş için içi boş kart çerçevesi yerine `sessions.pastEmpty`. Geçmiş kartı en yeni **5** satırla açılır (`shared/src/sessionList.ts` `PAST_PREVIEW`), "Tümünü göster (N)" yerinde genişletir / "Daha az göster". **Sayfalama BİLEREK yok:** sunucu zaten 20'de kesiyor (`pastTruncated`), ≤4 sayfa için sayfa düğmesi tıklama ekler, bilgi eklemez. `retentionTruncated` notu yalnız bütün satırlar görünürken basılır (kart kapalıyken "son 20 gösteriliyor" yanlış olurdu). Yeni test yazılmadı (kural: manuel test önce); web 797 / mobil 270 test, web build, mobil tsc+lint, i18n parity yeşil. Commit + manuel test kullanıcıda. |

## M — Mobil

| Kimlik | Plan | Dosya | Eski # | Durum | Bağımlılık | Son adım | Not |
|---|---|---|---|---|---|---|---|
| M-1 | Expo RN host uygulaması | `2026-09-01-plan4-mobile.md` | Plan 4 | superseded | B-6 ✓ (Ek A) | — | **Ek A (2026-09-02) gövdeden önce okunur:** cihaz-yerel liste YOK (`GET /api/sessions`), Apple girişi YOK, 15 tür 4 grup + "Bowling", grup tint'leri, Profil `/api/me`, `BROWSING` yer tutucu + "Karıştır", `sessionType: GROUP`, `locationLabel`. Google OAuth client id'leri kullanıcıda. Task 7 (EAS build) `I-1:T4`'ü besler |
| M-2 | **Mobil parite** — ortak dil dosyaları (`frontend/shared/src/i18n`), react-native-maps + pinler, Yeni buluşma tip/gruplu chip/elle konum, Mekanlar ekranı (tam ekran harita + şerit), Bireysel kurulum, Lobi/Bekle/Karar haritaları, Profil dil/tercih | `2026-09-02-plan13-mobile-parity-map.md` | Plan 13 | superseded | **M-1**, B-5 ✓, B-6 ✓, W-4 ✓ | — | W-4'ten sonra: dil dosyaları web'den shared'a taşınır (tek kaynak). Expo Go'da Google Maps yok → dev build. Maps SDK anahtarları `app.config.ts` env'den (kullanıcı) |
| M-3 | **Mobil parite — haritasız uzlaşma** — paylaşılan adalet mantığı (`frontend/shared`), TravelChips/FairnessBadge RN, ulaşım türü seçici, haritasız varsayılanlar, Deste/Deste bitti/Runoff/Karar v2 | `2026-09-03-plan17-mobile-fairness-mapfree.md` | Plan 17 | superseded | **M-2**, B-7, `W-6:T1` | — | 6 görev; Task 1 shared `fairness.ts`'i tüketir (W-6 üretir). Web artboard'ları onaylanınca `Mobil Ekranlar v2` 04–08 pariteye çekilir; o zamana dek web 390 artboard'ları + karar dokümanı §4 bağlayıcı |
| M-4 | **Mobil temel** — Expo SDK 57 CNG prebuild (Expo Go yok, New Arch; React 19 ön koşulu), pnpm workspace + jest-expo, `app.config.ts` (AASA/assetlinks `bumpinto.app/j/*`, edge-to-edge, predictive back) + EAS dev build + sözleşme denetimi, `theme.ts` v3 + Phosphor eşlemesi, 13 atom, i18n + 9 saf `lib` modülü `frontend/shared`'a (web shim), Google girişi + secure-store + `authStore` + Giriş (O2), Oturumlar (P1/P2) + Profil (P22) | `2026-09-06-plan38-mobile-shell-v3.md` | Plan 38 | **done** (2026-09-08) | B-6 ✓ (mevcut API) | — | **M-1/M-2/M-3'ü supersede eder.** 9 görev (plan38 üçe bölündü: M-4/M-7/M-8). Kapılar: mobil 10 test + tsc + `expo lint` temiz, web+shared 639 test, i18n paritesi, `expo install --check` temiz, android prebuild uyarısız. K-M5: `SessionView`'da çapa etiketi yok, `midpointLabel`'dan okunur. **Plandan üç sapma (T-notları):** `personColor` + `languages` shared'a 10./11. modül olarak alındı (kullanıcı kararı, kişi rengi kimlik kuralı), shared testleri web vitest'i üzerinden koşar (ayrı vitest kurulmadı), `expo-linear-gradient` eklendi (gradyan avatar/monogram). UI: Mobil Ekranlar v3 P1/P2/P22, O2. |
| M-5 | **Mobil mağaza ve yasal paketi** — `app.config.ts` + `withPrivacyInfo` plugin (purpose string'ler O4/O7, `ITSAppUsesNonExemptEncryption=false`, izinler, target API 36), konum/mikrofon ön-ekranları + red kurtarma, Apple girişi (`expo-apple-authentication`), Hesap ve veriler, yasal okuyucular, açık rıza + rıza kapılı analitik, hesabı sil ("SİL"), bildir/engelle, `docs/store/RELEASE-CHECKLIST.md`, gerçek istemci testleri | `2026-09-06-plan39-mobile-store-compliance.md` | Plan 39 | done | **B-14**, **W-14** (yasal URL'ler), **M-4** | — | R-M1–R-M7, R-M15, R-M16. 12 görev. Play kapalı test (12 tester × 14 gün) M-5 bitmeden başlatılır. UI: Mobil Onboarding, İzinler ve Yasal O2–O19. **Yapıldı 2026-09-08** (77 mobil test / tsc / lint temiz, 643 web yeşil). SAPMALAR: (1) `BlockListResponse` sözleşmede YOK — `GET /api/me/blocks` `BlockDto[]` döner, codegen esas alındı; (2) shared istemci adları W-13/14'ten devralındı (`putConsents`/`report`/`blockParticipant`/`exportMyData`), yalnız `listBlocks`+`removeBlock` eklendi; (3) yasal metinler `frontend/shared/src/content/legal/`e taşındı — web ile TEK kaynak, okuyucu ÜÇ DİLDE (K-M4 doğmadan kapandı); (4) testler `app/` altında DEĞİL `src/__tests__/`te (M-4 route-tree kapısı) ve `renderRouter` yok — ekran doğrudan çizilir; (5) `edgeToEdgeEnabled` SDK 57'de kaldırıldığı için beyan edilmedi, `ios.deploymentTarget` 15.1 değil **16.4** (paket 16.4 altını reddediyor); (6) T3'ün `app/sessions/new.tsx` / `app/j/[slug].tsx` bağlantısı ve derin link testi **M-7'ye kaldı** (rotalar henüz yok) — `LocationDeniedCard` hazır, anonim rota sözleşmesi `app/_layout.tsx` `ANONYMOUS_ROUTES` ile kilitlendi. |
| M-6 | **Sesli sohbet — mobil** — `voiceMesh` saf mantığı `frontend/shared/src/voice/`'a (web adaptöre indi), `react-native-webrtc` + plugin + `InCallManager` + 16 KB `.so` hiza denetimi, STOMP `liveChannel` RN portu (`X-Participant-Token` el sıkışma), mikrofon izni (O7 → sistem → red), `dockState` 7 durum + `VoiceDock` (P25), `ParticipantRow` mikrofon/halka, arka plan bekçisi, Maestro + iki uçlu kontrol listesi | `2026-09-06-plan40-mobile-voice.md` | Plan 40 | **done** (2026-09-08) | B-12 ✓, **M-7 ✓**, B-14 ✓ | — | R-M10 + R-M9 dock. 8 görev. K10 ("sesli sohbet yalnız web") KAPANDI. Kapılar: mobil **206 test** (47 süit) + `tsc` + `expo lint` temiz, web **667** (663 → +4), i18n tr 642/en 653/nl 653. **RİSK KAPISI GEÇTİ (cihaz kanıtıyla):** `expo prebuild --clean` uyarısız + CocoaPods kuruldu; `./gradlew :app:assembleDebug` **BUILD SUCCESSFUL** (4m40s, app-debug.apk üretildi) — yani `react-native-webrtc` 124.0.8 + `@config-plugins/react-native-webrtc` 15.0.2 + `react-native-incall-manager` 4.2.2 SDK 57 / New Arch altında DERLENİYOR; 16 KB sayfa denetimi `libjingle_peerconnection_so.so align=0x4000` (Android 15+ yükler). **HÂLÂ DOĞRULANMAMIŞ:** (a) cihazda ses YÖNLENDİRMESİ (kulaklık/hoparlör/bluetooth — `InCallManager` interop katmanı), (b) uçtan uca gerçek ses (iki uç gerekir), (c) iOS derlemesi denenmedi, (d) `.maestro/04-voice-dock.yaml` YAZILDI, KOŞULMADI (K-M36). `scripts/ws-smoke.mjs` CANLI sunucuya karşı koştu: **el sıkışma DOĞRULANDI** (`X-Participant-Token` başlıkla kabul edildi, 401 yok); sinyal RÖLESİ doğrulanamadı — oda açmak host'a özel (`403 only the host can do this`) ve betik bunu sessizce geçmiyor, `TOKEN=<host jetonu>` ile tekrar koşulmalı (K-M37). Sapmalar: (1) **T4 ÇOKTAN VARDI** — M-5 O7 alt sayfasını (`app/(sheets)/mic-consent.tsx`), `requestMicrophone` (`granted/denied/blocked`) ve `presentMicConsent()`i zaten yazmıştı ve o modül kendini "M-6 dock'unun TEK giriş noktası" diye tanımlıyordu; planın `MicPrimingSheet`i ve `voice.priming.*` anahtarları YAZILMADI (ikinci mikrofon arayüzü + ikinci gerekçe metni olurdu). `micPermission.ts` yalnız rıza→`getUserMedia` köprüsü ve beşinci durum `failed` ekler (izin var ama cihaz akışı vermiyor — bunu `denied` saymak kullanıcıyı olmayan bir ayara gönderirdi). (2) `AudioSink`e `setMuted` eklendi: planın yüzeyinde yoktu ve taşımada web'in YEREL SUSTUR yeteneği (R-W15, `PersonSheet`) sessizce kaybolurdu. RN sink'inde no-op (kişi başı yerel sustur bugün yalnız webde). (3) `check-so-alignment.mjs` node_modules'te DEĞİL Gradle önbelleğinde arar: `react-native-webrtc` ikiliyi vendorlamıyor, `org.jitsi:webrtc:124.+` ile Maven'den çekiyor — kapı ancak bir gradle çözümünden sonra koşabilir. (4) `useSessionLive` M-7'nin 3 sn'lik poll'unu STOMP + 30 sn yedek ile değiştirdi; mobilin uygulama-durumu/çevrimdışı kapıları KORUNDU. (5) Dock beş ekrana tek tek eklenmedi: M-7'nin `VoiceDockSlot` yer tutucusu dolduruldu (o dosya tam bunun için açılmıştı). (6) jest: `react-native-webrtc` + `react-native-incall-manager` yüzey ikizleri eklendi; `voiceStore` testinde sahte mesh fabrikanın İÇİNDE (babel hoisting). (7) Eklenti `BLUETOOTH_CONNECT` EKLEMEDİ — bluetooth kulaklık yönlendirmesi cihazda sınanmalı (K-M37). (8) Yeni i18n: `voice.micBlocked`, `voice.speaker` (3 dil). (9) `VoiceDock.useNow` web'den ayrıştı: React Compiler hem çizimde `Date.now()`u hem efekt içinde senkron `setState`i reddediyor. UI: P25, O7, P6/P10/P11/P17 dock. **2026-09-09 CİHAZ KOŞUSU (K-M37) iki hata buldu ve ikisi de düzeltildi:** (i) dock beş ekranda `VoiceDockSlot` ile `ScrollView`in İÇİNDE mount ediliyordu; `position:absolute` orada kaydırma içeriğine tutunuyor ve dock listenin dibine düşüyordu — plan zaten "yönlendiricide bir kez mount edilir" diyordu, sapma geri alındı: `VoiceDock` artık `app/s/[slug].tsx`te ekranın KARDEŞİ, `VoiceDockSlot` yeniden yer tutucu. (ii) Hap içindeki eylem düğmesi tam genişlikteydi (atomun varsayılanı) ve avatarları/kişi sayısını/kalan süreyi eziyordu — `width:auto` verildi. Üçüncü bulgu ürün hatası olduğu için ayrı kayda alındı: **K-M38** (bayat hesap jetonu misafir isteklerini 401 yapıyor). |
| M-7 | **Kurma ve katılım** — Yeni buluşma (P3/P4: 3 etkinlik sınırı, çapa, tembel harita alt sayfası), konum izni akışı (O3 → sistem → O6) + Bireysel kurulum (P5), Katıl (P8/P9, derin link, host çevrimdışı notu, 409 "çok uzak"), durum yönlendirici + Lobi/Bekle (P6/P7/P10; M-8 ekranları `return null` iskeleti), Mekanlar (P11/P12/P13) + `RangeBar` | `2026-09-06-plan41-mobile-create-join.md` | Plan 41 | **done** (2026-09-08) | **M-4 ✓** | — | plan38'den bölündü. 6 görev; R-M14, R-M2, R-M8, R-M9(statik). M-8 ekranlarının iskeletleri T4'te açıldı. Kapılar: mobil 147 test + tsc + lint temiz, web 663, i18n paritesi, emülatörde (`bumpinto-api35`) P3→O3→sistem→O6→adres→kur→P6 ve P4→MapLibre seçici gözle doğrulandı; App Link `https://bumpinto.app/j/<slug>` gerçek istemcide P8'i açtı. **SAPMALAR:** (1) oturum rotası `/sessions/[slug]` DEĞİL **`/s/[slug]`** — muhafız ön ek eşlemesi yapıyor ve `/sessions`+`/sessions/new` hesap istiyor, aynı ön ekte misafir katıldığı anda giriş ekranına düşüyordu (M-4'ün `futureRoute` beklentisiyle de aynı); (2) K-M8 uygulandı — `newSession` kuralları, `activity`, `geo`, `travelText` shared'a taşındı, web shim'e döndü; (3) `locationStore` M-5'in `permissions.ts`'ini çağırır (planın ham `expo-location` taslağı değil) ve O3 sonucunu `adopt()` ile benimser — ikinci sistem diyaloğu yok; (4) `RangeBar` girdisi `travel[]` (K-B26), plandaki `travelMinutes` bayattı; (5) P9'daki "sen ~N km" satırı BASILMAZ — `SessionPreview` mesafe alanı taşımıyor; (6) `waiting.changeLocationAndMode` için `(sheets)/location-mode` açıldı; (7) harita seçici AYRI ROTA DEĞİL, formun üstünde açılan yerinde alt sayfa (`MapPickerSheet`, RN `Modal` + `animationType="slide"`) — rota olarak açıldığında form ekrandan kalkıyor ve geri dönüşte kaydırma sıfırlanıyordu; `app/(sheets)/map-picker.tsx` silindi. Alt sayfa rotaları da artık `animation: "slide_from_bottom"` (Android'de `presentation: "modal"` tek başına yandan/soluk geçiş bırakıyordu). Ayrıca haritanın işaretçisi `MapMark` DEĞİL yerinde çizilen sabit ölçülü bir iğne: `MapMark`ın kesikli halkası ve katılımcı noktaları yerel işaretçi görünümünün dışına taşıyor, haritanın üstünde uçuşan daireler çiziyordu (kullanıcı bildirdi, emülatörde doğrulandı); (8) alt sayfaların TEK uygulaması `BottomSheet` organizması (kullanıcı isteği 2026-09-08): yaprak aşağıdan yukarı KAYAR (`translateY`), karartma YERİNDE açılır (`opacity`) — tek parça kaydırılırsa perde de aşağıdan gelir ve katman hissi kaybolur. Yerel `Modal` kullanılmaz (M-5'in O16 kararı genelleştirildi): karartmanın altındaki sayfa görünür kalır. Kapanış animasyonu bitene kadar ağaçta durur; Android geri tuşu ve karartmaya dokunma da kapatır. Silme onayı (O16) ve harita seçici artık aynı bileşeni kullanıyor. `useReduceMotion` `src/lib/`e çıkarıldı, `Segmented` de oradan okuyor; (9) `Segmented` atomunda seçili kabarcık artık yuva başına zemin DEĞİL rayda KAYAN tek parça (kullanıcı isteği 2026-09-08) — `Animated` translateX, 200 ms, yerel sürücü; "hareketi azalt" açıkken anında oturur. Grup/Bireysel, orta nokta/çapa, ulaşım rayı ve mekan sıralaması aynı davranışı alır. Kayma emülatörde süre geçici 2500 ms'ye çıkarılıp ara kareler yakalanarak doğrulandı (adb `screencap` 200 ms'lik bir geçişi yakalayamıyor). UI: P3–P13, O3/O6. |
| M-8 | **Karar akışı** — Deste (P14: kaydırma, damga, haptik, `TravelBars`), Deste bitti/Liste/Gönderildi (P15–P17), Runoff/berabere/Karar (P18–P20; sonuç kartı statik), çevrimdışı/hata (P24/P23, netinfo), Maestro e2e "giriş → katıl → deste → karar" | `2026-09-06-plan42-mobile-decision-flow.md` | Plan 42 | **done** (2026-09-08) | **M-7 ✓** | — | plan38'den bölündü. 5 görev; R-M13. M-7'nin açtığı beş `return null` iskeleti (Deck/Sent/Runoff/Tie/Result) dolduruldu; yönlendirici YENİDEN YAZILMADI (yalnız `view` prop'u geçirildi). Kapılar: mobil **155 test** (38 süit) + `tsc` + `expo lint` temiz, web **663**, i18n tr 640/en 651/nl 651. **Maestro: YAZILDI, KOŞULMADI** — `.maestro/{01-signin-join-deck-decide,02-deeplink,03-location-permission}.yaml` + README; dev build + Türkçe cihaz + hazır oturum ister, ilk koşu sonucu buraya yazılacak (K-M36). Sapmalar: (1) **Runoff seçimi İKİ ADIM** — karta dokunmak seçer, `runoff.lockIn` gönderir; plan42'nin taslak testi tek dokunuşta oy yolluyordu, ama runoff oyu geri alınamaz ve hem artboard hem web CTA'lı (testin niyeti korunarak uyarlandı). (2) Plan test parçacıklarındaki `travelMinutes` → gerçek `travel[]` (K-B26). (3) `SwipeCard` jesti `Gesture.Pan().runOnJS(true)` ile **JS ipliğinde**: shared'ın saf geometrisi worklet değil, UI ipliğinden çağrılamaz; kopya eşik yazmak yerine iplik seçildi. Cihazda takılma görülürse doğru düzeltme `shared/swipeMath.ts`e `"worklet"` yönergesi eklemektir. (4) jest'e `react-native-worklets/jest/resolver.js` resolver'ı eklendi (reanimated import'u `loadUnpackers` ile patlıyordu); reanimated STUB'lanmadı. (5) `ErrorScreen` M-7'de P23'ün tüm dallarıyla yazılmıştı — dokunulmadı. (6) `SentScreen` kişi BAŞINA deste ilerlemesi çizmez: sözleşmede `deckDone` var, "kaç kart kaldı" YOK (alan icat edilmedi) — grup toplamı gösterilir. (7) `VenueRow`a opsiyonel `checked`/`onToggle` eklendi (P15/P16/P17 aynı satırı kullansın diye), `RangeBar`dan `FairnessNote` çıkarıldı (TravelBars ile tek cümle). (8) Yeni i18n: `runoff.tieNote` (3 dil). "Dürt" ÇİZİLMEDİ (K-M4 aday kalır). UI: P14–P20, P23, P24. |
| M-9 | **Mobil v3 cilası** — `nudge`/`sessionByCode`/`joinCode.ts`/`og.ts` shared, presence 2.0 + dürt (`socialStore` 60 sn, `nudged` toast + haptik), mekan kartı 2.0 (`tagline`, saat, `expo-image` foto, FSQ atfı), sonuç kartı görseli (`react-native-view-shot` 1080×1920 + `expo-sharing`), takvim (ICS `shared/src/ics.ts` + saat sayfası; `expo-calendar` yok), oturum kodu + QR (`expo-camera` tarama, `react-native-qrcode-svg`), Live Activity **taslağı** (yalnız `NSSupportsLiveActivities` + no-op arayüz; gerçek uygulama B-16) | `2026-09-06-plan43-mobile-v3-polish.md` | Plan 43 | **done** (2026-09-09) | **M-8 ✓**, **B-15 ✓** | — | R-M9, R-M11, R-M12, R-M17. 8 görev. **Kapılar (hepsi KOŞULDU):** backend **523** (0 hata) · web **685 / 106 dosya** · mobil **259 test / 59 süit** · `tsc --noEmit` ve `expo lint` temiz · i18n tr 662/en 673/nl 673 (parite tam) · `expo config --type prebuild` hatasız · `expo prebuild --clean` + `run:android` BUILD SUCCESSFUL · **iOS prebuild üretildi ve `withLiveActivity` etkisi DOĞRULANDI**: `ios/BumpInto/Info.plist` içinde `NSSupportsLiveActivities = true`, `NSCameraUsageDescription` ve `ITSAppUsesNonExemptEncryption=false` yerinde. Taban çizgisi: web 676→685, mobil 222→259. **CİHAZDA doğrulandı** (emülatör `bumpinto-api35`, yeni dev build): dürt → yeşil `Nudged Ayse` şeridi + 60 sn soğuma (düğme pasifleşiyor) ve sunucu 204; `presence.lastSeen` gerçek veride "Last seen · 11:44 PM"; sonuç kartı `react-native-view-shot` ile **1080×1920 PNG** üretti (cihazdan çekilip ölçüldü) ve `expo-sharing` sayfası kart önizlemesiyle açıldı; `expo-image` gerçek mekan fotoğrafını hem kartta hem sonuç ekranında çizdi; takvim `.ics` cihazda üretildi (CRLF, `VTIMEZONE` yok, `DTSTART` seçilen saatle birebir: 23:00 Amsterdam → `20260909T210000Z`) ve paylaşım sayfası açtı; QR sayfasının kodu ekran görüntüsünden makineyle çözüldü → `https://bumpinto.app/j/<slug>`. **SAPMALAR (plan bayattı, gerekçeleri):** (1) `nudge` + `presence`/`share`/`calendar` i18n ağaçları W-15'te zaten vardı → yalnız eksik anahtarlar ve `code` ağacı eklendi; (2) `socialStore` M-5'ten VARDI (bildir/engelle) → üzerine YAZILMADI, dürt aynı depoya eklendi; (3) dürt bağlantısı üç ekrana değil M-8'in `ParticipantList` organizmasına yapıldı (+ `SentScreen` doğrudan satır kullandığı için ayrıca); (4) satır KİMİN dürtüleceğine karar VERMEZ — lobide konumu bekleyen, P17'de desteyi bitirmeyen (aynı uç, farklı kapı); (5) T3'ün büyük kısmı (meta satırı, `tagline`, `expo-image` foto) M-8'de ZATEN yapılmıştı → yalnız FSQ atfı yeni, ve `Attribution`ın veri-güdümlü `providers: string[]` propu korunarak eşleme `shared/src/attribution.ts`e (`attributionProviders`) kondu; (6) `travel.minOne` yok, mevcut `travel.min` kullanıldı (ikinci anahtar açılmadı); (7) `defaultMeetAt` web'in epoch aritmetiği korundu (yarım saatlik ofsetli bölgelerde doğru), plandaki `setMinutes` taslağı alınmadı; (8) **T7 `expo-widgets` ALINMADI** — revizyon notu öneriyordu ama görevin kapsamı yalnız köprü ve `expo-widgets`in işi widget target'ı ÜRETMEK; yarım yapılandırılmış hâli hiçbir widget üretmeden prebuild'i riske atardı. Plandaki asıl `plugins/withLiveActivity.js` (yalnız `NSSupportsLiveActivities`) yazıldı, `expo-widgets` B-16'ya not edildi; (9) `.maestro/04-code-join.yaml` yerine **`05-code-join.yaml`** (04 sesli sohbete ait). **M-9'un BULDUĞU üç ayrı iş:** K-M39 (mobil katılımcı jetonu yaşam döngüsü — dürt cihazda 403 verdi), K-M40 (`expo-file-system` depolama izinleri), K-M41 (web'de FSQ atfı eksik). |
| M-10 | **Sessiz yenileme — mobil** — `tokenStore` yenileme jetonunu SecureStore'da tutar, `getAccessToken` `exp` YAKLAŞIRKEN (ör. <60 sn) önden yeniler; shared kesicisi mobil adaptörle bağlanır (`refresh()` gövdeyle, çerez yok); çıkışta ve hesap silmede yenileme jetonu da silinir | `2026-09-09-plan45-refresh-token.md` | Plan 45 | **done** (2026-09-09) | **B-16**, W-16 (ortak kesici) | — | K-M38'in düzeltmesi (ölü jetonu at) KORUNUR — emniyet ağı olarak kalır, yerine geçmez. `exp` çözücüsü zaten `tokenStore.ts`te (`atob` KULLANILMAZ — Hermes'te garanti değil). **Cihaz doğrulaması ZORUNLU:** K-M38'i doğuran senaryo birebir tekrarlanır — jeton süresi geçmişken uygulamayı aç, derin linkle oturuma gir, görünüm YÜKLENMELİ (bugün 401 alıyordu). Ayrıca: uygulama arka plandan dönerken tek yenileme, çevrimdışıyken yenileme denenmez.  **UYGULANDI (Plan 45).** `tokenStore` yenileme jetonunu ayrı SecureStore anahtarında tutar; `expiringSoon` (<60 sn) ile önden yenileme, çevrimdışıyken YENİLEME YOK, çıkışta `api.logout(rt)` sunucuda aileyi iptal eder. `atob` KULLANILMADI. **CİHAZ DOĞRULAMASI YAPILDI (emülatör bumpinto-api35, backend 8061 + `adb reverse tcp:8060 tcp:8061`):** (a) K-M38 senaryosu birebir — 60 sn TTL ile giriş, jeton ölene dek bekle, uygulamayı arka plana al, `bumpinto://j/<slug>` derin linki: **Katıl ekranı ve ardından lobi görünümü YÜKLENDİ** (eskiden "Bu oturum bulunamadı"), `refresh_tokens` satırı 1→2 yani yenileme SESSİZ oldu; (b) **tepkisel 401 yolu ayrıca zorlandı** — backend farklı `TOKEN_SECRET` ile yeniden başlatılıp istemcinin TAZE sandığı jeton sunucuya reddettirildi: satır 7→8, ekran yüklendi, çıkış OLMADI; (c) cihaz kullanıcısında **sıfır** `reuse detected` alarmı (tek-uçuşluluk tuttu). Ekran görüntüleri `/tmp/m10-0*.png`. |
| M-11 | **Keşfet — mobil** — `discoverStore` + `app/discover/index.tsx` (P1m/P1b), yeni buluşma formuna açık plan alanları (P3), plan detayı + katılım isteği (P2), host istekleri ekranı (P4) + "Buluştunuz mu?" modalı (P5), profil ilgi alanları | `.github/modernize/java-upgrade/20260902170714/plan.md` §M | plan.md §M | **done** (2026-09-11 gece, inline, M-12 ile birleşik — kilit 23) | **M-4 ✓, M-7 ✓, B-17 ✓** | M1–M5 + kapılar | 5 görev (M1–M5). Plan gövdesi bu izi `M-10` diye anıyor — o numara plan45'te kullanıldı, doğrusu **M-11**. Alt sekme YOK (plan38 "tek stack"). **Kapılar (M-12 ile ortak):** mobil **347 test / 76 süit** (taban 270/62), `tsc` + `expo lint` temiz, web **797 / 117** (shared locales değişti), i18n **tr 818 · en 836 · nl 836** (bu işin anahtarları yalnız `plan.title`, `badge.earned/locked`); `expo install --check` yama sürümü kayması ÖNCEDEN VAR (`package.json`/lockfile değişmedi, kurulum yapılmadı). Görev başına tek inceleme turu (5 tur, Opus): **2 Critical + 10 Important kapatıldı**, 1 Important aday (aşağıda); düzeltme testleri mutasyonla doğrulandı. **Cihaz doğrulaması YAPILMADI** (Maestro kurulu değil) — manuel test kullanıcıda. **Plandan sapmalar:** (1) **M1** giriş Oturumlar üst çubuğunda Pusula `IconButton`; `app/discover/[slug].tsx` YAZILMADI — kart doğrudan `/j/{slug}` (plan detayı orada, K-B37); şehir hapı, host alt satırı ve kartta ulaşım eki çizilmez (API'de yok / 390 artboard'da yok); çip sırası ilk yüklemede donar; sunucunun bu sürümün tanımadığı türü çip olarak çizilmez (ikonu yok, ekran çökerdi); son seçili tür kaldırılamaz; dakika konumu izin verilmişse `locationStore.point`, yoksa profil varsayılanı, 2 ondalık, açılışta izin istenmez; 390 aralık rayı 3'lü; her açılış "Bu hafta"; yeni rotalar için typed routes yerelde yeniden üretildi (`.expo` gitignore'lu). (2) **M2** = M-12 T1+T2 (kilit 23): kapasite `Stepper` (sınırda `accessibilityState.disabled`, `disabled` prop YOK); tarih/saat için MEVCUT `@react-native-community/datetimepicker` (iOS compact, Android diyalog — plan "eklenmez" diyordu, bağımlılık M-9'dan beri var) ve Tarih seç'e girişte boş/geçmiş alan `defaultMeetAt()` ile dolar; Şimdi çapası TAZE konum: `locationStore.refresh()` (izin varsa yeniden okur; okurken ve `asking`'de eski noktayı düşürür; eski yazılı adresi düşürür; yeni açılmış adres modunu korur); `toRequest` try/catch → planın hata anahtarı; SOLO↔plan değişmezi setter'larda; form taze açılışta SIFIRLANIR, O3 ön-ekranı dönüşünde korunur (T2 incelemesi: kalan Şimdi/OPEN/Herkes taslağı tek dokunuşla herkese açık plan yayınlıyordu); `?now=1` / `?open=1&activity=X` (bilinmeyen tür yok sayılır). (3) **M3** plan detayı `PlanIntroScreen`, `/j/[slug]` önizleme `openPlan` taşıyınca çizer; `seatStore` plana bağlı (404 → yok, 409 → kapandı, ağ hatası durumu değiştirmez; 409 gövdeleri düz metin; "host cannot request own plan" → oturuma); OPEN onayında katılımcı jetonu `mine` gövdesinden; PENDING'de 10 sn yoklama (uygulama öndeyken); istek KOORDİNAT gönderir (izinli nokta ya da profil varsayılanı), ETİKET göndermez; hesaplı ziyaretçide `getSession` ön kontrolü (üye olmayana sunucu 403) oturum durumu bilinene dek BEKLER, koltuklu hesap `/s/`'ye geçer (T3 incelemesi); K-B37: `JoinForm` 409 `open_plan_seat_request_required` → önizleme tazelenir, geçilemezse `join.errJoin` (T3); `SignInBlock` molekülü (molecules dizinine BİLEREK eklenmedi); P2'deki paylaş düğmesi YAPILMADI (web'de de yok); çıkışta katılımcı jetonları da silinir (T3 **Critical**: paylaşılan cihazda sonraki kişi öncekinin koltuğuyla odaya giriyordu); dizinsiz bekleyen `Avatar` nötr (host rengini ödünç alıyordu). (4) **M4** P4 ayrı rota `app/sessions/[slug]/requests.tsx` (hesap ister) + Lobi giriş satırı (açık plan + hesap oturumu, bekleyen sayısı); `seatRequestsStore` plana bağlı + `listen(slug, canFetch)` (`liveEvents` kapısı; `app/s/[slug].tsx`'te host + hesap kapısı); onayda haptik Success; `minutes` hiç çizilmez; **check-in plan49'daki `app/(sheets)/check-in.tsx` ROTASI DEĞİL**, `app/s/[slug].tsx`'te her aşama ekranının KARDEŞİ yerinde `CheckinPrompt` (`BottomSheet`) — misafir için anonim rota açmak gerekmedi; işaret SecureStore `bumpinto.checkin.{slug}`, kapatmak işaretlemez; jeton yoksa önce onarım, onarılamazsa tekrar denenebilir; 403'te jeton atılır, onarılır, bir kez yeniden denenir — yalnız 404 ya da onarım sonrası 403 kalıcı (T4 **Critical**: bayat/eksik jeton cevabı sunucuya yazmadan işaretliyordu); rozet farkı yalnız "önce" okunabildiyse (T4); `Button.hint` → `accessibilityHint`. (5) **M5** ilgi alanları tercih alt sayfasında (`field=interests`, çoklu, en çok 5, iyimser, hata → sunucunun onayladığı liste); kilit `meStore.saving`'de, `update` sürüm sayacıyla yalnız en son kaydın yanıtını yazar, kayıt sırasında başlamış `load` bayat sonucu yazmaz, `clear` uçuştaki yanıtları düşürür (T5 incelemesi); iki eski sayaç kartı `BadgeGrid`e yerini bıraktı (spec §11.6; `profile.hosted/friends` mobilde artık kullanılmıyor, anlamları değişmedi). **Aday (T3 incelemesi):** seat-state kuralları web `SeatRequestCard` ↔ mobil `seatStore` kopya ve host-kendi-planı dalında ayrışmış → shared saf fonksiyona taşınmalı; web'e dokunmayı gerektirdiği için bu işte yapılmadı |
| M-12 | **"Buradayım" + kitle + rozetler — mobil** — `newSessionStore.setPlan` (kural shared'da, K-M8), `new.tsx` alanları, Keşfet "Buradayım" + Şimdi + süren kart, `PlanIntroScreen` süren/OPEN, `BadgeGrid` + check-in rozet anı (haptik) | `2026-09-11-plan49-instant-plan-mobile.md` | plan49 | **done** (2026-09-11 gece, inline, M-11 ile birleşik — kilit 23) | **B-18 ✓**, **W-18 T1 ✓** (shared), M-11 M1–M5 ✓ | T1–T5 + kapılar (M-11 satırında) | 5 görev. T1+T2 = M-11 M2, T3 = M1, T4 = M3, T5 = M4 (check-in) + M5 (profil). **Sapmalar (W-18 devri + kod gerçeği; ayrıntı M-11 satırında):** (1) kapasite `Stepper` + `MIN/MAX_CAPACITY` (plan `Segmented`/`CAPACITIES`); (2) sayaçlar `profile.stat*`; `badge.earned/locked` eklendi (tr → en/nl); (3) Şimdi çapası taze konum (`locationStore.refresh`, profil varsayılanı ve bayat `point` çapa olmaz, ANCHOR'da kalmış çapa sızmaz); (4) `toCreateRequest` fırlatır → try/catch; SOLO↔plan setter'da; form taze açılışta sıfırlanır; (5) gerçek yönlendirici `app/s/[slug].tsx`, Keşfet rotası `app/discover/index.tsx`; (6) OPEN koltuk jetonu `mine` gövdesinden; bekleyende 10 sn yoklama, ağ hatası durumu "yok"a çevirmez; (7) 409'lar düz metin; hesapsız kullanıcı hesap uçlarını çağırmaz; check-in öncesi jeton onarımı; (8) `seat_requests_changed` yalnız host + hesap oturumunda tazeler; (9) Keşfet/koltuk/istek depoları plana bağlı, çıkışta/hesap silmede sıfırlanır (+ `meStore`, katılımcı jetonları); (10) K-B37: `/j` 409 → önizleme tazele → plan detayı, önizleme gelmeden form yok; (11) veride olmayan çizilmez (semt/dakika/host alt satırı/şehir); (12) Keşfet "Şimdi" süzgeç, her açılış "Bu hafta", süren damga amber, `plan.safetyOpen`, `meetAtOptions`; (13) rozet anı `meStore.load()` + haptik Success, SecureStore işareti, yalnız cevap işaretler; (14) kişi rengi kanonik dizin, bekleyen nötr; (15) plan49'daki `app/(sheets)/check-in.tsx` rotası YERİNE yerinde `CheckinPrompt`; tarih için mevcut datetimepicker; (16) **K-B40'a DOKUNULMADI** (shared kural aynen). **Cihaz borcu:** emülatör `bumpinto-api35`'te Şimdi formu → Keşfet süren kart → Katıl → host istekleri → check-in rozet anı elle koşulacak; Maestro akışı yazılmadı |

**Spec dışı görevler** — planlama/yürütme sırasında bulundu, spec'te yok. `Plan` kolonu:
`done` için kalemin çıktığı plan, `açık`/`aday` için hedef plan.

| Kimlik | Görev | Durum | Plan | Not |
|---|---|---|---|---|
| K-M1 | "Mekanlar grup 390 host" artboard'ını çiz | done | W-6 | 2026-09-03: `Web Ekranlar v2` içinde `Mekanlar grup 390 host` olarak çizildi (liste-önce, sticky "Karıştır ve kaydır"); mobil dosya M-3'te pariteye çekilir |
| K-M2 | **Mobil harita motoru MapLibre'dir** (Google Maps DEĞİL) | karar | M-7 | 2026-09-08 kullanıcı doğrulaması + kod denetimi. Kaynak karar: açık hibrit spec `2026-09-06-open-hybrid-venue-stack-design.md` §mobil; backend varsayılanı `MAP_ENGINE=maplibre` (`application.yml`), web W-12 ile geçti. plan38/plan41'deki "`react-native-maps` 1.27'de kalındı" notu **bayattı ve düzeltildi**. `react-native-maps` bağımlılıktan ÇIKARILDI (hiç import edilmemişti, yalnız native autolinking'e giriyordu); `app.config.ts`'ten `ios.config.googleMapsApiKey` ve `android.config.googleMaps` **silindi** — anahtar tanımlamak Google Maps SDK'sını pakete sokar ve `PrivacyInfo.xcprivacy` / Play Data safety beyanlarını yalanlar. M-7 `@maplibre/maplibre-react-native` kurar, atıf `attribution.open` |
| K-M3 | M-1/M-2/M-3 UI kaynağını `Mobil Ekranlar v3` + `Mobil Onboarding, İzinler ve Yasal` yap; izin akışı (O3→O4/O5, O7), Hesap ve veriler (O8–O17), edge-to-edge/predictive back, Live Activity (P26) sonraki iz | done | M-4 | M-4'te uygulandı; UI kaynağı v3 dosyaları (O2/P1/P2/P22 bu planda, O3/O6 + P3–P13 M-7'de, O8–O17 M-5'te, P26 sonraki iz). Purpose string'ler O4/O7'den Info.plist'e M-5'te aynen. |
| K-M5 | `SessionView`'da çapa etiketi/koordinatı yok (M-4:T2 sözleşme denetimi); çapalı oturumda yer adı `midpointLabel`'dan, yarıçap `radiusKm`'den okunur | done | M-4 | 2026-09-08 doğrulandı: `anchorLabel`/`anchorLat` sayımı 0. M-7 çapalı oturum satırını bu iki alandan çizer, yeni alan icat etmez. Ayrı alan gerekirse B-16'ya K-B görevi açılır |
| K-M6 | Mobil `Avatar`/`MapMark` kişi rengi `@bumpinto/shared` `personTint`'ten gelir; web `lib/personColor.ts` artık shim | done | M-4 | 2026-09-08 kullanıcı kararı: plan38 T4 `photoTints` diyordu ama 2026-09-08 "bir kişi = bir renk" kuralı sonradan geldi. Palet + kanonik dizin tek dosyada (`shared/src/personColor.ts`); web CSS gradyanını, mobil `expo-linear-gradient`i shim'de üretir. `photoTints` mekan fotoğrafı yer tutucusu olarak KALDI |
| K-M7 | Profil "Varsayılan konum" satırı ve tercih alt sayfasının konum alanı KAPALI çizili — konum seçici (harita + geocode) M-7'de gelince açılır | **done** (2026-09-11) | M-7 | M-4:T8 kararı: sahte ekran icat edilmedi, artboard satırı da silinmedi. "Hesap ve veriler" + "Destek" satırları aynı gerekçeyle kapalı → M-5 **2026-09-11 KAPANDI:** `frontend/mobile/app/profile.tsx` satırı `MapPickerSheet`i açıyor, seçim `meStore.update({ defaultLocation })` ile yazılıyor (K-M46 sonrası); tercih alt sayfasına konum alanı bilinçli EKLENMEDİ, varsayılanı temizleme kapsam dışı. Manuel test kullanıcıda geçti. |
| K-M8 | **Store'lar shared'a TAŞINMAZ; içlerindeki KURAL taşınır.** M-7/M-8 `newSessionStore`/`locationStore`/`deckStore` yazarken doğrulama + geçiş + türetme mantığını saf fonksiyon olarak `frontend/shared`'a koyar, zustand store'u ince sarmalayıcı kalır | karar (M-7/M-8'de uygulandı) | M-7, M-8 | 2026-09-08 kullanıcı sorusu üzerine karar. Gerekçe: (a) web store'larının 8/10'u DOM'a dokunmuyor ama hepsi `../lib/api` **singleton**'ını içe aktarıyor — paylaşım için fabrika/DI şart, ~30-70 satırlık IO orkestrasyonu için bu maliyet karşılamıyor; (b) `authStore` transportu gerçekten farklı (cookie+GIS ↔ bearer+SecureStore+native modül) — asla taşınmaz; (c) v3 mobil akışı web'in portu DEĞİL (P3/P4 ≠ web NewSession), ortak store iki UI'ı sonsuza dek aynı state şekline bağlar. **Kanıt:** kopya `cta()` eşlemesi zaten ayrışmıştı — `RUNOFF` webde `goVenues`, mobilde `goLobby` (plan38 kod parçası bayattı). Kural `shared/src/sessionCta.ts` `sessionCtaKey`'e alındı, iki `SessionCard` de oradan okuyor. Aynı desen: `slugFromInvite` → M-9'da `joinCode.ts` ile shared'a; `voiceMesh` saf kısmı → M-6 |
| K-M9 | **`app/` altına test dosyası KONULMAZ** — expo-router `require.context` regex'i yalnız `+api`/`+html`/`+middleware`'i eler, `.test.tsx` de ROTA olur; `@testing-library/react-native` bundle'a girip Node `console`'unu ister ve uygulama cihazda AÇILMAZ | done | M-4 | 2026-09-08 emülatörde yakalandı (`Android Bundling failed`). Jest/tsc/lint YEŞİLDİ — bu hatayı yalnız uygulamayı çalıştırmak gösterir. plan38 T8 test yolunu `app/sessions/index.test.tsx` diye veriyordu; `src/__tests__/sessions.test.tsx`'e taşındı. Regresyon kapısı: `src/__tests__/routeTree.test.ts` (`app/` altında test dosyası olmadığını doğrular). **M-7/M-8 ekran testlerini `src/__tests__/`e yazar** |
| K-M10 | `app/sessions/index.tsx`'teki `futureRoute()` yardımcısı **M-7 kapanışında SİLİNİR** | **kapandı** | M-7 | `experiments.typedRoutes` var olmayan yolu derleme hatası yapıyor; `/new`, `/j/[slug]`, `/s/[slug]` rotalarını M-7 (plan41 T1/T3/T4) açacak. Yer tutucu ekran icat etmemek için ileri referans tek yerde işaretlendi. M-7 dosyaları ekleyince yardımcı kaldırılır, çağrılar doğrudan tiplenir **2026-09-08 silindi:** `/sessions/new`, `/s/<slug>`, `/j/<slug>` doğrudan tiplendi. `/s/` ön eki bilinçli — bkz. M-7 sapma (1) |
| K-M4 | "Dürt" (`POST …/nudge`) + `lastSeenAt`/`linkOpenedAt` mobil UI'ı — P6/P10/P17'de tasarımda var | **done** (2026-09-09) | M-9 (plan43) T2 | 2026-09-08 M-7'de kayda alındı. **Uç ZATEN VAR** (`NudgeController`, `api.nudge`, `presence.*` anahtarları, web `ParticipantList` kullanıyor) — plandaki "uç yok" notu bayattı. M-7 düğmeyi bilerek ÇİZMEDİ: presence 2.0'ın soğuma sayacı, `nudged` olayı ve haptiği M-9'a ait ve `nudged` üreticisi M-6'nın canlı kanalına bağlı (K-M6); yarım bir dürtme yüzeyi sessizce başarısız olan bir düğme demekti. **M-9 T2'de uygulandı:** P10'da davetliye düğme yerine `presence.hostOnly` satırı (W-15 host kilidiyle tek uygulama); `nudged` üreticisi M-6 indiği için `useSessionLive`e tek satırla bağlandı (K-M6 önerisi kapandı) |
| K-M28 | **Mobilde kendi satırın hep "çevrimdışı" görünüyordu** | kapandı | M-7 | 2026-09-08 emülatörde görüldü: canlı kanal M-6'da açıldığı için sunucu bu istemciyi hiç çevrimiçi görmüyor ve kullanıcı Lobi'de KENDİ satırını %55 soluk, "· çevrimdışı" etiketli buluyordu. `ParticipantRow` artık kendi satırında presence'ı HİÇ çizmiyor (nokta, etiket, soluklaştırma) — ekrana bakan kişiye "çevrimdışısın" demek zaten anlamsız. Başkalarının presence'ı sunucudan gelmeye devam ediyor. Regresyon testi var |
| K-M29 | **RNTL 14: test başına TEK `render`** — ikincisi SONRAKİ testlerin `screen`'ini bozuyor | kapandı | M-7 | 2026-09-08 bulundu (K-M17'nin devamı). Bir test içinde ikinci bir `await render(...)` çağrılırsa o testten sonraki tüm testlerde sorgular boş ağaca bakıyor ve üretim kodu doğruyken "Unable to find…" veriyor; `unmount()` KURTARMIYOR. M-5'in `participantRow.test.tsx`'i bu deseni SON testinde kullandığı için sorun görünmüyordu — M-7'de dosyaya test eklenince beş test birden kırıldı. Kural: aynı bileşenin başka durumu için AYRI `test(...)`. **Hata mesajı suçluyu değil SONRAKİ testi gösterir** |
| K-M30 | `jest-expo` `AppState.currentState`i `jest.fn()` bırakıyor — "uygulama önde mi" kapısı testte hep kapalı | kapandı | M-7 | 2026-09-08: `useSessionLive` canlı sorguyu ön plan kapısına bağlıyor; ikiz düzeltilmeden yönlendirici testleri üretim kodu doğruyken boş ekran görüyordu. `jest.setup.ts` artık cihazın normal hâlini taklit ediyor (`currentState = "active"`, `addEventListener` → `{remove}`). **Ön/arka plan GEÇİŞİ burada doğrulanmış sayılmaz** — gerçek geçiş Maestro'da (M-8:T5) |
| K-M31 | MapLibre kurulumu Play beyanlarını BOZMUYOR — APK'da doğrulandı | done | M-7 | 2026-09-08 `aapt2 dump permissions app-debug.apk`: `ACCESS_FINE_LOCATION` + `RECORD_AUDIO` duruyor, `ACCESS_BACKGROUND_LOCATION` / `ACCESS_COARSE_LOCATION` / `CAMERA` YOK. `@maplibre/maplibre-react-native` kendi manifestinde COARSE istiyor ama `blockedPermissions` onu `tools:node="remove"` ile eliyor. Döşemeler ANAHTARSIZ (`/api/config.tiles.styleUrl` → OpenFreeMap); emülatörde Eindhoven haritası ve `attribution.open` satırı gözle doğrulandı |
| K-M32 | `join.whoCopy` İngilizcesi tek isimde "Mehmet are ready" diyor | **done** (2026-09-11) | W/M ortak | 2026-09-08 mobil P8'de görüldü ama metin PAYLAŞILAN (`shared/src/i18n/locales/en.json`), web Katıl ekranı da aynısını basıyor. Çoğul biçim gerekiyor (`whoCopy_one`/`whoCopy_other`) — i18n paritesi CLDR eklerini zaten tanıyor. Tek taraflı değiştirilmedi: kopya kararı kullanıcının **2026-09-11 KAPANDI:** `shared/src/i18n/locales/{en,nl}.json` `join.whoCopy_one`/`_other` (tr eksiz), `WhoIsHere.tsx` ve mobil `app/j/[slug].tsx` `count` geçiyor. Manuel test (EN/NL tek kişi) kullanıcıda geçti. |
| K-M33 | **Çıkışsız ekran: `router.back()` geçmiş yokken ÖLÜ** | kapandı | M-7 | 2026-09-08 kullanıcı bildirdi: dil alt sayfasında geri düğmesi hiçbir şey yapmıyor, ekranda kilitli kalınıyor (`The action 'GO_BACK' was not handled by any navigator`). Sebep: bir ekrana geçmiş yığını OLMADAN girilebiliyor — derin link, Android'in arka plandaki uygulamayı öldürüp URL'i geri yüklemesi, geliştirmede Metro yeniden yüklemesi (`dumpsys`: `flg=0x10018000`, MainActivity yeniden kuruluyor ve expo-router son URL'i restore ediyor). 13 çağrı yerinin HEPSİ bu tuzaktaydı. Çözüm: `src/lib/nav.ts` → `goBackOr(fallback)` — `router.canGoBack()` false ise mantıksal ebeveyne `replace`. Yedekler: alt sayfalar → `/profile` / `/s/<slug>` / `/sessions`, yasal okuyucu → `/` (anonim açılıyor). **Statik kapı** `src/__tests__/backExit.test.ts`: `app/` altında çıplak `router.back()` kalırsa test kırılır (jest/tsc bunu göremez — kod derleniyor, yalnız çalışma anında ölü). Emülatörde birebir yeniden üretilip doğrulandı: geçmişsiz dil sayfasından geri artık Profil'e düşüyor. **Açık alternatif:** expo-router `unstable_settings.anchor` ile her derin linkli rotaya ebeveyn vermek — denenmedi, `/j/` misafir akışını etkileyebilir |
| K-M34 | **Çift dokunuş aynı eylemi İKİ KEZ tetikliyordu** | kapandı | M-7 | 2026-09-08 kullanıcı bildirdi: bir düğmeye hızlı iki kez dokununca aynı ekran iki kez açılıyor, geri için iki kez basmak gerekiyordu. `onPress` her dokunuşta ateşlenir, gezinme ASENKRONDUR — ikinci dokunuş ilk `router.push` yığına işlenmeden geçer; yazma uçlarında (iki `createSession`, iki `join`) daha pahalı. Çözüm `src/lib/useOncePress.ts`: ÖRNEK BAŞINA 600 ms kilit, `Button` / `IconButton` / `SettingsRow` atomlarında + gezinen çıplak `Pressable`larda (`sessions/index` avatarı, `LocationField`). Kilit GENEL DEĞİL: farklı düğmeler birbirini engellemez ve `await` sonrası PROGRAMATİK gezinmeler hiç etkilenmez — genel kilit onları sessizce yutardı. `Chip`/`Segmented` hariç (arka arkaya hızlı seçim meşru). Karar saf `shouldAcceptPress`e ayrıldı: `Date.now`u taklit etmek RNTL ortamını bozuyor. Emülatörde doğrulandı: çift dokunuş → lobi, TEK geri → liste |
| K-M35 | **Android geri tuşu UYGULAMA GENELİNDE ölüydü** — her ekrandan uygulamayı kapatıyordu | kapandı | M-7 | 2026-09-08, K-M34'ü ölçerken bulundu; **M-4'ten beri var, hiç sınanmamış**. `app.config.ts`'teki `predictiveBackGestureEnabled: true` manifeste `android:enableOnBackInvokedCallback="true"` yazıyor; Android o zaman eski `Activity.onBackPressed()` yolunu KAPATIP uygulamadan `OnBackInvokedCallback` bekliyor. Bu sürümlerde kaydeden YOK — `react-native-screens` 4.26 ve RN 0.86 ReactAndroid kaynaklarında `OnBackInvoked` hiç geçmiyor — dolayısıyla olay JS'e ulaşmıyor ve sistem varsayılanı activity'yi bitiriyor. Hem `KEYCODE_BACK` hem GERÇEK kenar hareketiyle ölçüldü (adb artefaktı değil). Bayrak `false` yapıldı, `prebuild --clean` + yeniden derleme sonrası geri tuşu yığını popluyor. APK izinleri yeniden doğrulandı (K-M20): `ACCESS_FINE_LOCATION` + `RECORD_AUDIO`, engellenen üçü yok. **Predictive back ancak kütüphaneler geri çağrıyı kaydedince yeniden açılabilir** — I-3 yayın kapısında yeniden değerlendirilmeli |
| K-M11 | Yerel mobil koşu ortamı: Android SDK paketleri ve iOS imza | kısmen | I-3 | 2026-09-08 kuruldu: `platforms;android-36` + `build-tools;36.0.0` + `system-images;android-35;google_apis_playstore;arm64-v8a` (sürümler RN 0.86 `gradle/libs.versions.toml`'den: minSdk 24, target/compileSdk 36, buildTools 36.0.0, ndk 27.1.12297006 — ndk zaten kuruluydu). **Android doğrulandı:** `BUILD SUCCESSFUL`, APK emülatöre kuruldu, `Android Bundled`, O2 ekranı çizildi. **iOS KOŞMUYOR — iki ayrı engel (2026-09-08 ölçüldü):** (1) **Ücretsiz Apple hesabı yetmez.** `associatedDomains` (Universal Links) `associated-domains` entitlement'ı üretiyor; Xcode "Personal development teams … do not support the Associated Domains capability" diyor. Sign in with Apple (R-M1, M-5) da aynı listede → **Apple Developer Program (ücretli) M-5 için zaten şart.** Yerel geliştirme çıkışı eklendi: `app.config.ts`'te `BUMPINTO_DEV_NO_APPLINKS=1` entitlement'ı düşürür (varsayılan AÇIK; `bumpinto://` şeması kalır, yalnız https açılışı gider). (2) **Xcode 26.1.1 çok eski.** Bayrakla imza kapısı geçildi ama `expo-modules-jsi` (57.0.8 VE 57.1.0) Swift 6.2.1'de derlenmiyor: 13× `'weak' must be a mutable variable` (`weak let`, SE-0481) + 2× `SWIFT_RETURNS_RETAINED` C++ interop hatası. Yerel toolchain'de tek satırlık `weak let` denemesiyle doğrulandı: hiçbir dil kipinde ve `-enable-upcoming-feature WeakLet` ile de derlenmiyor. Paket yükseltmesi çözmüyor → **Xcode güncellemesi gerekiyor (26.2+).** I-3 CI'sinin macOS runner'ı da bu sürümü tutturmalı. **Android tamamen yeşil** |
| K-M12 | **P1/P2/P22 cihazda hiç çizilmedi** — yalnız O2 (Giriş) emülatörde doğrulandı | açık | M-7 | 2026-09-08: `frontend/mobile/.env` yok → `googleWebClientId` undefined → giriş tamamlanamıyor → Oturumlar/Profil'e ulaşılamıyor. Google OAuth istemci kimlikleri plan38 "Ön koşul"unda KULLANICIDA. Derin linkle atlatma denendi, `bumpinto://sessions` dev-launcher'a takılıyor (expo-dev-client şemayı kendi yakalıyor). Bu ekranlar jest'te GERÇEK bileşenlerle render ediliyor (2 test) — doğrulanmayan yalnız yerel çizim (font, LinearGradient, yerleşim); O2 aynı atomlarla doğru çizdiği için risk düşük. **`.env` tuzağı:** emülatörde `localhost:8060` Mac'teki backend'e ulaşmaz → `EXPO_PUBLIC_API_URL=http://10.0.2.2:8060` (emülatör) ya da LAN IP (fiziksel cihaz). M-7 ilk koşusunda kapanır **2026-09-08 genişledi:** M-5'in O3/O7/O8/O9–O14/O15–O17/O18–O19 ekranları da yalnız jest'te doğrulandı; cihaz koşusu `.maestro/store-compliance.yaml` ile yapılır. |
| K-M13 | plan39 (M-5) sözleşme tablosu `BlockListResponse` diyor ama codegen'de o ad YOK — `GET /api/me/blocks` doğrudan `BlockDto[]` dönüyor | kapandı | M-5:T1 | 2026-09-08 M-5 ön koşul denetiminde bulundu. Diğer 8 ad doğrulandı (`AppleLoginRequest`, `ConsentsDto`, `UpdateConsentsRequest`, `ReportRequest`, `BlockRequest`, `BlockDto`, `/api/me/blocks`, `ParticipantDto.blocked`, `authProviders`). **Zararsız sapma:** sarmalayıcı tip gereksiz, istemci yanıtı `BlockDto[]` olarak tipler — YENİ TİP YAZILMAZ. Plan "ad uyuşmazlığı = duruş noktası" diyor; karar: bu tek ad için durulmaz, kayıt yeter. B-16'da sarmalayıcı eklenirse istemci tarafı tek satırda döner **2026-09-08 kapandı:** `api.listBlocks()` codegen'in `BlockDto[]` şeklini kullanıyor, sarmalayıcı tip yazılmadı. |
| K-M14 | `extra.exportEnabled` bayrağını `true` yap, "Verilerimi indir" satırını aç | açık | B-15 sonrası | `GET /api/me/export` B-15'te (R-B6). İstemci tarafı M-5:T6'da yazıldı ve satır bayrak kapalıyken `disabled`; yalnız bayrak çevrilir + indirme/paylaşma bağlanır. Artboard notu "JSON · e-postana gelir", i18n `account.exportHint` ise "tarayıcına iner" (web kopyası) — mobil metin bayrakla birlikte düzeltilir **2026-09-09 TRIYAJ DUZELTMESI: bu bir bayrak cevirme DEGIL.** `GET /api/me/export` artik var (B-15, K-W27) ama mobil satirin `onPress`i bugun `() => undefined` — indirme/paylasma HIC yazilmadi (`expo-file-system` + paylasim sayfasi gerekir). Ustelik kopya kararsiz: artboard "JSON · e-postana gelir", i18n `account.exportHint` "tarayicina iner" (web kopyasi), mobilde ise dogru davranis PAYLASIM SAYFASI. Kopya karari kullanicinin -> **KARAR kovasi**, kod kovasi degil |
| K-M15 | Play kapalı test kanalını başlat (12 tester × 14 gün) + tacir bilgisini kesinleştir | açık | M-5 | Kritik yol riski (gereksinim dok. §6/3). `TRADER_PLACEHOLDER` (`src/content/legal/index.ts`) **yayın kapısıdır** ve TODO taramasının bilinçli istisnasıdır — `docs/store/RELEASE-CHECKLIST.md` C bölümü izler |
| K-M16 | Analitik sağlayıcı seçimi (Clarity/GA4) ve `setProviderLoader` bağlanması | aday | M-7 | `src/lib/analytics.ts` kapısı hazır ve testli: rıza `false` iken sağlayıcı **hiç `require` edilmez**, `track()` yalnız allowlist alanlarını taşır. Sağlayıcı gelince tek satır — ve o satır `track()` gövdesinde, kapının İÇİNDE kalır |
| K-M17 | RNTL 14 + React 19: `fireEvent` durum güncellemesini bir sonraki iddiadan önce boşaltmıyor | kapandı | M-5:T8–T10 | 2026-09-08 sahada bulundu; çıplak `TextInput` ile de yeniden üretildi (atomlarla ilgisi yok). `Switch`'te ayrıca `onValueChange` yerel düğümde YOK. Çözüm: `src/testUtils/interact.ts` (`tap` / `typeText` / `toggleSwitch`) — hepsi `act` sarmalı, öğeyi her çağrıda yeniden sorgular. **Yeni ekran testleri bu yardımcıları kullanmalı**, yoksa test üretim kodu doğruyken kırılır ya da yanlış yönde yeşil kalır |
| K-M18 | Yasal içerik `frontend/web/src/content/legal/` → `frontend/shared/src/content/legal/` taşındı | kapandı | M-5:T7 | Mobil okuyucu ve web sayfası artık TEK kaynaktan okuyor: mağazaya verilen URL ile uygulama içi metin ayrışamaz. `LegalBlock` tipi de shared'da; web `LegalBlocks` onu yeniden dışa aktarıyor. Yan etki: plan39'un "v1 yalnız TR" kısıtı geçersiz — okuyucu üç dilde |
| K-M19 | Google OAuth: platform başına AYRI client + iOS URL şeması | açık | M-5 sonrası | 2026-09-08: iOS eklentisi seçeneksizdi → Firebase yoluna düşüp URL şemasını hiç yazmıyordu (giriş tarayıcıdan dönemez, prebuild hata da vermez). Düzeltildi: `EXPO_PUBLIC_GOOGLE_IOS_URL_SCHEME`, `EAS_BUILD_PROFILE=production`'da eksikse **derleme patlar**. Backend TEK client bilir (web, `aud`); iOS ve Android client'ları koda girmez. **Android'de her imza anahtarı için ayrı client**: yerel debug SHA-1 `5E:8F:16:06:2E:A3:CD:2C:4A:0D:54:78:76:BA:A6:F3:8C:AB:F6:25`, EAS keystore, Play App Signing — sonuncusu ancak yayından sonra alınır, unutulursa mağaza sürümünde `DEVELOPER_ERROR` |
| K-M20 | `expo run:android` var olan `android/` dizinini BAYAT bırakabiliyor — manifest app.config.ts ile ayrışıyor | kapandı | M-5 doğrulaması | 2026-09-08 emülatörde yakalandı: kurulu APK'da `RECORD_AUDIO` **yoktu** — mikrofon izni sistem diyaloğu hiç açılmadan reddedilirdi ve M-6 sesli sohbeti sessizce ölürdü. Sebep: `android/` M-4 zamanından kalmıştı, `expo-audio` ve yeni izin listesi hiç uygulanmamıştı. Çözüm: izin/beyan değişikliğinden sonra `expo prebuild --clean --platform android` ŞART. Temiz derleme doğrulandı — APK'da **tam olarak iki izin** var: `ACCESS_FINE_LOCATION` + `RECORD_AUDIO`; `blockedPermissions` (`CAMERA`, `ACCESS_BACKGROUND_LOCATION`, `ACCESS_COARSE_LOCATION`) sorunsuz çalışıyor. **Doğrulama APK'dan yapılır: `aapt2 dump permissions <apk>` — `adb shell dumpsys package` KULLANILMAZ, sistemin türetilmiş görünümünü basar (FINE isteyen uygulamada COARSE'u da listeler) ve yanlış alarm verir** |
| K-M21 | `del.confirmWord` mobilde SABİT KODLANMIŞTI ("SİL") | kapandı | M-5:T9 | 2026-09-08 emülatörde görüldü: İngilizce arayüzde "Type SİL to confirm" yazıyordu. Web `t("del.confirmWord")` kullanıyor (SİL / DELETE / VERWIJDER); mobil de aynı yola geçti, karşılaştırma `toLocaleUpperCase("tr")` ile yerele duyarlı kaldı. Cihazda "Type DELETE" olarak doğrulandı, küçük harf `delete` de düğmeyi açıyor |
| K-M22 | `permissions.ts` `canAskAgain` ÖN ELEMESİ sistem diyaloğunu ilk seferde tamamen engelliyordu | kapandı | M-5:T3/T4 | 2026-09-08 emülatörde yakalandı — **birim testler yanlış davranışı doğruluyordu**. Android'de `canAskAgain`, `shouldShowRequestPermissionRationale()`'a düşer ve izin HİÇ istenmemişken de `false` olur; `get*` sonucuna bakıp `blocked` dönmek ön-ekrandaki "Devam et"i işlevsiz bırakıyordu (mikrofon ve konum, ikisi de). Düzeltme: sadece `granted` için erken dönülür, kalan karar `request*`'a bırakılır — kalıcı reddi o zaten diyalog açmadan `canAskAgain: false` ile bildirir. Cihazda doğrulandı: ön-ekran → sistem diyaloğu → ret → uygulamaya temiz dönüş, mikrofon ve konum için ayrı ayrı |
| K-M23 | Oturum muhafızı yoktu — hesap ekranları ANONİM açılabiliyordu | kapandı | M-5 | 2026-09-08 kullanıcı bildirdi (cihaz koşusunda derin linkle `/account/consent` ve `/account/delete` girişsiz açılıyordu). `app/_layout.tsx` içine `AuthGuard`: durum `out` ve yol anonim değilse `/`'a atar. `restore()` KÖKE taşındı (giriş ekranından alındı) — derin linkle doğrudan `/account`'a gelindiğinde giriş ekranı hiç kurulmuyor, durum `unknown`ta kalıyor ve muhafız çalışmıyordu. `unknown`/`busy` sırasında yönlendirme YOK (token okunmadan atmak oturumlu kullanıcıyı düşürürdü). `/` anonim listeye TAM EŞLEŞME olarak eklendi — ön ek olsaydı her yol anonim olurdu. 6 test (`authGuard.test.tsx`), biri uçtan uca. Cihazda doğrulandı (`bumpinto-api35`): `/account`, `/account/consent`, `/account/delete` derin linkleri anonimken giriş ekranına düşüyor. **Yazarken bir regresyon çıktı ve düzeltildi:** muhafız izin ön-ekranlarını da kapatmıştı — `/location-consent` ve `/mic-consent` hesap yüzeyi DEĞİL, yetenek sorusudur ve davet linkiyle gelen misafirin (M-7) de görmesi gerekir, anonim listeye alındılar. `/participant` ve `/prefs` bilerek dışarıda: ikisi de `/api/me/*`'e yazar |
| K-M24 | Kullanıcıya giden metinlerden teknoloji/sağlayıcı adları çıkarıldı | kapandı | M-5 | 2026-09-08 kullanıcı kararı. Çıkanlar: `permission.micBody` "(WebRTC)", `consent.analyticsHint` "(Clarity/GA4)" — sağlayıcı **henüz seçilmedi** (K-M16), adı yazmak zaten yanlıştı —, `support.a3` ve `privacy.ts` ses satırındaki "(P2P)" jargonu; üç dilde. **KALANLAR BİLİNÇLİ ve yasal zorunluluktur, silinmez:** (a) `attribution.*` — Google Maps Platform Ek Hizmet Şartları, "Powered by Foursquare" ve ODbL 1.0 "© OpenStreetMap contributors" atıf yükümlülüğü; kaldırmak lisans ihlali + mağaza reddi. (b) `privacy.ts` / `dataRights.ts` içinde Google Places / Foursquare / OpenStreetMap-Nominatim adları — GDPR m.13(1)(e)-(f) ve KVKK m.10 verinin AKTARILDIĞI tarafları ve yurt dışı aktarımı açıklamayı zorunlu kılar |
| K-M25 | `pnpm dev:mobile android` cihaz yoksa hata verip çıkıyordu | kapandı | araç | 2026-09-08: script artık projenin ihtiyacına göre AVD kuruyor — **`bumpinto-api35`** (`pixel_8`, API 35, `google_apis_playstore`, mimariye göre arm64-v8a/x86_64). `google_apis_playstore` ZORUNLU: Google girişi Play Services ister, sade `default` imajda `GoogleSignin` çalışmaz. `pixel_8` (1080×2400) mağaza telefon ekran görüntüsü ölçüsüne (1080×2340) en yakın profil. Sistem imajı yoksa ~1.5 GB indirme için İZİN İSTENİR; etkileşimsiz kabukta indirme başlatılmaz, komut yazdırılır. Aynı koşuda iki hata daha: (a) `const AVD` üst düzey `await` çağrısından SONRA tanımlıydı → TDZ `ReferenceError`; (b) açık emülatörün AVD adı MODEL adıyla (`sdk_gphone64_arm64`) karşılaştırıldığı için tekilleştirme hiç çalışmıyordu, aynı emülatör listede iki kez çıkıyordu — ad artık `adb -s <serial> emu avd name` ile sorulur (c) `avdmanager` cihaz profilinden üretirken `hw.keyboard = no` yazıyor — Mac klavyesi emülatöre HİÇ yazamıyordu, yalnız ekran klavyesi çalışıyordu (oturum adı, adres, "SİL" onayı gibi her metin girişi elle tıklamayı gerektirirdi). Script artık oluşturmadan sonra `config.ini`'de `hw.keyboard = yes` yapıyor (`tuneAvdConfig`; dosya yeniden yazılmaz, yalnız bilinen anahtar değiştirilir). Ayar AÇILIŞTA okunur — mevcut bir AVD'de değiştirdiysen emülatörü yeniden başlat. Doğrulama: `adb shell dumpsys input` çıktısında `qwerty2 (built-in keyboard)` satırı `ALPHAKEY` sınıfıyla görünmeli **Yeni AVD'de Google girişi ilk denemede başarısız olur: cihazda hiç Google hesabı yoktur** (logcat'te `SignInHubActivity` → `AccountPickerActivity` → `PreAddAccountActivity` zinciri). Ayarlar'dan hesap eklendikten sonra çalışır — bu bir SHA-1 ya da backend adresi sorunu DEĞİLDİR, teşhiste ikisi de yanlış yönlendirir. 2026-09-08 uçtan uca doğrulandı: giriş → `api.me()` → profil gerçek sunucu verisiyle (tercihler dahil) çizildi |
| K-M26 | P2 Oturumlar: "Yeni buluşma" birincil eylemi KAYDIRMA İÇİNDEYDİ | kapandı | M-4 ekranı | 2026-09-08 kullanıcı cihazda gördü: geçmiş buluşmalar uzadıkça birincil eylem ekranın dışına düşüyor, kullanıcı en alta kaydırmadan göremiyordu. Artboard P2 `.cta`'yı `.scroll`un KARDEŞİ olarak çiziyor (sabit alt çubuk + üstünde `.fade` gradyanı + `ph-plus` ikonu) — kod üçünü de atlamıştı. Düzeltildi; boş durumdaki kopya düğme de kaldırıldı (artboard'da orada yok, sabit çubukta). Alt güvenli alan payı eklendi, jest çubuğunun altında kalmıyor. **Testler bunu GÖRMÜYORDU** — iki regresyon testi eklendi: düğme ScrollView atalarında olmamalı ve boş durumda TEK kez çizilmeli. Aynı deseni kullanan diğer M ekranları M-7/M-8'de yazılırken kontrol edilmeli **Aynı ekranda ÜST çubuk da kaydırma içindeydi** (kullanıcı fark etti): artboard P2 çerçevesi `.sb` / `.top` / `.scroll` / `.cta` — üçü sabit, YALNIZ ortası kayar. `.top` de sabit çubuğa taşındı, marka ve profil avatarı her an erişilebilir. Regresyon testi ikisini birden korur (`insideScrollView` yardımcısı). Ayrıca P22 Profil'de gezinme satırları değer yuvasında "Belirlenmedi" gösteriyordu (ayar taşıyormuş izlenimi) — `Row`'a `nav` bayrağı eklendi |
| K-M27 | P1/P22 artboard paritesi — beş sapma daha | kapandı | M-4 ekranları | 2026-09-08 kullanıcı cihazda tek tek gösterdi. (1) `<h1>Nereye gidiyoruz?</h1>` HİÇ çizilmiyordu; `sessions.title` anahtarı vardı ama kullanılmıyordu. (2) Wordmark'ın gradyan iğnesi (`.wm-p`) yoktu — web `Wordmark` atomunun mobil karşılığı yazıldı. (3) Kart başlıkları BOŞTU: `name` boşken boş bir `h2`/`h3` çiziliyor, satır yüksekliği kadar boşluk bırakıp rozeti hizasız gösteriyordu. Web'in çözümü alındı — başlık `name || activityListLabel(...)`; fonksiyon `frontend/shared/src/activityLabel.ts`e taşındı, web kendi lib'inden yeniden dışa aktarıyor (tek kaynak). **`Intl.ListFormat` Hermes'te YOK** — cihazda doğrulandı, nokta ayracına düşen yedek devreye giriyor. (4) Geçmiş satırları kağıt üstünde serbestti, artboard'daki gibi tek KART + 16px içeriden ayırıcıya alındı ve satıra yatay iç boşluk verildi. (5) `Badge`'de ikon metnin İÇİNDEYDİ, satır ekseni kayıyordu — `icon` yuvası eklendi (ikon artık metnin kardeşi) |
| K-M36 | Maestro e2e akışları YAZILDI ama hiç KOŞULMADI | açık | M-8 | 2026-09-08 M-8:T5. `frontend/mobile/.maestro/` içindeki üç akış (ana akış, derin link, konum izni) gerçek dizelerle yazıldı ve README kurulum + örnek oturum `curl`'lerini taşıyor, ama koşmak için dev build + Türkçe cihaz + hazır bir oturum gerekiyor; bu oturumda cihaz yoktu. **Derin link ve izin diyaloğu hâlâ DOĞRULANMAMIŞ yapıştırıcıdır** (untested-seam kuralı): Jest ikizleri bu yolları sınamıyor. İlk koşuda seçiciler OEM izin diyaloğu metnine göre düzeltilebilir; sonuç M-8 satırına yazılır. |
| K-M39 | **Mobil katılımcı jetonu yaşam döngüsü:** `participantTokens` YALNIZ bellekte ve yalnız oturum KURARKEN / KATILIRKEN yazılıyor. Listeden açılan (ya da uygulama yeniden başladıktan sonraki) bir oturumda jeton YOK; katılımcı yetkisi isteyen her YAZMA ucu 403 alıyor | **done** (2026-09-09) | M-7/M-8 izi (M-9 dışı) | 2026-09-09 M-9 cihaz doğrulamasında bulundu ve SUNUCUDAN kanıtlandı: `ForbiddenException: participant token required`. Dürt bunu ilk gösteren yüzey, ama aynı boşluk `swipe`/`deck-done`/`location`/`force-decision`/`runoff-vote`/`voice` için de geçerli. Backend'in kendi Javadoc'u yolu söylüyor (`ParticipantTokenDelivery#refresh`): **web çerezi yeniden yazar, mobil KATILIM ucuyla onarır** — `app/s/[slug].tsx` bu onarımı hiç çağırmıyor. Doğru düzeltme: jetonsuz açılan oturumda hesap kimliğiyle `api.join` çağırıp aynı koltuğu + taze jetonu almak (ve jetonu SecureStore'a yazmayı değerlendirmek). M-9 KAPSAMI DIŞI: token yaşam döngüsü M-7/M-8'in işi ve otomatik yeniden katılımın koltuk/ad/konum etkisi ayrıca tasarlanmalı **2026-09-09 KAPANDI ve CIHAZDA KANITLANDI.** Uc parca: (1) `frontend/mobile/src/lib/participantSession.ts` `repairParticipantToken(slug, view)` — jeton YOKSA ve `view.viewer` VARSA (sunucu bizi hesap koltugundan taniyor) `api.join(slug, {displayName})` cagrilir, taze jeton hatirlanir; kendi hatasini YUTAR (cagiran `loadView`, onarimin 500'u "oturum bulunamadi" ekranina dusmemeli). Iki kapi zorunlu: jeton varsa cagrilmaz (30 sn'lik poll her tur bir katilim istegi atardi), `viewer` yoksa cagrilmaz (anonim katilim HAYALET koltuk acardi). (2) `sessionStore.loadView` her okumadan sonra onarimi calistirir. (3) `useSessionLive` kanali ILK okumadan SONRA aciyor — WS el sikismasi (`SessionWsHandshake`) de katilimci jetonu ister; siralanmazsa handshake reddedilir ve kanal 5 sn'lik yeniden baglanmayi beklerdi. **SOLO icin backend'de kapi sirasi degisti (K-B24).** **CIHAZ KANITI** (bumpinto-api35, kendi backend'im :8061, `adb reverse tcp:8060 tcp:8061`, kullanicinin IDE JVM'i :8060'ta dokunulmadi; oturum `s44oxa6l`, GROUP/8 kisi, LISTEDEN acildi, her turda `am force-stop`): ONCE (onarim gecici devre disi) `POST .../nudge/... -> 403 FORBIDDEN`, katilim ucuna hic gidilmedi. SONRA `GET .../s44oxa6l -> 200`, `POST .../participants -> 201 CREATED` (onarim), `GET .../ws -> 101 SWITCHING_PROTOCOLS`, `POST .../nudge/... -> 204 NO_CONTENT`; **403 YOK**. Yan etki yok: katilimci sayisi 8 -> 8 (hayalet koltuk acilmadi), kendi satiri "'s-Hertogenbosch · ~25 min · Ready" degismedi, "Nudge Smoke" 60 sn sogumasina girdi (zil gercekten caldi). **Kapsam disi kalan iki kalem:** (a) jeton VAR ama 24 sa TTL'i dolduysa onarim tetiklenmez — 403'ten tetiklenen onarim paylasilan `http` kesicisini degistirir (web'i de etkiler); (b) `app/j/[slug].tsx` jetonu olmayan uyeye hala katilim FORMUNU gosteriyor (form gonderilince koltuk kurtariliyor, yani calisiyor ama gereksiz bir adim). Testler: `mobile/src/lib/participantSession.test.ts` (4), `mobile/src/store/sessionStore.test.ts` (3), `useSessionLive.test.tsx` sira testi. Mobil 269 test / 61 suit + tsc + lint temiz |
| K-M40 | `expo-file-system` READ/WRITE_EXTERNAL_STORAGE izinlerini (`maxSdkVersion=32`) manifeste devrediyor | **kapandı** (2026-09-09) | M-9 T7 | Yalnız uygulamanın KENDİ önbelleğine yazıyoruz (kart PNG'si, `.ics`) — hiçbir API düzeyinde izin gerekmiyor. İkisi de `blockedPermissions`a alındı ve `appConfig.test.ts` bekçisine yazıldı; üretilen manifestte `tools:node="remove"` ile doğrulandı. Play "Data safety" formu kapalı listeyle doldurulduğu için sessizce sızan izin kabul edilemezdi |
| K-M41 | **Web'de FSQ tagline atfı eksik** — web `VenueMeta`/`VenueCard`/`WinnerCard` `tagline` basıyor ama `Attribution`a yalnız `v.provider` veriyor; FSQ tips'ten türeyen metin Foursquare atfı ister (GUIDE kural 8) | **done** (2026-09-09) | W izi (W-17 ya da bakım) | 2026-09-09 M-9 T3'te fark edildi. Mobil tarafı kapatıldı; eşleme zaten PAYLAŞILAN'da (`shared/src/attribution.ts` `attributionProviders`) — web'de yapılacak iş her `<Attribution providers={…}>` çağrısını bu yardımcıya çevirmek (6 çağrı yeri). M-9 mobil planı olduğu için web izine dokunulmadı (kapsam genişletmesi); **lisans yükümlülüğü olduğundan ertelenmemeli** **2026-09-09 KAPANDI:** web'deki 6 `<Attribution>` cagri yeri paylasilan `attributionProviders`a cevrildi — `VenueCard`, `ResultActions`, `RunoffList` (2 basim, tek turetme), `VenueBrowser`, `DeckScreen`. Ikinci uygulama SILINDI: `web/src/lib/provider.ts` (`providerIds`, yalniz `provider` okuyordu, `taglineSource`i hic gormuyordu) kaldirildi. Regresyon: `VenueCard.test.tsx` "FSQ kaynakli tagline Foursquare atfini da getirir" (cagri yeri geri alinarak KIRMIZI dogrulandi) + `VenueBrowser.test.tsx` "FSQ kaynakli tagline liste atfina Foursquare'i ekler". Web 692 test yesil. **Test edilmeyen 3 cagri yeri** (ResultActions/RunoffList/DeckScreen) ayni tek satirlik degisiklik; `tsc -b` ve mevcut suitler yesil, ama kendi FSQ testleri yok — ResultScreen testi config seed etmedigi icin orada atif hic cizilmiyor |
| K-M42 | QR'ın **uygulamanın KENDİ tarayıcısıyla** okunması cihazda doğrulanmadı | açık | kullanıcı (manuel) | 2026-09-09: emülatörün sanal sahne kamerası başsız olarak poster'a NİŞANLANAMIYOR (`-virtualscene-poster` ile QR sahneye kondu, ama kamerayı çevirmek için emülatör penceresinde fare/WASD gerekiyor). Doğrulanan: QR'ın kendisi doğru (`https://bumpinto.app/j/<slug>`, ekran görüntüsünden makineyle çözüldü), kamera izni + canlı önizleme + Play ön-bilgilendirme şeridi çalışıyor, `parseInvite` birim testli. Eksik olan tek halka: gerçek karenin çözülüp yönlendirmeyi tetiklemesi. **Kullanıcı 2026-09-09'da bunu elle test edeceğini söyledi.** `docs/store/DEVICE-CHECKLIST-M9.md` §4 |
| K-M43 | M-9 yüzeyleri **iOS'ta hiç koşulmadı** (bu makinede iOS dev build kurulmadı) | açık | I-3 ya da elle | 2026-09-09. **iOS DERLENDİ, KURULDU ve AÇILDI** (simülatör iPhone 17 / iOS 26.1, Xcode 26.6): `expo run:ios` BUILD SUCCESSFUL → uygulama kuruldu → giriş ekranı (O2, Apple düğmesi dahil) çizildi. **Kurulu bundle'ın Info.plist'i doğrulandı**: `NSSupportsLiveActivities = true` (withLiveActivity çalışıyor), `NSCameraUsageDescription`, `ITSAppUsesNonExemptEncryption=false`; `PlugIns/` YOK — widget target üretilmedi (M-9 kapsam kilidi tutuyor). Açık kalan: M-9 **yüzeylerinin** iOS çalışma anı — `UTI: public.png` / `com.apple.ical.ics` paylaşımı, `NSCameraUsageDescription` diyaloğu, `expo-sharing` sayfası, iOS `DateTimePicker display="compact"`. Liste: `docs/store/DEVICE-CHECKLIST-M9.md` |
| K-M44 | Giriş düğmelerinde marka işaretleri: mobil Google düğmesinde resmi **renkli "G"** yoktu (Apple'ın glifi vardı) | **done** (2026-09-09) | kullanıcı isteği | `src/components/atoms/GoogleLogo.tsx` — `react-native-svg` ile resmi dört renkli işaret. Phosphor'un `GoogleLogoIcon`u BİLEREK kullanılmadı: tek renkli ve Google'ın "kendi düğmeni yaparsan resmi işareti kullan" kuralını karşılamıyor. Marka renkleri `theme.ts` token'ı DEĞİL, marka sabiti (aynı istisna `Button`daki `#fff`de de var). Simülatörde doğrulandı. **Web'de değişiklik GEREKMEDİ**: oradaki düğmeyi GIS kendisi render ediyor ve resmi G'yi zaten basıyor |
| K-M45 | **GIS düğmesi uygulama diliyle ayrışıyordu** — sayfa `?lng=tr` ile tamamen Türkçeyken düğme "Doorgaan met Google" kalıyordu | **done** (2026-09-09) | kullanıcı isteği (M-9 dışı bulgu) | 2026-09-09 web ekran görüntüsünde yakalandı. Kök sebep: `renderButton`a geçilen `locale` seçeneğini **GIS YOK SAYIYOR**, kendi çözümlemesine (tarayıcı/hesap/IP) düşüyor — `?lng=tr` deneyiyle kanıtlandı (sayfa TR, düğme NL). Doğru yol betiğin `?hl=` parametresi: `gsi/client?hl=<resolvedLanguage>`. Düzeltildi ve doğrulandı ("Google ile devam edin"). **Kalan sınır:** betik sayfa başına bir kez yüklendiği için uygulama içinden dil değiştirilirse düğme eski dilde kalır (yeniden yükleme gerekir) — GIS ikinci yüklemeye izin vermiyor, kod yorumunda yazılı |
| K-M37 | Sesli sohbetin CİHAZ doğrulaması — **büyük kısmı 2026-09-09'da KAPANDI**, kalan üç kalem açık | kısmen açık | M-6 | **Emülatörde (`bumpinto-api35`, Android 15, arm64) uçtan uca koşuldu.** DOĞRULANDI: (1) APK kuruldu ve açıldı, çökme yok; (2) misafir derin linki (`bumpinto://j/<slug>`) → Katıl → O3 konum ön-ekranı → Bekle ekranı (P10) çizildi; (3) dock P25:2'de (`1 person · 116 min left`) ve Katıl'dan sonra P25:4'te (`In voice chat` + mikrofon/hoparlör/ayrıl) çizildi; (4) **`react-native-webrtc` yerel modülü YÜKLENDİ ve çalıştı** — `getUserMedia(audio)` gerçek `MediaStream` döndürdü (16 KB hiza kaygısı böylece ampirik olarak kapandı); (5) **`InCallManager` New Arch altında çalıştı** — `start audioRouteManager`, `USAGE_VOICE_COMMUNICATION` ses odağı `AUDIOFOCUS_REQUEST_GRANTED`, kablolu kulaklık/gürültü/medya-düğmesi olayları ve yakınlık sensörü kaydedildi, `setKeepScreenOn(true)`; ayrılışta `stop()` + olay kaydı temizliği. `UnsatisfiedLinkError`/FATAL YOK. (6) **iOS simülatör derlemesi: `** BUILD SUCCEEDED **`** (Debug, `CODE_SIGNING_ALLOWED=NO`). (7) `ws-smoke.mjs` host jetonuyla koştu: el sıkışma + **sinyal rölesi** (`from` damgalı ICE kişisel kutuya döndü) doğrulandı. **HÂLÂ AÇIK:** (a) bluetooth kulaklık yönlendirmesi — emülatörde BT yok, ayrıca eklenti `BLUETOOTH_CONNECT` EKLEMİYOR (kütüphane API 31+'ta izni bulamayınca çöküyor değil, bluetooth'u SESSİZCE atlıyor: `AppRTCBluetoothManager` yalnız uyarı basar) — **izin 2026-09-09'da kullanıcı kararıyla EKLENDİ** (`app.config.ts` → manifestte doğrulandı) ve çalışma zamanı talebi `requestBluetoothConnect` ile katılım yolunda yapılıyor (EN İYİ ÇABA: reddedilirse ses telefondan çıkar, akış bozulmaz); gerçek BT kulaklıkla yönlendirme HÂLÂ sınanmadı — emülatörde bluetooth yok; (b) iki uçlu GERÇEK ses (ikinci cihaz/web istemcisi gerekir); (c) iOS CİHAZ koşusu (yalnız derleme yapıldı); (d) Maestro akışları (araç kurulu değil, ayrıca 04 Google girişi istiyor). |
| K-M38 | **Bayat hesap jetonu, MİSAFİR isteklerini 401 yapıyor** — cihazda bulundu, ürün hatası | kapandı | M-4/M-5 kimlik | 2026-09-09 M-6 cihaz doğrulamasında yakalandı ve A/B ile kanıtlandı. `createHttp` her isteğe SecureStore'daki hesap jetonunu `Authorization: Bearer` olarak ekliyor; `tokenStore` sürede denetim YAPMIYOR. Sunucu geçersiz/süresi dolmuş bearer'ı görünce 401 veriyor ve `X-Participant-Token`a HİÇ bakmıyor. Kanıt (curl): yalnız katılımcı jetonu → **200**; aynı jeton + geçersiz `Authorization` → **401**. Cihaz A/B: bayat jetonla Katıl sonrası "Bu oturum bulunamadı" hata ekranı; `pm clear` ile jeton silinince AYNI akış Bekle ekranını çizdi. ETKİ: hesap jetonu 12 saatte doluyor, oturum 24 saat yaşıyor — arada uygulamayı açan HERKES oturumunu kaybediyor. Seçenekler: (1) `tokenStore` `exp`i okuyup süresi dolmuş jetonu düşürsün (en dar düzeltme); (2) 401'de jetonu temizleyip bir kez yeniden dene; (3) sunucu bearer başarısızsa katılımcı jetonuna düşsün. **KAPANDI 2026-09-09 (kullanıcı kararı: seçenek 1).** `tokenStore.getAccessToken` artık JWT `exp`ini okuyup süresi dolmuş jetonu SecureStore'dan siler ve `null` döner; çözülemeyen jeton da kullanılmaz (`exp` taşımayan jeton kabul edilir — istemci süre uydurmaz). `atob` KULLANILMADI: Hermes'te varlığı garanti değil ve `tsc` onu yalnız DOM tip kütüphanesinden tanıyor, yani derleme geçerken cihazda patlayabilirdi — yerine dosya içinde saf base64url çözücü (latin1; okuduğumuz tek alan `exp` ve JSON'un yapısı ASCII). 6 birim testi: `src/lib/tokenStore.test.ts`. **KÖK ÇÖZÜM DEĞİL:** bu yalnız ölü jetonu göndermemeyi sağlar; kullanıcı 12 saat sonra yine sessizce çıkışa düşer. **KÖK ÇÖZÜM GELDİ — Plan 45 (2026-09-09), B-16 + W-16 + M-10 done.** İki katman: (1) SUNUCU — `SecurityConfig.bearerTokenResolver` artık bayat bearer'ı, istek KENDİ BAŞINA kimlik taşıyorsa (public uç ya da o slug'a ait GEÇERLİ katılımcı jetonu) DÜŞÜRÜYOR; kök mekanizma doğrulandı: `BearerTokenAuthenticationFilter` `ParticipantTokenFilter`tan ÖNCE koşup zinciri kesiyordu. Çözüm gövdesi `ParticipantTokens`te — filtre ve resolver aynı kodu okur. Canlı curl A/B ile kanıtlandı: yalnız katılımcı jetonu **200** · + geçersiz bearer **200** (eskiden 401) · + süresi geçmiş gerçek imzalı bearer **200** · `/api/me` bayat bearer'la hâlâ **401** (karşı kapı: istemci yenilemeyi bu sayede öğreniyor). (2) İSTEMCİ — 15 dk erişim jetonu + rotasyonlu yenileme jetonu, tek-uçuşlu 401 kesicisi. `tokenStore` kapısı EMNİYET AĞI olarak KALDI, kaldırılmadı. |
| K-M46 | **Mobil `meStore.update` kısmi gövde gönderiyordu** — mobilde dil seçmek sunucudaki varsayılan konumu, etkinliği ve ulaşım türünü SİLİYORDU | **done** (2026-09-11) | K-M7 ön koşulu | `PUT /api/me` tam değiştirmedir (`UserProfile.withPreferences`: null gelen konum/etiket/etkinlik/dil/ulaşım türü silinir, yalnız ad ve `interests` korunur); `app/(sheets)/prefs.tsx` `{ language }` gibi tek alan veriyordu. `frontend/mobile/src/store/meStore.ts` web `authStore.updatePrefs` desenine geçti (gövde mevcut `me`'yi taşır, `patch` yalnız değişeni ezer, `interests` gönderilmez; `me` yüklenmemişse yazmaz) — tek test `src/store/meStore.test.ts`. Manuel test (dil değişince diğer tercihler korunuyor) kullanıcıda geçti. |
| K-M47 | Mobilde engel listesi / engel kaldırma yüzeyi yok (K-W16'nın mobil karşılığı) | aday | M izi, sonraki plan | 2026-09-11 K-W16 web'de `/account/blocks`'u açtı; backend `BlockDto.displayName` ve shared `api.listBlocks()`/`api.removeBlock()` hazır, mobil `app/account/*` altında kullanan yok. Mobil artboard taraması yapılmadı. |

## I — Altyapı

| Kimlik | Plan | Dosya | Eski # | Durum | Bağımlılık | Son adım | Not |
|---|---|---|---|---|---|---|---|
| I-1 | CI + Docker + K8s deploy | `2026-09-01-plan5-ci-deploy.md` | Plan 5 | ready | B-2 ✓, W-1 ✓ · *Task 4 için* `B-3` + `W-4` + ~~`M-2:T5`~~ (M-2 superseded, 2026-09-11 notu: EAS build artık I-3'te) | — | **Task 1–3 şimdi koşabilir.** **Ek A (2026-09-02):** web build-arg'ları (`VITE_GOOGLE_CLIENT_ID`, `VITE_GOOGLE_MAPS_KEY`, `VITE_GOOGLE_MAPS_MAP_ID`), Map ID + stil, referrer/paket kısıtlı anahtarlar, kota alarmı, yayın listesine SOLO uçtan uca. Task 4 = yayın kontrol listesi; B-3 `done` ve retention CronJob uygulanmadan işaretlenmez (spec §6 GDPR). Postgres kullanıcının mevcut kümesinde |
| I-2 | **Açık veri servisleri** — PostGIS (compose + küme), `tools/venues-open` (Overture NL + OSM NL osm2pgsql + Wikidata P18 + dedupe, staging→swap, aylık CronJob, GHCR imajı), Nominatim NL, OSRM ×3 (prepare Job + refresh CronJob), isteğe bağlı PMTiles + nginx, `bumpinto-geo` ConfigMap, runbook | `2026-09-06-plan32-open-hybrid-infra.md` | Plan 32 | done | I-1:T1–T3 (imaj/secret adları: Secret `bumpinto-backend`) · B-13:T6 (V11 tabloyu yaratır) | Task 8/8 + Opus incelemesi | Spec §5.2, §7–§9, §13, §15–§16. 8 görev. Disk ~30 GB (Nominatim 15, OSRM 5–10, PostGIS 2, tiles 2). Yıkıcı adımlar "KULLANICI ÇALIŞTIRIR" etiketli (`docker compose down -v`, küme `psql`). Swap SQL indeks adlarını `ALTER INDEX … RENAME` ile V11 adlarına döndürür. İlk ithal tür başına sayım + Wikidata foto kapsaması basar (TripAdvisor 1b kararının girdisi). **Not:** osm2pgsql 2.1 imajda (plan 1.x bekliyordu; ilk gerçek ithalde doğrulanır); küme doğrulaması yapılmadı: yalnız client-side dry-run; T1 Step 3 (`docker compose down -v`) ve Step 5 (küme `psql create extension postgis`) KULLANICI'da; PMTiles yedeği isteğe bağlı, env ile açılır |
| I-3 | **Mobil yayın hattı** — `eas.json` profilleri (development/preview/production + submit; `appVersionSource: remote`, `runtimeVersion: appVersion`), imzalama EAS-managed (ASC API key, EAS keystore + Play App Signing; GitHub sırrı yalnız `EXPO_TOKEN` + `E2E_HOST_TOKEN`), `mobile-ci.yml` (+ `setup-mobile` composite), `mobile-build.yml` (PR etiketi → preview APK; `mobile-v*` tag → production, sürüm eşleşme kapısı), `mobile-e2e.yml` (Android emülatör + Maestro, preview APK'sını tüketir), `mobile-submit.yml` (korumalı ortam + `check-release-checklist.mjs`), Maestro `takeScreenshot` ile mağaza görselleri + `store-shots.mjs`, 16 KB `.so` hiza + `targetSdk 36` kapısı, `docs/RELEASE-MOBILE.md` runbook | `2026-09-06-plan44-infra-mobile-release.md` | Plan 44 | ready | **M-4 ✓** (build), **M-5** (submit), I-1:T1–T3 (secret kalıbı) | — | 9 görev. K-I1: e2e'nin "giriş" ayağı CI'da koşamaz (gerçek Google hesabı) → `katıl → deste → karar` deep link + katılımcı jetonuyla; giriş elle yayın kapısı. Ücretli/yıkıcı adımlar "KULLANICI ÇALIŞTIRIR". |

---

## Çapraz iz kilitleri

1. **Flyway sırası (kural 9).** B-5 (V3) → B-6 (V4) → B-3 (V5). B-3 artık B-6'dan önce koşamaz.

2. **I-1 ⇄ B-3 (imaj/secret adları).** Sıra: `I-1 Task 1-3` → `B-3 tümü` → `I-1 Task 4`.
   `B-3:T5` K8s CronJob'ı `I-1:T3`'teki backend imajının ve secret adının aynısını kullanır.

3. **W-3 → B-6.** W-3 Task 4–7 (`/api/me`, liste, çıkış, `preview`, `viewer`) B-6'yı bekler; Task 1–3
   (kabuk, i18n, iki bölge) beklemez.

4. **W-4 → B-5 + W-3.** Mekanlar/`shuffle`/`points`/`approxLocation`/`midpoint` B-5'ten; kabuk ve
   `authStore` W-3'ten.

5. **M-2 → W-4.** Dil dosyalarının `frontend/shared`'a taşınması web'i de değiştirir; W-4 kapanmadan
   yapılırsa çakışır. M-1 ise yalnız B-6'yı bekler.

6. **I-1:T4 ← B-3, W-4, M-2:T5.** Yayın kontrol listesi: retention CronJob, web uçtan uca (Grup +
   Bireysel, gerçek Maps anahtarları), EAS internal build.

7. **B-14 → B-3.** V13–V16, B-3'ün V12'sinden sonra gelir; `AccountDeletion` `purge_after` alanını B-3'ün temizlik işi tüketir. B-3 done olmadan B-14 T1 açılmaz.

8. **W-13 → W-12 (K-B26).** `travelMinutes` → `travel[]` geçişi `shared/fairness.ts`'i değiştirir; W-12'nin `Attribution sources[]` ve `travel[]` üretimi olmadan W-13 T2 çakışır.

9. **W-14 / M-5 → B-14.** `openapi.json`'da `/api/auth/apple`, `DELETE /api/me`, `/api/me/consents`, `/api/me/export`, `/api/reports`, `/api/me/blocks` görünmeden başlamaz. M-5 ayrıca W-14'ün `/privacy /terms /kvkk /account/delete` URL'lerini (mağaza meta verisi) bekler.

10. **M-4 ⇄ W (shared taşıma).** M-4 T3 i18n JSON'larını ve 9 saf `lib` modülünü `frontend/shared`'a taşır, web'de shim bırakır; aynı anda koşan W-13/W-14 bu dosyalara dokunuyorsa önce M-4 T3 kapanır.

11. **W-15 → B-15.** `lastSeenAt`/`linkOpenedAt`/`nudge`/`nudged` B-15 T5–T6'dan; R-W15 (rapor/engel) yalnız B-14'ü bekler — W-15 T6 önce koşabilir.

12. **M-7 → M-4 → (M-8).** M-7 T4 durum yönlendiricisi M-8 ekran dosyalarını `return null` iskeletiyle açar; M-8 aynı dosyaları doldurur, yönlendiriciyi yeniden yazmaz. M-7 T4'ün `BROWSING` testi T5 (Mekanlar) bitince tam yeşil.

13. **M-9 → B-15 + M-8; M-6 → M-7.** `nudged` olayının mobilde üretilmesi M-6'nın `liveChannel` portuna bağlıdır (K-M6); M-9 T2 M-6 yoksa yalnız toast/soğuma katmanını kurar.

17. **M-9 ⇄ W-15 (paylaşılan dosyalar) — 2026-09-09'da UYGULANDI.** `nudge` satırını W-15 zaten eklemişti;
    M-9 yalnız `sessionByCode`ı ekledi. ICS **web'den PAYLAŞILAN'a taşındı** (`frontend/shared/src/ics.ts`),
    `frontend/web/src/lib/ics.ts` shim'e çevrildi (M-4:T3b deseni) — `MeetTimeDialog` değişmeden çalışıyor,
    takvim metni artık tek yerde. Aynı desen atıf eşlemesinde: `shared/src/attribution.ts` (web henüz tüketmiyor, K-M41).

14. **I-3 ⇄ M-4/M-5.** I-3 T1 `eas.json`'daki `preprod` profilini `preview` yapar (M-4 T2 ile çelişirse I-3 kazanır); T6 submit ve T7 görseller M-5'in `RELEASE-CHECKLIST.md`'sini bekler.

15. **I-2 ⇄ I-1 ⇄ B-13.** `deploy/k8s/` I-2 ile doğar; I-1:T3 backend Deployment'ı
    `configMapRef: bumpinto-geo`'yu I-2'den alır (Plan 5 Ek B). `venues-open-import` swap'i
    B-13'ün V11'ini bekler — V11 yoksa iş bilerek patlar. Sıra: **I-1:T1–T3 → B-13 (V11 dahil)
    → I-2:T5–T8 → I-1:T4**. I-2:T1–T4 (compose + `tools/venues-open` + testler) hiçbirini beklemez.

16. **B-14 → W-14 → M-5.** M-5'in yasal okuyucularındaki "Tarayıcıda aç" ve Play hesap silme URL'si
   W-14 rotalarına bağlıdır; W-14 yayına çıkmadan mağaza formu doldurulamaz. `POST /api/reports`,
   `PUT /api/me/consents`, `DELETE /api/me` B-14'ten gelir. Yasal METİN artık
   `frontend/shared/src/content/legal/` altında **ortaktır** (K-M18): web metnini değiştiren mobili de
   değiştirir, ikisi ayrışamaz.

17. **M-5 → M-6 (mikrofon kancası).** Sesli sohbet dock'u `getUserMedia` çağırmadan ÖNCE
   `src/lib/micConsent.ts` `presentMicConsent()` bekler; `"granted"` dışında bir sonuçta oylama akışı
   BOZULMADAN hata durumuna geçer. Mesh yalnız `src/lib/blocks.ts` `voicePeers()` listesine teklif
   gönderir — engelli çift aynı odaya alınmaz.

18. **M-5 → M-7 (konum ön-ekranı).** "Neredesin?" alanına dokunuş `/(sheets)/location-consent?next=…`
   açar ve `locationPermission` (`granted`/`denied`/`blocked`/`manual`) ile döner; `denied`/`blocked`'da
   `LocationDeniedCard` çizilir. Muhafız yazılırken `app/_layout.tsx` `ANONYMOUS_ROUTES` listesi esastır.

19. **W-17 → B-17.** `openapi.json`'da `/api/discover`, `/seat-requests`, `/checkin` ve
   `MeResponse.interests` görünmeden W-17'nin T1 dışındaki görevleri başlamaz. Sözleşme değişince
   `pnpm codegen` — `api-types.ts` elle düzenlenmez.

20. **M-11 → M-4 ✓ + M-7 ✓ + B-17.** Mobil kabuk ve "Yeni buluşma + Katıl" akışı hazır; kalan tek
   bağımlılık B-17'nin sözleşmesi. Keşfet alt sekme DEĞİL, Oturumlar'dan itilen rota (plan38 "tek stack").

21. **B-17 ⇄ K-B15 (orta nokta semt adı).** Keşfet kartı `midpointLabel` basıyor; orta-nokta modunda o
   alan bugün `find-venues`a kadar null (`DeckFlow:136`). Kullanıcı kararı 2026-09-09: etiket **açık plan
   kurulurken** bir kez ters geocode edilir. B-17'nin `createSession(openPlan)` görevi (Task B4) bunu içerir;
   K-B15 bağımsız bir satır olmaktan çıkıp bu izin parçası oldu.

22. **W-18 → B-18 + W-17.** `api-types.ts`'te `OpenPlanInput.openUntil/audience`, `PlanCardDto.openUntil`,
   `StatsDto.plansMet/metStreakWeeks` görünmeden W-18 T2+ başlamaz (T1 shared modülleri codegen'siz de
   yazılabilir). W-17 W2 henüz başlamadıysa W2 doğrudan W-18 T2 şekliyle uygulanır: "Keşfet'te göster"
   anahtarlı ara şekil ÜRETİLMEZ. W-17 W1'in `discover.thisWeek/weekend/all` anahtarları W-18 T6'da
   `discover.range.*` altına taşınır.

23. **M-12 → B-18 + W-18:T1 + M-11.** Mobil store'a W2-benzeri alan/`validate` yazılmaz (K-M8); kural
   shared `openPlan.ts`'te, i18n anahtarları shared locales'te (W-18 T6). M-11 M2 başlamadıysa M2 doğrudan
   M-12 T1+T2 şekliyle uygulanır.

24. **B-19 → B-18.** `audience` kolonu ve `FRIENDS` enum değeri B-18'de iner; B-19 yalnız kapıyı açar
   (`SessionController.openPlanOf` 400'ü kalkar) ve `friendships`'i getirir. Spec §8 modeli bağlayıcı;
   gizlilik metni değişikliği hukukçu kapısına bağlı.


**B-4 açılırsa** (deferred): API sözleşmesi değişir → W-1/W-2/W-3/W-4/M-1/M-2 geriye dönük düzeltme;
`B-4 T7` `I-1:T3`'ten sonra koşar.
