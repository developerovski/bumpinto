# Plan 3: pnpm Workspace + Web Katılım Uygulaması

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** JS monorepo'yu kurmak ve katılımcının linkten girip katıldığı, kaydırdığı ve sonucu gördüğü web uygulamasını teslim etmek.

**Architecture:** pnpm workspaces (`frontend/*`). `@bumpinto/shared`: OpenAPI tipli **axios** client (tek HTTP adaptörü). Web: **atomic design** (`components/atoms|molecules|organisms` + `pages`), state **Zustand** store'ları (`sessionStore`, `deckStore`). Canlı veri: 3 sn polling + STOMP (olay → store.refresh).

**Tech Stack:** pnpm 9+, Vite 6, React 18, TypeScript, react-router-dom 7, **axios**, openapi-typescript 7 (yalnız tip üretimi), **zustand 5**, @stomp/stompjs 7, Vitest + Testing Library (smoke — AGENTS.md).

**Ön koşul:** Plan 2 `done`; backend lokalde çalışabilir durumda.

---

## UI Kaynağı: Claude Design (BAĞLAYICI)

Bu plandaki ekranların görsel kaynağı Claude Design'dır. Plandaki JSX/stil kodu
işlevsel başlangıç iskeletidir — **nihai görünüm değildir**. Yürütücü ajan:

1. Her UI görevine başlarken ilgili artboard'u Claude Design MCP ile **güncel**
   halinden okur (kullanıcı tasarımı düzenlemiş olabilir — plandan/bellekten varsayma):
   - Ekranlar: `mcp__claude_design__read_file` → project_id
     `719fcd5f-bb62-4356-9c53-7d4f0a8fbe36`, path `Web Ekranlar v2.dc.html`.
     Dosya büyük — `offset`/`limit` pencereleriyle oku; ekran bloğunu
     `data-screen-label="…"` özniteliğinden bul.
   - Token/bileşen referansı: project_id `b536b3aa-8945-4865-b7e5-e693f8d5a588`,
     path `Design System v2.dc.html`.
2. Yerleşim, boşluk, renk, tipografi, köşe ve **mikro-kopya metni** dahil tasarıma
   sadık kal. Plandaki kod ile tasarım çelişirse **tasarım kazanır**.
3. Tasarımda karşılığı olmayan ekran/durum **icat edilmez**: INDEX'te `blocked` +
   kullanıcıya sor. Ajan kendi tasarımını YAPMAZ.
4. Her UI görevinin kapanışında ekran, artboard ile karşılaştırılır (browser aracı
   varsa `render_preview` serve_url'i ile; yoksa kullanıcıdan görsel onay iste).

Artboard ↔ sayfa eşlemesi:

| Artboard | Sayfa (page) |
|---|---|
| W1 · Katıl | `pages/JoinForm.tsx` |
| W2 · Katıldın | `pages/WaitingRoom.tsx` |
| W3 · Deste web | `pages/DeckScreen.tsx` |
| W4 · Sonuç | `pages/ResultScreen.tsx` |
| (web artboard'u yok) Runoff | `pages/RunoffScreen.tsx` — kaynak: `Mobil Ekranlar v2.dc.html` içindeki `07 Runoff`; bire bir uyarlanır, yeni öğe icat edilmez |

### Tasarım denetimi bulgusu (2026-09-01) — W3'te kapatılması ZORUNLU

`ActivityType` 5→15 genişletmesi sonrası yapılan UI/UX denetiminin **tek web bulgusu**:

- **`.pho-tag` "foto · Places" rozeti koşullu olmalı.** W3'te (`data-screen-label="Deste web"`)
  kartın sağ üstünde duruyor. Yeni 10 aktivite türünün mekanları yalnız Google'dan geliyor ve
  `VenueDto.photoUrl` bu türlerde **daima `null`**. Rozet olduğu gibi bırakılırsa kullanıcıya
  var olmayan bir fotoğrafın kaynağını bildirir. `photoUrl != null` değilse **hiç render edilmez**.

Web'de başka hiçbir şey etkilenmiyor:

- **Oturum kurma ekranı web'de yok** — aktivite seçici yalnız mobilde (Plan 4, artboard 03).
  15 chip yerleşimi sorunu bu planı ilgilendirmiyor.
- Tür yalnız W1'deki `<span class="bg g-fl">` rozetinde görünüyor; `.bg` zaten
  `white-space:nowrap` ve en uzun etiket ("Doğa yürüyüşü") sığıyor.
- **Fotoğrafsız kart tasarımda zaten çözülmüş durumdur** — Design System v2 "Hareket ve kurallar"
  bölümünde kural olarak yazılı: *"Fotoğraf yoksa ambient gradyan + monogram — asla çizgili kutu."*
  W3 ve W4'te tek bir `<img>` yok; `.pA/.pB/.pC` gradyanı + `.pho-mono` monogramı zaten
  varsayılan görünüm. `photoUrl` için placeholder tasarlamaya gerek YOK, icat da edilmez.

## Frontend mimari kuralları (BAĞLAYICI — kullanıcı talimatı 2026-09-01)

- **Atomic design:** her görsel parça reusable bileşendir. `atoms` (tek sorumluluklu
  yapı taşları: Button, Input, Badge…), `molecules` (atomlardan kurulu birimler:
  VenueCard, ParticipantRow…), `organisms` (sayfa bölümleri: VenueDeck,
  ParticipantList…), `pages` (kompozisyon + store bağlama; kendi stili yok).
  Sayfaların içinde ham `<button>/<input>` veya kopyalanmış stil YASAK — hepsi
  bileşenden gelir.
- **State: Zustand.** Sunucu görünümü `sessionStore`, deste etkileşimi `deckStore`.
  Bileşenler prop alır; store'a yalnız pages/organisms bağlanır.
- **HTTP: axios.** Tek instance `@bumpinto/shared`'da; interceptor'lar token'ları
  ekler. Bileşen/hook içinden doğrudan `axios`/`fetch` çağrısı YASAK — her çağrı
  `api.*` fonksiyonundan geçer.

## Güvenlik modeli (BAĞLAYICI — kullanıcı talimatı 2026-09-01)

- **Web hassas bilgi TUTMAZ.** Token'lar backend'in bastığı HttpOnly cookie'lerde
  (`bumpinto_at`, `bumpinto_pt_{slug}`) yaşar; JS tarafında localStorage/sessionStorage'da
  token YOK. Axios `withCredentials: true` + `X-Client: web` header'ı ile çağırır;
  cookie'yi tarayıcı otomatik taşır.
- **Profiller:** `local` (`.env.development` — proxy, boş VITE_API_URL),
  `preprod` (`.env.preprod`), `prod` (`.env.production`). Build:
  `pnpm build:web` (prod) / `pnpm build:web:preprod` (`vite build --mode preprod`).

## Bu plana özel kurallar

- **INDEX güncelle** (başlarken `in-progress`, görev sonlarında `Son adım`, bitince `done`).
- **Git yazma YOK** — commit kullanıcıda. Komutlar `rtk` ile (`rtk pnpm ...`).
- Aksi belirtilmedikçe komutlar **repo kökünden** çalıştırılır.
- Renk/font token DEĞERLERİ Task 3'teki `tokens.css`'e yazılır ama kaynağı Design
  System v2'dir — çelişkide Claude Design kazanır.

---

### Task 1: pnpm workspace iskeleti

**Files:**
- Create: `pnpm-workspace.yaml`
- Create: `.npmrc`
- Create: `package.json` (repo kökü)
- Create: `frontend/shared/package.json`
- Create: `frontend/shared/tsconfig.json`

- [ ] **Step 1: Dosyaları yaz**

`pnpm-workspace.yaml`:

```yaml
packages:
  - frontend/*
```

`.npmrc`:

```ini
node-linker=hoisted
```

`package.json` (kök):

```json
{
  "name": "bumpinto",
  "private": true,
  "scripts": {
    "codegen": "curl -sf http://localhost:8080/v3/api-docs -o frontend/shared/openapi.json && pnpm --filter @bumpinto/shared generate",
    "dev:web": "pnpm --filter @bumpinto/web dev",
    "build:web": "pnpm --filter @bumpinto/web build",
    "build:web:preprod": "pnpm --filter @bumpinto/web build -- --mode preprod",
    "test:web": "pnpm --filter @bumpinto/web test -- --run"
  }
}
```

`frontend/shared/package.json`:

```json
{
  "name": "@bumpinto/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "generate": "openapi-typescript openapi.json -o src/api-types.ts"
  },
  "dependencies": {
    "axios": "^1.7.9"
  },
  "devDependencies": {
    "openapi-typescript": "^7.6.0",
    "typescript": "^5.6.0"
  }
}
```

`frontend/shared/tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src"]
}
```

- [ ] **Step 2: Kur** — Run: `rtk pnpm install` → hatasız, kökte tek `pnpm-lock.yaml`.

- [ ] **Step 3: INDEX güncelle + Commit (kullanıcı)** — `feat(workspace): pnpm monorepo iskeleti`

---

### Task 2: @bumpinto/shared — tipli axios client

**Files:**
- Create: `frontend/shared/openapi.json` (codegen — elle yazılmaz)
- Create: `frontend/shared/src/api-types.ts` (codegen çıktısı — elle yazılmaz)
- Create: `frontend/shared/src/http.ts`
- Create: `frontend/shared/src/api.ts`
- Create: `frontend/shared/src/index.ts`

- [ ] **Step 1: Codegen** — backend açıkken Run: `rtk pnpm codegen`
Expected: `api-types.ts` içinde `"/api/sessions/{slug}"` yolları ve `SessionView` şeması.

- [ ] **Step 2: http.ts** (tek axios instance + token interceptor'ları)

```typescript
import axios, { type AxiosInstance } from "axios";

export type AuthProviders = {
  /** Host JWT (mobil). Web'de tanımsız bırakılır. */
  getIdToken?: () => Promise<string | null> | string | null;
  /** Oturuma özel katılımcı token'ı. */
  getParticipantToken?: (slug: string) => string | null | undefined;
};

const SLUG_RE = /\/api\/sessions\/([^/]+)/;

export type HttpOptions = {
  /** Web: true — HttpOnly cookie'ler otomatik taşınır. Mobil: false. */
  withCredentials?: boolean;
  /** Backend'in cookie mi body mi döneceğini seçer. */
  client?: "web" | "mobile";
};

export function createHttp(baseUrl: string, providers: AuthProviders,
    options: HttpOptions = {}): AxiosInstance {
  const http = axios.create({
    baseURL: baseUrl,
    timeout: 10000,
    withCredentials: options.withCredentials ?? false,
    headers: options.client ? { "X-Client": options.client } : undefined,
  });
  http.interceptors.request.use(async (config) => {
    const idToken = await providers.getIdToken?.();
    if (idToken) config.headers.set("Authorization", `Bearer ${idToken}`);
    const match = (config.url ?? "").match(SLUG_RE);
    const participantToken = match ? providers.getParticipantToken?.(match[1]) : undefined;
    if (participantToken) config.headers.set("X-Participant-Token", participantToken);
    return config;
  });
  return http;
}
```

- [ ] **Step 3: api.ts** (uç başına tipli fonksiyon — bileşenler axios'u hiç görmez)

```typescript
import type { AxiosInstance } from "axios";
import type { components } from "./api-types";

export type Schemas = components["schemas"];
export type SessionView = Schemas["SessionView"];
export type VenueDto = Schemas["VenueDto"];
export type ParticipantDto = Schemas["ParticipantDto"];

export function createBumpintoApi(http: AxiosInstance) {
  return {
    loginGoogle: (idToken: string) =>
      http.post<Schemas["LoginResponse"]>("/api/auth/google", { idToken }).then((r) => r.data),
    createSession: (body: Schemas["CreateSessionRequest"]) =>
      http.post<Schemas["CreateSessionResponse"]>("/api/sessions", body).then((r) => r.data),
    getSession: (slug: string) =>
      http.get<SessionView>(`/api/sessions/${slug}`).then((r) => r.data),
    join: (slug: string, body: Schemas["JoinRequest"]) =>
      http.post<Schemas["JoinResponse"]>(`/api/sessions/${slug}/participants`, body)
        .then((r) => r.data),
    updateLocation: (slug: string, body: Schemas["LocationRequest"]) =>
      http.put(`/api/sessions/${slug}/location`, body).then(() => undefined),
    findVenues: (slug: string) =>
      http.post<SessionView>(`/api/sessions/${slug}/find-venues`).then((r) => r.data),
    swipe: (slug: string, body: Schemas["SwipeRequest"]) =>
      http.post(`/api/sessions/${slug}/swipes`, body).then(() => undefined),
    undoSwipe: (slug: string, venueId: string) =>
      http.delete(`/api/sessions/${slug}/swipes/${venueId}`).then(() => undefined),
    deckDone: (slug: string) =>
      http.post(`/api/sessions/${slug}/deck-done`).then(() => undefined),
    forceDecision: (slug: string, body?: Schemas["ForceDecisionRequest"]) =>
      http.post<SessionView>(`/api/sessions/${slug}/force-decision`, body ?? {})
        .then((r) => r.data),
    runoffVote: (slug: string, body: Schemas["RunoffVoteRequest"]) =>
      http.post(`/api/sessions/${slug}/runoff-votes`, body).then(() => undefined),
  };
}

export type BumpintoApi = ReturnType<typeof createBumpintoApi>;
```

- [ ] **Step 4: index.ts**

```typescript
export { createHttp, type AuthProviders, type HttpOptions } from "./http";
export {
  createBumpintoApi,
  type BumpintoApi,
  type ParticipantDto,
  type Schemas,
  type SessionView,
  type VenueDto,
} from "./api";
export type { components, paths } from "./api-types";
```

- [ ] **Step 5: Tip kontrolü** — Run: `rtk pnpm --filter @bumpinto/shared exec tsc --noEmit` → hatasız.

- [ ] **Step 6: INDEX güncelle + Commit (kullanıcı)** — `feat(shared): tipli axios client`

Not: `openapi.json` + `api-types.ts` üretilmiş ama commit'lenir; API değişince
`pnpm codegen` yenilenir. `api.ts`'teki şema adları backend DTO record adlarıdır —
codegen sonrası uyuşmazlık görürsen `api-types.ts`'e bak, tahmin etme.

---

### Task 3: Web iskeleti + tokens + ATOM bileşenleri

**Files:**
- Create: `frontend/web/package.json`
- Create: `frontend/web/vite.config.ts`
- Create: `frontend/web/tsconfig.json`
- Create: `frontend/web/index.html`
- Create: `frontend/web/src/main.tsx`
- Create: `frontend/web/src/App.tsx`
- Create: `frontend/web/src/test-setup.ts`
- Create: `frontend/web/src/lib/api.ts`
- Create: `frontend/web/.env.development` / `.env.preprod` / `.env.production`
- Create: `frontend/web/src/styles/tokens.css`
- Create: `frontend/web/src/styles/ui.css`
- Create: `frontend/web/src/components/atoms/` → `Button.tsx`, `TextInput.tsx`, `Badge.tsx`,
  `Chip.tsx`, `Sticker.tsx`, `Avatar.tsx`, `Progress.tsx`, `Highlight.tsx`, `HandNote.tsx`,
  `index.ts`
- Test: `frontend/web/src/App.test.tsx`

- [ ] **Step 1: package.json**

```json
{
  "name": "@bumpinto/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "test": "vitest"
  },
  "dependencies": {
    "@bumpinto/shared": "workspace:*",
    "@stomp/stompjs": "^7.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.1.0",
    "zustand": "^5.0.2"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.0",
    "@testing-library/react": "^16.1.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "jsdom": "^25.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
```

- [ ] **Step 2: vite.config.ts / tsconfig.json / test-setup.ts / index.html**

`vite.config.ts`:

```typescript
import react from "@vitejs/plugin-react";
import { defineConfig } from "vite";

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      "/api": "http://localhost:8080",
      "/ws": { target: "ws://localhost:8080", ws: true },
    },
  },
  test: {
    environment: "jsdom",
    setupFiles: ["./src/test-setup.ts"],
  },
});
```

`tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "types": ["vitest/globals", "@testing-library/jest-dom"]
  },
  "include": ["src", "../shared/src"]
}
```

`src/test-setup.ts`:

```typescript
import "@testing-library/jest-dom/vitest";
```

`index.html`:

```html
<!doctype html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>BumpInto — ortada buluşun</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Bricolage+Grotesque:opsz,wght@12..96,500;12..96,700;12..96,800&family=Figtree:wght@400;500;600;700&family=Caveat:wght@600&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 3: styles/tokens.css** (kaynak: Design System v2 — çelişkide o kazanır)

```css
:root {
  --paper: #fffbf6;
  --card: #ffffff;
  --ink: #27203b;
  --ink2: #6e6584;
  --ink3: #a79db8;
  --flame: #fd3e6b;
  --flame-deep: #de2456;
  --flame-wash: #ffe9ef;
  --sun: #ffc93c;
  --hl: #ffe27a;
  --grass: #0b7a44;
  --grass-wash: #dff5e9;
  --violet: #6234d8;
  --amber: #a96a0b;
  --amber-wash: #fff1d6;
  --line: #f1e8de;
  --line2: #e4d9cd;
  --line-in: #91869c;
  --font-head: "Bricolage Grotesque", system-ui, sans-serif;
  --font-body: "Figtree", system-ui, sans-serif;
  --font-hand: "Caveat", cursive;
  --grad: linear-gradient(120deg, #fd3e6b 10%, #ff7854 90%);
  --sh1: 0 1px 3px rgba(39, 32, 59, 0.05), 0 4px 14px rgba(39, 32, 59, 0.06);
  --sh2: 0 2px 6px rgba(39, 32, 59, 0.08), 0 12px 30px rgba(39, 32, 59, 0.12);
  --r: 22px;
}
```

- [ ] **Step 4: styles/ui.css** (yalnız atomların sınıfları + sayfa yerleşimi — sayfalarda ham stil yok)

```css
* {
  box-sizing: border-box;
}
body {
  margin: 0;
  background: var(--paper);
  color: var(--ink);
  font-family: var(--font-body);
  font-size: 16px;
  line-height: 1.5;
}
.page {
  max-width: 480px;
  margin: 0 auto;
  padding: 20px 18px 32px;
  min-height: 100dvh;
  display: flex;
  flex-direction: column;
  gap: 15px;
}
h1,
h2,
h3 {
  font-family: var(--font-head);
  letter-spacing: -0.02em;
  margin: 0;
}
h1 {
  font-size: 34px;
  font-weight: 800;
  line-height: 1.08;
}
.a-hl {
  background: linear-gradient(100deg, transparent 0 1%, var(--hl) 4% 96%, transparent 99%);
  padding: 1px 8px;
  border-radius: 8px;
  display: inline-block;
  transform: rotate(-1.2deg);
}
.a-hand {
  font-family: var(--font-hand);
  font-size: 19px;
  color: var(--ink2);
  transform: rotate(-1.5deg);
}
.a-card {
  background: var(--card);
  border: 1px solid var(--line);
  border-radius: var(--r);
  box-shadow: var(--sh1);
  padding: 16px;
}
.a-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 9px;
  min-height: 52px;
  width: 100%;
  padding: 0 22px;
  border-radius: 999px;
  border: 1.5px solid transparent;
  font-family: var(--font-head);
  font-size: 16px;
  font-weight: 700;
  cursor: pointer;
  text-decoration: none;
  color: inherit;
}
.a-btn:focus-visible {
  outline: 2.5px solid var(--flame-deep);
  outline-offset: 3px;
}
.a-btn--flame {
  background: var(--flame-deep);
  color: #fff;
  box-shadow: 0 8px 24px rgba(222, 36, 86, 0.3);
}
.a-btn--white {
  background: var(--card);
  color: var(--ink);
  border-color: var(--line2);
  box-shadow: var(--sh1);
}
.a-btn--round {
  border-radius: 50%;
  padding: 0;
  width: 64px;
  min-height: 64px;
  font-size: 22px;
}
.a-btn--round-sm {
  width: 46px;
  min-height: 46px;
  font-size: 16px;
}
.a-btn--grad {
  background: var(--grad);
  color: #fff;
  box-shadow: 0 10px 26px rgba(222, 36, 86, 0.35);
}
.a-btn:disabled {
  opacity: 0.45;
  box-shadow: none;
}
.a-inp {
  min-height: 52px;
  padding: 0 18px;
  border-radius: 16px;
  border: 1.5px solid var(--line-in);
  background: var(--card);
  font-size: 16px;
  font-family: var(--font-body);
  color: var(--ink);
  width: 100%;
}
.a-inp:focus {
  outline: none;
  border-color: var(--flame-deep);
  box-shadow: 0 0 0 3px var(--flame-wash);
}
.a-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 4.5px 11px;
  border-radius: 999px;
  font-size: 12px;
  font-weight: 700;
  white-space: nowrap;
}
.a-badge--flame { background: var(--flame-wash); color: var(--flame-deep); }
.a-badge--grass { background: var(--grass-wash); color: var(--grass); }
.a-badge--amber { background: var(--amber-wash); color: var(--amber); }
.a-badge--neutral { background: #f4eee6; color: var(--ink2); }
.a-chip {
  display: inline-flex;
  align-items: center;
  min-height: 46px;
  padding: 0 18px;
  border-radius: 999px;
  border: 1.5px solid var(--line2);
  background: var(--card);
  font-size: 14.5px;
  font-weight: 600;
  color: var(--ink2);
  cursor: pointer;
  font-family: var(--font-body);
}
.a-chip--on {
  background: var(--flame-wash);
  border-color: var(--flame-deep);
  color: var(--flame-deep);
}
.a-sticker {
  display: inline-flex;
  padding: 6px 13px;
  border-radius: 12px;
  font-family: var(--font-head);
  font-size: 12.5px;
  font-weight: 800;
  color: var(--ink);
  background: var(--sun);
  border: 1.5px solid var(--ink);
  box-shadow: 2px 3px 0 rgba(39, 32, 59, 0.18);
  transform: rotate(-2.5deg);
}
.a-avatar {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: var(--font-head);
  font-weight: 700;
  font-size: 14px;
  color: #fff;
  flex: 0 0 auto;
}
.a-progress {
  height: 7px;
  border-radius: 4px;
  background: #f0e9e0;
  overflow: hidden;
}
.a-progress > i {
  display: block;
  height: 100%;
  background: var(--grad);
  border-radius: 4px;
}
.row {
  display: flex;
  align-items: center;
  gap: 10px;
}
.field {
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.label {
  font-size: 14px;
  font-weight: 600;
}
.muted {
  color: var(--ink2);
  font-size: 13px;
}
.err {
  font-size: 13px;
  color: #c0392b;
}
@media (prefers-reduced-motion: reduce) {
  * {
    animation: none !important;
    transition: none !important;
  }
}
```

- [ ] **Step 5: lib/api.ts + ortam dosyaları**

`src/lib/api.ts` — web'de token saklama YOK; cookie'ler tarayıcıda:

```typescript
import { createBumpintoApi, createHttp } from "@bumpinto/shared";

export const api = createBumpintoApi(
  createHttp(import.meta.env.VITE_API_URL ?? "", {}, {
    withCredentials: true, // HttpOnly cookie'ler her istekte taşınır
    client: "web",         // backend token'ı cookie'ye yazar, body'ye koymaz
  }),
);
```

`.env.development` (local — Vite proxy, same-origin):

```ini
VITE_API_URL=
VITE_WS_URL=
```

`.env.preprod`:

```ini
VITE_API_URL=https://api.preprod.bumpinto.app
VITE_WS_URL=wss://api.preprod.bumpinto.app/ws
```

`.env.production`:

```ini
VITE_API_URL=https://api.bumpinto.app
VITE_WS_URL=wss://api.bumpinto.app/ws
```

- [ ] **Step 6: Atom bileşenlerini yaz** (her biri kendi dosyasında — reusable, sınıflar yalnız burada)

`atoms/Button.tsx`:

```tsx
import type { ButtonHTMLAttributes, ReactNode } from "react";

type Props = ButtonHTMLAttributes<HTMLButtonElement> & {
  kind?: "flame" | "white" | "grad";
  shape?: "pill" | "round" | "round-sm";
  children: ReactNode;
};

export default function Button({ kind = "flame", shape = "pill", children, ...rest }: Props) {
  const classes = ["a-btn", `a-btn--${kind}`];
  if (shape !== "pill") classes.push("a-btn--round");
  if (shape === "round-sm") classes.push("a-btn--round-sm");
  return (
    <button {...rest} className={classes.join(" ")}>
      {children}
    </button>
  );
}
```

`atoms/TextInput.tsx`:

```tsx
import type { InputHTMLAttributes } from "react";

export default function TextInput(props: InputHTMLAttributes<HTMLInputElement>) {
  return <input {...props} className="a-inp" />;
}
```

`atoms/Badge.tsx`:

```tsx
import type { ReactNode } from "react";

export default function Badge(props: {
  tone?: "flame" | "grass" | "amber" | "neutral";
  children: ReactNode;
}) {
  return <span className={`a-badge a-badge--${props.tone ?? "neutral"}`}>{props.children}</span>;
}
```

`atoms/Chip.tsx`:

```tsx
export default function Chip(props: { label: string; on?: boolean; onClick: () => void }) {
  return (
    <button
      type="button"
      aria-pressed={props.on}
      onClick={props.onClick}
      className={`a-chip${props.on ? " a-chip--on" : ""}`}
    >
      {props.label}
    </button>
  );
}
```

`atoms/Sticker.tsx`:

```tsx
import type { CSSProperties, ReactNode } from "react";

export default function Sticker(props: { children: ReactNode; style?: CSSProperties }) {
  return (
    <span className="a-sticker" style={props.style}>
      {props.children}
    </span>
  );
}
```

`atoms/Avatar.tsx`:

```tsx
const PALETTE = [
  "linear-gradient(135deg,#fd3e6b,#d91e52)",
  "linear-gradient(135deg,#18b26b,#0b7a44)",
  "linear-gradient(135deg,#7c4dff,#5a2fd0)",
  "linear-gradient(135deg,#ffb020,#e08900)",
];

export default function Avatar(props: { name: string; index?: number }) {
  return (
    <span
      className="a-avatar"
      style={{ background: PALETTE[(props.index ?? 0) % PALETTE.length] }}
      aria-hidden
    >
      {props.name[0]?.toUpperCase()}
    </span>
  );
}
```

`atoms/Progress.tsx`:

```tsx
export default function Progress(props: { value: number }) {
  return (
    <div className="a-progress" aria-hidden>
      <i style={{ width: `${Math.min(100, props.value * 100)}%` }} />
    </div>
  );
}
```

`atoms/Highlight.tsx`:

```tsx
import type { ReactNode } from "react";

export default function Highlight({ children }: { children: ReactNode }) {
  return <span className="a-hl">{children}</span>;
}
```

`atoms/HandNote.tsx`:

```tsx
import type { CSSProperties, ReactNode } from "react";

export default function HandNote(props: { children: ReactNode; style?: CSSProperties }) {
  return (
    <p className="a-hand" style={props.style}>
      {props.children}
    </p>
  );
}
```

`atoms/index.ts`:

```typescript
export { default as Avatar } from "./Avatar";
export { default as Badge } from "./Badge";
export { default as Button } from "./Button";
export { default as Chip } from "./Chip";
export { default as HandNote } from "./HandNote";
export { default as Highlight } from "./Highlight";
export { default as Progress } from "./Progress";
export { default as Sticker } from "./Sticker";
export { default as TextInput } from "./TextInput";
```

- [ ] **Step 7: main.tsx + App.tsx + smoke test**

`src/main.tsx`:

```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import App from "./App";
import "./styles/tokens.css";
import "./styles/ui.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>,
);
```

`src/App.tsx`:

```tsx
import { Route, Routes } from "react-router-dom";
import Highlight from "./components/atoms/Highlight";
import SessionPage from "./pages/SessionPage";

export default function App() {
  return (
    <Routes>
      <Route path="/j/:slug" element={<SessionPage />} />
      <Route
        path="*"
        element={
          <main className="page" style={{ justifyContent: "center" }}>
            <h1>
              Ortada <Highlight>buluşalım.</Highlight>
            </h1>
            <p className="muted">Bir davet linkin olmalı — örn. bumpinto.app/j/x7k2m</p>
          </main>
        }
      />
    </Routes>
  );
}
```

Geçici `src/pages/SessionPage.tsx` (Task 4'te gerçek haliyle DEĞİŞTİRİLİR):

```tsx
export default function SessionPage() {
  return <main className="page">yükleniyor…</main>;
}
```

`src/App.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { MemoryRouter } from "react-router-dom";
import { describe, expect, it } from "vitest";
import App from "./App";

describe("App", () => {
  it("davet linki olmayan ziyaretçiye açıklama gösterir", () => {
    render(
      <MemoryRouter initialEntries={["/"]}>
        <App />
      </MemoryRouter>,
    );
    expect(screen.getByText(/davet linkin olmalı/i)).toBeInTheDocument();
  });
});
```

- [ ] **Step 8: Doğrula** — Run: `rtk pnpm install && rtk pnpm test:web` → `1 passed`.

- [ ] **Step 9: INDEX güncelle + Commit (kullanıcı)** — `feat(web): iskelet + atom bileşenleri`

---

### Task 4: Zustand store'ları + SessionPage + Katıl (W1)

**Files:**
- Create: `frontend/web/src/store/sessionStore.ts`
- Create: `frontend/web/src/store/useSessionLive.ts`
- Create: `frontend/web/src/components/molecules/Field.tsx`
- Modify: `frontend/web/src/pages/SessionPage.tsx`
- Create: `frontend/web/src/pages/JoinForm.tsx`

- [ ] **Step 1: sessionStore.ts**

```typescript
import type { SessionView } from "@bumpinto/shared";
import { AxiosError } from "axios";
import { create } from "zustand";
import { api } from "../lib/api";

type SessionState = {
  slug: string;
  view: SessionView | null;
  needsJoin: boolean;
  error: string | null;
  bind: (slug: string) => void;
  refresh: () => Promise<void>;
};

export const useSessionStore = create<SessionState>((set, get) => ({
  slug: "",
  view: null,
  needsJoin: false,
  error: null,

  bind: (slug) => set({ slug, view: null, needsJoin: false, error: null }),

  refresh: async () => {
    const { slug } = get();
    if (!slug) return;
    try {
      const view = await api.getSession(slug);
      set({ view, needsJoin: false, error: null });
    } catch (e) {
      const status = e instanceof AxiosError ? e.response?.status : undefined;
      if (status === 401) set({ needsJoin: true, view: null });
      else if (status === 404) set({ error: "Bu oturum bulunamadı — link doğru mu?" });
      // diğer hatalar: mevcut görünümü koru, polling tekrar dener
    }
  },
}));
```

- [ ] **Step 2: useSessionLive.ts** (yaşam döngüsü: polling + STOMP → store.refresh)

```typescript
import { Client } from "@stomp/stompjs";
import { useEffect } from "react";
import { useSessionStore } from "./sessionStore";

const POLL_MS = 3000;

export function useSessionLive(slug: string) {
  const bind = useSessionStore((s) => s.bind);
  const refresh = useSessionStore((s) => s.refresh);

  useEffect(() => {
    bind(slug);
    void refresh();
    const timer = setInterval(() => void refresh(), POLL_MS);

    const wsUrl =
      (import.meta.env.VITE_WS_URL as string | undefined) ??
      `${location.protocol === "https:" ? "wss" : "ws"}://${location.host}/ws`;
    const client = new Client({
      brokerURL: wsUrl,
      reconnectDelay: 5000,
      onConnect: () => client.subscribe(`/topic/session/${slug}`, () => void refresh()),
    });
    client.activate();

    return () => {
      clearInterval(timer);
      void client.deactivate();
    };
  }, [slug, bind, refresh]);
}
```

- [ ] **Step 3: molecules/Field.tsx** (label + input + hata — form molekülü)

```tsx
import type { InputHTMLAttributes } from "react";
import TextInput from "../atoms/TextInput";

type Props = InputHTMLAttributes<HTMLInputElement> & { label: string; error?: string | null };

export default function Field({ label, error, id, ...rest }: Props) {
  return (
    <div className="field">
      <label className="label" htmlFor={id}>
        {label}
      </label>
      <TextInput id={id} aria-invalid={!!error} {...rest} />
      {error && (
        <p className="err" role="alert">
          {error}
        </p>
      )}
    </div>
  );
}
```

- [ ] **Step 4: SessionPage.tsx** (yalnız kompozisyon — stil yok)

```tsx
import { useParams } from "react-router-dom";
import { useSessionLive } from "../store/useSessionLive";
import { useSessionStore } from "../store/sessionStore";
import DeckScreen from "./DeckScreen";
import JoinForm from "./JoinForm";
import ResultScreen from "./ResultScreen";
import RunoffScreen from "./RunoffScreen";
import WaitingRoom from "./WaitingRoom";

export default function SessionPage() {
  const { slug = "" } = useParams();
  useSessionLive(slug);
  const { view, needsJoin, error, refresh } = useSessionStore();

  if (error) {
    return (
      <main className="page" style={{ justifyContent: "center" }}>
        <h1>Hmm.</h1>
        <p className="muted">{error}</p>
      </main>
    );
  }
  if (needsJoin || !view) {
    return <JoinForm slug={slug} onJoined={() => void refresh()} />;
  }
  switch (view.status) {
    case "COLLECTING":
    case "SUGGESTING":
      return <WaitingRoom view={view} />;
    case "SWIPING":
      return <DeckScreen slug={slug} view={view} />;
    case "RUNOFF":
      return <RunoffScreen slug={slug} view={view} />;
    case "DECIDED":
      return <ResultScreen view={view} />;
    default:
      return (
        <main className="page">
          <p className="muted">Bu oturumun süresi dolmuş.</p>
        </main>
      );
  }
}
```

Derleme için `WaitingRoom/DeckScreen/RunoffScreen/ResultScreen` geçici
`export default () => null;` dosyaları olarak açılır; Task 5-7'de gerçek içerikle değiştirilir.

- [ ] **Step 5: JoinForm.tsx** (artboard W1 — MCP'den güncel halini oku!)

```tsx
import { useState } from "react";
import { api } from "../lib/api";
import { Avatar, Button, Highlight } from "../components/atoms";
import Field from "../components/molecules/Field";

type Coords = { lat: number; lng: number; label: string };

// MVP tavizi (belgeli): Nominatim istemciden; trafik artarsa backend proxy.
async function geocode(query: string): Promise<Coords | null> {
  const res = await fetch(
    `https://nominatim.openstreetmap.org/search?format=json&limit=1&q=${encodeURIComponent(query)}`,
    { headers: { Accept: "application/json" } },
  );
  const rows: { lat: string; lon: string; display_name: string }[] = await res.json();
  if (!rows.length) return null;
  return {
    lat: Number(rows[0].lat),
    lng: Number(rows[0].lon),
    label: rows[0].display_name.split(",")[0],
  };
}

export default function JoinForm(props: { slug: string; onJoined: () => void }) {
  const [name, setName] = useState("");
  const [address, setAddress] = useState("");
  const [coords, setCoords] = useState<Coords | null>(null);
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState<string | null>(null);

  function useMyLocation() {
    setError(null);
    navigator.geolocation.getCurrentPosition(
      (pos) =>
        setCoords({
          lat: pos.coords.latitude,
          lng: pos.coords.longitude,
          label: "Mevcut konumun",
        }),
      () => setError("Konum izni alınamadı — adres yazabilirsin."),
    );
  }

  async function submit(e: React.FormEvent) {
    e.preventDefault();
    setBusy(true);
    setError(null);
    try {
      let location = coords;
      if (!location && address.trim()) {
        location = await geocode(address.trim());
        if (!location) {
          setError("Bu adres bulunamadı — yakındaki bir şehri dene.");
          return;
        }
      }
      await api.join(props.slug, {
        displayName: name.trim(),
        lat: location?.lat,
        lng: location?.lng,
      });
      props.onJoined(); // token HttpOnly cookie'de — web'de saklanmaz
    } catch {
      setError("Katılamadın — bu oturum kapanmış olabilir.");
    } finally {
      setBusy(false);
    }
  }

  return (
    <main className="page">
      <div className="row">
        <Avatar name="B" />
        <span>Arkadaşın seni buluşmaya çağırdı</span>
      </div>
      <h1>
        <Highlight>Buluşmaya</Highlight> katıl
      </h1>
      <p className="muted">Konumunu at, ortada buluşalım. Hesap filan gerekmez.</p>

      <form onSubmit={submit} className="field" style={{ gap: 15 }}>
        <Field
          id="join-name"
          label="Adın"
          value={name}
          onChange={(e) => setName(e.target.value)}
          placeholder="Arkadaşların sana ne der?"
          autoComplete="name"
        />
        <div className="field">
          <span className="label">Neredesin?</span>
          <Button type="button" kind="white" onClick={useMyLocation}>
            {coords ? coords.label : "Mevcut konumumu kullan"}
          </Button>
          <Field
            id="join-address"
            label="…ya da şehir / adres yaz"
            value={address}
            onChange={(e) => {
              setAddress(e.target.value);
              setCoords(null);
            }}
          />
        </div>
        {error && (
          <p className="err" role="alert">
            {error}
          </p>
        )}
        <Button type="submit" disabled={busy || !name.trim()}>
          Katıl
        </Button>
        <p className="muted" style={{ textAlign: "center" }}>
          Konumun yalnızca bu buluşma için kullanılır.
        </p>
      </form>
    </main>
  );
}
```

- [ ] **Step 6: Doğrula** — Run: `rtk pnpm --filter @bumpinto/web exec tsc --noEmit && rtk pnpm test:web` → yeşil.

- [ ] **Step 7: INDEX güncelle + Commit (kullanıcı)** — `feat(web): zustand storelari + katil akisi`

---

### Task 5: Bekleme odası (W2) — ParticipantList organizması

**Files:**
- Create: `frontend/web/src/components/molecules/ParticipantRow.tsx`
- Create: `frontend/web/src/components/organisms/ParticipantList.tsx`
- Modify: `frontend/web/src/pages/WaitingRoom.tsx`

- [ ] **Step 1: molecules/ParticipantRow.tsx**

```tsx
import type { ParticipantDto } from "@bumpinto/shared";
import { Avatar, Badge } from "../atoms";

export default function ParticipantRow(props: { participant: ParticipantDto; index: number }) {
  const p = props.participant;
  return (
    <div className="row" style={{ padding: "13px 16px" }}>
      <Avatar name={p.displayName ?? "?"} index={props.index} />
      <span style={{ flex: 1, fontWeight: 700 }}>
        {p.displayName}
        {p.host && <span className="muted"> · kuran</span>}
      </span>
      <Badge tone={p.hasLocation ? "grass" : "amber"}>
        {p.hasLocation ? "Hazır" : "Bekliyor"}
      </Badge>
    </div>
  );
}
```

- [ ] **Step 2: organisms/ParticipantList.tsx**

```tsx
import type { ParticipantDto } from "@bumpinto/shared";
import { Progress } from "../atoms";
import ParticipantRow from "../molecules/ParticipantRow";

export default function ParticipantList({ participants }: { participants: ParticipantDto[] }) {
  const ready = participants.filter((p) => p.hasLocation).length;
  return (
    <>
      <div className="row" style={{ justifyContent: "space-between" }}>
        <span className="label">Kimler var</span>
        <span className="muted">
          {ready} / {participants.length} hazır
        </span>
      </div>
      <Progress value={ready / Math.max(participants.length, 1)} />
      <div className="a-card" style={{ padding: "4px 0" }}>
        {participants.map((p, i) => (
          <ParticipantRow key={p.id} participant={p} index={i} />
        ))}
      </div>
    </>
  );
}
```

- [ ] **Step 3: pages/WaitingRoom.tsx** (artboard W2 — MCP'den oku)

```tsx
import type { SessionView } from "@bumpinto/shared";
import { HandNote, Sticker } from "../components/atoms";
import ParticipantList from "../components/organisms/ParticipantList";

export default function WaitingRoom({ view }: { view: SessionView }) {
  return (
    <main className="page">
      <Sticker>hazırlanıyor…</Sticker>
      <h1 style={{ marginTop: 6 }}>{view.name ?? "Buluşma"}</h1>
      <p className="muted">Mekanlar gelince buradan kaydıracaksın — sayfayı kapatma yeter.</p>
      <ParticipantList participants={view.participants ?? []} />
      <HandNote>not: kuran kişi "mekanları bul" deyince başlıyoruz</HandNote>
    </main>
  );
}
```

- [ ] **Step 4: Doğrula + INDEX + Commit (kullanıcı)** — `rtk pnpm --filter @bumpinto/web exec tsc --noEmit` → `feat(web): bekleme odasi`

---

### Task 6: Deste (W3) — deckStore + VenueDeck organizması

**Files:**
- Create: `frontend/web/src/store/deckStore.ts`
- Create: `frontend/web/src/components/molecules/VenueCard.tsx`
- Create: `frontend/web/src/components/molecules/DeckActions.tsx`
- Create: `frontend/web/src/components/organisms/VenueDeck.tsx`
- Modify: `frontend/web/src/pages/DeckScreen.tsx`

- [ ] **Step 1: store/deckStore.ts** (deste etkileşimi — API çağrıları burada, bileşenlerde değil)

```typescript
import { create } from "zustand";
import { api } from "../lib/api";

type DeckState = {
  slug: string;
  index: number;
  liked: Record<string, boolean>;
  listMode: boolean;
  sending: boolean;
  start: (slug: string, venueCount: number) => void;
  decide: (venueId: string, like: boolean) => Promise<void>;
  setLike: (venueId: string, like: boolean) => Promise<void>;
  undo: (venueId: string) => Promise<void>;
  setListMode: (on: boolean) => void;
  finish: () => Promise<void>;
};

export const useDeckStore = create<DeckState>((set, get) => ({
  slug: "",
  index: 0,
  liked: {},
  listMode: false,
  sending: false,

  start: (slug, venueCount) => {
    if (get().slug === slug) return; // yeniden mount'ta ilerlemeyi koru
    set({ slug, index: 0, liked: {}, listMode: venueCount < 6 }); // az sonuç → liste (spec §4)
  },

  decide: async (venueId, like) => {
    set((s) => ({ index: s.index + 1, liked: { ...s.liked, [venueId]: like } }));
    await api.swipe(get().slug, { venueId, liked: like });
  },

  setLike: async (venueId, like) => {
    set((s) => ({ liked: { ...s.liked, [venueId]: like } }));
    await api.swipe(get().slug, { venueId, liked: like });
  },

  undo: async (venueId) => {
    set((s) => {
      const liked = { ...s.liked };
      delete liked[venueId];
      return { index: Math.max(0, s.index - 1), liked };
    });
    await api.undoSwipe(get().slug, venueId);
  },

  setListMode: (on) => set({ listMode: on }),

  finish: async () => {
    set({ sending: true });
    try {
      await api.deckDone(get().slug);
    } finally {
      set({ sending: false });
    }
  },
}));
```

- [ ] **Step 2: molecules/VenueCard.tsx** (reusable — deste, liste, runoff, sonuç hepsi bunu kullanır)

```tsx
import type { CSSProperties } from "react";
import type { VenueDto } from "@bumpinto/shared";
import { Badge } from "../atoms";

const PHOTO_FALLBACKS = [
  "radial-gradient(130% 100% at 18% 8%,#ffd9a8 0%,transparent 62%),linear-gradient(165deg,#f9c08a,#e8794f)",
  "radial-gradient(130% 100% at 80% 6%,#b8f0d8 0%,transparent 60%),linear-gradient(165deg,#8fddbb,#2f9e71)",
  "radial-gradient(130% 100% at 22% 10%,#d9c8ff 0%,transparent 60%),linear-gradient(165deg,#c1a8f5,#7c4dff)",
];

export default function VenueCard(props: {
  venue: VenueDto;
  photoHeight?: number;
  style?: CSSProperties;
}) {
  const v = props.venue;
  const fallback = PHOTO_FALLBACKS[(v.deckOrder ?? 0) % PHOTO_FALLBACKS.length];
  const monogram = v.name
    ?.split(" ")
    .map((w) => w[0])
    .slice(0, 2)
    .join("")
    .toLowerCase();
  return (
    <div className="a-card" style={{ padding: 10, borderRadius: 24, ...props.style }}>
      <div
        style={{
          height: props.photoHeight ?? 260,
          borderRadius: 16,
          background: v.photoUrl ? `url(${v.photoUrl}) center/cover` : fallback,
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
        }}
      >
        {!v.photoUrl && (
          <span
            style={{
              fontFamily: "var(--font-head)",
              fontWeight: 800,
              fontSize: 40,
              color: "rgba(255,255,255,.5)",
              transform: "rotate(-4deg)",
            }}
          >
            {monogram}
          </span>
        )}
      </div>
      <div style={{ padding: "12px 8px 6px", display: "flex", flexDirection: "column", gap: 8 }}>
        <div className="row" style={{ justifyContent: "space-between" }}>
          <h2 style={{ fontSize: 20 }}>{v.name}</h2>
          {v.rating != null && <Badge tone="grass">★ {v.rating}</Badge>}
        </div>
        <div className="row" style={{ flexWrap: "wrap", gap: 6 }}>
          {v.priceLevel != null && <Badge>{"€".repeat(v.priceLevel)}</Badge>}
          {Object.entries(v.travelMinutes ?? {}).map(([who, min]) => (
            <Badge key={who}>
              {who} {min} dk
            </Badge>
          ))}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: molecules/DeckActions.tsx** (geri al / geç / beğen — WCAG buton alternatifi)

```tsx
import { Button } from "../atoms";

export default function DeckActions(props: {
  onUndo: () => void;
  onPass: () => void;
  onLike: () => void;
}) {
  return (
    <div className="row" style={{ justifyContent: "center", gap: 20 }}>
      <Button type="button" kind="white" shape="round-sm" aria-label="Geri al" onClick={props.onUndo}>
        ↺
      </Button>
      <Button type="button" kind="white" shape="round" aria-label="Geç" onClick={props.onPass}>
        ✕
      </Button>
      <Button type="button" kind="grad" shape="round" aria-label="Beğen" onClick={props.onLike}>
        ♥
      </Button>
    </div>
  );
}
```

- [ ] **Step 4: organisms/VenueDeck.tsx** (kart yığını + klavye + store bağlama)

```tsx
import { useEffect } from "react";
import type { VenueDto } from "@bumpinto/shared";
import { useDeckStore } from "../../store/deckStore";
import { Progress } from "../atoms";
import DeckActions from "../molecules/DeckActions";
import VenueCard from "../molecules/VenueCard";

export default function VenueDeck({ venues }: { venues: VenueDto[] }) {
  const { index, decide, undo } = useDeckStore();
  const current = venues[index];
  const next = venues[index + 1];

  useEffect(() => {
    function onKey(e: KeyboardEvent) {
      if (!current) return;
      if (e.key === "ArrowRight") void decide(current.id!, true);
      if (e.key === "ArrowLeft") void decide(current.id!, false);
      if (e.key === "Backspace" && venues[index - 1]) void undo(venues[index - 1].id!);
    }
    window.addEventListener("keydown", onKey);
    return () => window.removeEventListener("keydown", onKey);
  }, [current, decide, undo, venues, index]);

  if (!current) return null;
  return (
    <>
      <Progress value={index / venues.length} />
      <div style={{ position: "relative" }}>
        {next && (
          <div
            style={{
              position: "absolute",
              inset: 0,
              transform: "rotate(2.6deg) scale(.97)",
              opacity: 0.6,
            }}
          >
            <VenueCard venue={next} />
          </div>
        )}
        <div style={{ transform: "rotate(-1.6deg)" }}>
          <VenueCard venue={current} />
        </div>
      </div>
      <DeckActions
        onUndo={() => venues[index - 1] && void undo(venues[index - 1].id!)}
        onPass={() => void decide(current.id!, false)}
        onLike={() => void decide(current.id!, true)}
      />
      <p className="muted" style={{ textAlign: "center" }}>
        ← geç · → beğen · butonlar da aynı işi yapar
      </p>
    </>
  );
}
```

- [ ] **Step 5: pages/DeckScreen.tsx** (artboard W3 — kompozisyon; liste modu + bitiş)

```tsx
import { useEffect, useMemo } from "react";
import type { SessionView } from "@bumpinto/shared";
import { Button, Highlight } from "../components/atoms";
import VenueCard from "../components/molecules/VenueCard";
import VenueDeck from "../components/organisms/VenueDeck";
import { useDeckStore } from "../store/deckStore";

export default function DeckScreen(props: { slug: string; view: SessionView }) {
  const venues = useMemo(
    () => [...(props.view.venues ?? [])].sort((a, b) => (a.deckOrder ?? 0) - (b.deckOrder ?? 0)),
    [props.view.venues],
  );
  const deck = useDeckStore();

  useEffect(() => {
    deck.start(props.slug, venues.length);
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [props.slug, venues.length]);

  const finished = deck.index >= venues.length;
  const likedCount = Object.values(deck.liked).filter(Boolean).length;

  if (finished && !deck.listMode) {
    return (
      <main className="page" style={{ justifyContent: "center", textAlign: "center" }}>
        <h1>
          Deste <Highlight>bitti!</Highlight>
        </h1>
        <p className="muted">{likedCount} mekanı beğendin.</p>
        <Button onClick={() => void deck.finish()} disabled={deck.sending}>
          Beğenilerimi gönder
        </Button>
        <Button kind="white" onClick={() => deck.setListMode(true)}>
          Listeye dön, düzelt
        </Button>
      </main>
    );
  }

  if (deck.listMode) {
    return (
      <main className="page">
        <div className="row" style={{ justifyContent: "space-between" }}>
          <h1 style={{ fontSize: 26 }}>Hangisi olsun?</h1>
        </div>
        {venues.map((v) => (
          <label key={v.id} className="row" style={{ alignItems: "stretch" }}>
            <input
              type="checkbox"
              checked={!!deck.liked[v.id!]}
              onChange={(e) => void deck.setLike(v.id!, e.target.checked)}
              style={{ width: 22, accentColor: "var(--flame-deep)" }}
            />
            <div style={{ flex: 1 }}>
              <VenueCard venue={v} photoHeight={120} />
            </div>
          </label>
        ))}
        <Button onClick={() => void deck.finish()} disabled={deck.sending}>
          Beğenilerimi gönder
        </Button>
      </main>
    );
  }

  return (
    <main className="page">
      <div className="row" style={{ justifyContent: "space-between" }}>
        <span className="label">
          {Math.min(deck.index + 1, venues.length)} / {venues.length}
        </span>
        <Button
          kind="white"
          style={{ width: "auto", minHeight: 36, fontSize: 13 }}
          onClick={() => deck.setListMode(true)}
        >
          Hepsini gör
        </Button>
      </div>
      <VenueDeck venues={venues} />
    </main>
  );
}
```

Not (belgeli): dokunmatik sürükleme jesti mobilde (Plan 4); `↺ ✕ ♥` karakterleri ikon
setine geçene dek geçici.

- [ ] **Step 6: Doğrula + INDEX + Commit (kullanıcı)** — tsc + test yeşil → `feat(web): deste ekrani`

---

### Task 7: Runoff + Sonuç (W4) + build

**Files:**
- Create: `frontend/web/src/components/molecules/ViralCard.tsx`
- Create: `frontend/web/src/components/organisms/RunoffList.tsx`
- Modify: `frontend/web/src/pages/RunoffScreen.tsx`
- Modify: `frontend/web/src/pages/ResultScreen.tsx`

- [ ] **Step 1: organisms/RunoffList.tsx**

```tsx
import { useState } from "react";
import type { VenueDto } from "@bumpinto/shared";
import { api } from "../../lib/api";
import { Button, HandNote } from "../atoms";
import VenueCard from "../molecules/VenueCard";

export default function RunoffList(props: { slug: string; finalists: VenueDto[] }) {
  const [choice, setChoice] = useState<string | null>(null);
  const [sent, setSent] = useState(false);

  async function lockIn() {
    if (!choice) return;
    await api.runoffVote(props.slug, { venueId: choice });
    setSent(true);
  }

  return (
    <>
      {props.finalists.map((v) => (
        <button
          key={v.id}
          type="button"
          onClick={() => !sent && setChoice(v.id!)}
          style={{
            all: "unset",
            cursor: "pointer",
            borderRadius: 24,
            outline: choice === v.id ? "2.5px solid var(--flame-deep)" : "none",
            outlineOffset: 3,
          }}
        >
          <VenueCard venue={v} photoHeight={140} />
        </button>
      ))}
      {sent ? (
        <HandNote style={{ textAlign: "center" }}>
          seçimin kilitli — diğerlerini bekliyoruz
        </HandNote>
      ) : (
        <Button onClick={() => void lockIn()} disabled={!choice}>
          Seçimimi kilitle
        </Button>
      )}
    </>
  );
}
```

- [ ] **Step 2: pages/RunoffScreen.tsx** (kaynak: mobil `07 Runoff` artboard'u)

```tsx
import type { SessionView } from "@bumpinto/shared";
import { Sticker } from "../components/atoms";
import RunoffList from "../components/organisms/RunoffList";

export default function RunoffScreen(props: { slug: string; view: SessionView }) {
  const finalists = (props.view.venues ?? []).filter((v) =>
    props.view.runoffVenueIds?.includes(v.id!),
  );
  return (
    <main className="page">
      <Sticker>Son düzlük</Sticker>
      <h1 style={{ marginTop: 6 }}>İkisi de güzel, biri kazanacak</h1>
      <p className="muted">
        Herkes bunları beğendi. Tek seçim hakkın var — kim neyi seçti, sonuçta belli olur.
      </p>
      <RunoffList slug={props.slug} finalists={finalists} />
    </main>
  );
}
```

- [ ] **Step 3: molecules/ViralCard.tsx + pages/ResultScreen.tsx** (artboard W4)

`molecules/ViralCard.tsx`:

```tsx
import { Sticker } from "../atoms";

export default function ViralCard() {
  return (
    <div
      className="a-card"
      style={{ background: "var(--flame-wash)", borderColor: "#f6c6d2", position: "relative" }}
    >
      <Sticker style={{ position: "absolute", right: 12, top: -12 }}>sıra sende</Sticker>
      <h3 style={{ fontSize: 17 }}>Sıradaki buluşma senden mi?</h3>
      <p className="muted">30 saniyede kur — arkadaşların linkle katılır.</p>
      <a className="a-btn a-btn--white" href="/" style={{ marginTop: 10 }}>
        Buluşma kur
      </a>
    </div>
  );
}
```

`pages/ResultScreen.tsx`:

```tsx
import type { SessionView } from "@bumpinto/shared";
import { Badge, HandNote, Highlight, Sticker } from "../components/atoms";
import VenueCard from "../components/molecules/VenueCard";
import ViralCard from "../components/molecules/ViralCard";

export default function ResultScreen({ view }: { view: SessionView }) {
  const winner = (view.venues ?? []).find((v) => v.id === view.decidedVenueId);
  if (!winner) return null;
  return (
    <main className="page">
      <span style={{ alignSelf: "center" }}>
        <Badge tone="flame">Ortak nokta</Badge>
      </span>
      <h1 style={{ textAlign: "center" }}>
        <Highlight>{winner.name}</Highlight>
      </h1>
      <div style={{ position: "relative" }}>
        <Sticker style={{ position: "absolute", right: 10, top: -14, zIndex: 2 }}>
          karar verildi!
        </Sticker>
        <VenueCard venue={winner} photoHeight={180} />
      </div>
      <a className="a-btn a-btn--flame" href={winner.mapsUrl ?? "#"} target="_blank" rel="noreferrer">
        Yol tarifi al
      </a>
      <ViralCard />
      <HandNote style={{ textAlign: "center" }}>not: kutlamak serbest</HandNote>
    </main>
  );
}
```

- [ ] **Step 4: Tam doğrulama** — Run:
`rtk pnpm --filter @bumpinto/web exec tsc --noEmit && rtk pnpm test:web && rtk pnpm build:web` → hepsi yeşil.

- [ ] **Step 5: Elle uçtan uca** (backend açıkken) — kur → katıl → kaydır → sonuç.

- [ ] **Step 6: INDEX'te Plan 3'ü `done` yap + Commit (kullanıcı)** — `feat(web): runoff, sonuc, viral blok`

---

## Plan sonu doğrulaması

- [ ] Spec eşlemesi: §2 adım 3-8; §4 etkileşim kuralları (buton+klavye+geri al+hepsini
  gör+az-sonuç listesi+beğeni gizliliği); §10 Warm Pop.
- [ ] **Mimari denetimi:** pages içinde ham `<button>/<input>`/stil kopyası yok (RunoffList
  içindeki reset'li seçim sarmalayıcısı hariç — bilinçli); tüm HTTP `api.*`'dan; state
  yalnız zustand store'larında.
- [ ] **Güvenlik denetimi:** kodda `localStorage`/`sessionStorage`'a token yazan satır YOK
  (grep ile doğrula); axios `withCredentials: true` + `X-Client: web`; üç ortam dosyası mevcut.
- [ ] Her ekran Claude Design artboard'uyla karşılaştırıldı (UI Kaynağı, madde 4).
- [ ] Belgeli tavizler: sürükleme jesti mobilde; Nominatim istemciden; ikon karakterleri geçici.
- [ ] Kullanıcıya bildir: Plan 4 başlayabilir.
