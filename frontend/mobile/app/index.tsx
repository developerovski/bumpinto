import { Text, View } from "react-native";

/**
 * Yer tutucu — M-4:T7 bunu Giriş ekranıyla (artboard O2) değiştirir.
 * Ham `Text`/`View` yalnız bu iskelet dosyasında serbesttir; atomlar T4'te gelir.
 */
export default function Index() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>BumpInto</Text>
    </View>
  );
}
