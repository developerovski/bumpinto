import DiscoverScreen from "../../src/screens/DiscoverScreen";

/** Keşfet (M-11/M-12). Hesap ister: `/discover` anonim rotalar listesinde DEĞİL. */
export default function DiscoverRoute() {
  return <DiscoverScreen />;
}
