/* Shim: `nearestParticipant` `@bumpinto/shared` `geo.ts`'te (mobil `MidpointCard` de aynı
   notu basıyor). Dosya KALIR: `MidpointCard.tsx` yalnız bileşen export edebilir (Fast Refresh),
   bu yüzden çağıranlar yardımcıyı ayrı bir modülden almaya devam eder. */
export { nearestParticipant } from "@bumpinto/shared";
