/* Shim: deste kaydırma geometrisi `@bumpinto/shared`'ta (mobil deste de aynı eşikleri okur). */
export {
  DRAG_START_PX,
  FLING_VELOCITY,
  MAX_ROTATE_DEG,
  SWIPE_THRESHOLD_PX,
  VERTICAL_DAMP,
  dragProgress,
  dragRotation,
  releaseDecision,
  swipeThreshold,
  type SwipeDir,
} from "@bumpinto/shared";
