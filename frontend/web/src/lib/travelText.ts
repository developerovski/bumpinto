/* Shim: adalet cümlesinin kuralı `@bumpinto/shared` `travelText.ts`'te (mobil `RangeBar` de
   aynı cümleyi basıyor — M-7'de taşındı). İki kopya ayrışırsa aynı mekan iki istemcide farklı
   "adil mi" cevabı verirdi. */
export {
  fairnessLine,
  initialOf,
  type FairnessLine,
  type Translate,
} from "@bumpinto/shared";
